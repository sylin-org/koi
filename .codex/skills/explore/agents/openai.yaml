interface:
  display_name: "Explore"
  short_description: "Focused ownership and risk exploration before edits"
  default_prompt: "Use $explore to inspect the relevant owner and pattern, then implement with proportional verification."

# alpine-linux — journal

## 2026-09-03 16:23 UTC — PH-5 installed entry and OpenRC observer research

commit: installed product remains exact frozen source `e49bfe2b3e403fa87d4b8b237b49f3bb9e5cb5ef`; fleet dispatch `3a28f8d8b2a21f48fedce852209633d5b70c387c` | run: `ph5-alpine-entry-01` | peer: none (ordinary local operator entry) | verdict: **PASS — installed entry complete; OpenRC canary waits for Debian's neutral observer contract**
koi state now: unchanged APK-owned `koi-1.0.0_rc2_git20260903-r3`/`koi-openrc-1.0.0_rc2_git20260903-r3`; exactly one `/usr/bin/koi --daemon`, PID `2023`, under `supervise-daemon` PID `2022`, installed SHA-256 `095371b9ed13cde254214155aa231db9333342c35255717654299fcd514e9941`. The sole package-owned workbench remains `/usr/bin/koi-desktop --minimized`, PID `28433`, SHA-256 `2521d422dd9c61568a31ecce74e9a8cad89a5ead3432954633e4755a91c464de`. Operator HTTP is healthy and loopback-only at 5651; Pond is restored desired=false with 5654 closed; native mDNS is Ready generation 1 with one desired/established permanent publication; Avahi is installed but stopped/disabled.
restoration: `/etc/koi/config.toml` remains SHA-256 `f3efca4fba827cc3a9bec60aeb138f0da41545fe5beea83257ea5e5515e1c374`, local-access policy `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, member identity `5ecf787095e170708b9db9d705db44c4386c1732332f87864fbd7196f357d3eb`, Pond intent `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`, init `821510e1abb5cce853e06e09dc3ffce423e2a9af4026e775b0d5e6283b1bcb7d`, and logrotate `fb260ead3c751912c5ef6e86429f108bb6f917feaabec17ee22d72e1bf78aedb`. APK world, repositories, and network-interface hashes remain `cd4a3a6a248c1b0bac45de90d159a439331c97bba2ff106dbc91234d3374132e`, `7293f98cf79a194e28daaee94f51b201527b057ad425abb46c1eebfbfabb02a8`, and `af1cbeb3da4a0448bc120f7255d36d424f4798a70bcf6976eeb8db9a7b554fb2`. No package, service, provider, network, firewall, credential, helper, or repository residue was created.
findings:

1. From neutral directory `/home/test`, a sanitized ordinary Plasma-user environment used only the standard `/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin` path: `cargo` was unavailable and zero `KOI_*`/Cargo overrides were present. Installed `koi` found `/etc/koi/config.toml` through owner-private local control, reported the one live daemon, native mDNS routing and counts, empty explicit health/proxy state, DNS on 53, and HEALTHY/Authenticated trust for `test-03`. No endpoint or token was supplied or exposed.
2. Both `koi-desktop --poke` and an ordinary `koi-desktop` launch acknowledged/revealed the already-running installed workbench and exited without creating a second process. Its live AT-SPI tree reported `Service running`, the daemon's exact nine-rung capability ladder, and the truthful trust role `member of a mesh`, explaining that the CA and roster live on another machine and exposing `Renew identity` rather than pretending this member can mint invites.
3. The ladder's actionable unavailable state was Pond `disabled: operator-controlled read-only listener`. Activating the installed `Phone` control returned exact URL `http://192.168.1.221:5654/`; the LAN address returned HTTP 200 and `/healthz` returned `OK`, while the workbench described it as read-only with controls retained locally. `Stop sharing` then closed 5654, the same URL was refused, and the initial false intent hash plus all three PIDs remained exact. The status ladder also honestly reported the optional runtime adapter disabled rather than healthy-shaped.
4. OpenRC 0.63.2 exposes a sufficient thin native observation seam without systemd emulation. `/run/supervise-koi.pid` identifies supervisor `2022`; `/run/openrc/options/koi/child_pid` identifies daemon `2023`; `exec`, `argv_*`, `pidfile`, `start_time`, `respawn_*`, and native `start_count=0` report installed command, bounded policy, uptime, and supervised restart count. `/run/openrc/started/koi`, `rc-status -S`, and the default runlevel prove active/supervised/enabled state. `/proc` separately measured supervisor 576 KiB RSS/9 descriptors/1 thread and child 20,652 KiB RSS/22 descriptors/14 threads, plus executable, parent-child relation, command line, and start ticks. OpenRC has no cgroup `TasksCurrent` analogue; the observer must serialize that as unavailable, not zero.
5. Upstream OpenRC's `supervise-daemon` contract confirms foreground child monitoring, periodic health functions, restart-on-unhealthy behavior, and `respawn_max` within `respawn_period`; the live recipe uses the required default lifecycle functions and those native controls. The current `installed-service-collect` remains systemd-shaped and probes a generic peer TCP port. Debian's dispatched neutral-core/real-Koi-traffic contract is not yet present on `origin/dev`, so no Alpine-only collector or premature canary was created. Once that shared seam lands, this host owns only the thin OpenRC observer and a short exact-r3/Koi-peer canary; the coordinated duration soak remains gated on every observer canary and CachyOS's shared run ID.

## 2026-09-03 15:34 UTC — PH-4 exact-source replacement artifact ready

commit: exact frozen source `e49bfe2b3e403fa87d4b8b237b49f3bb9e5cb5ef`; package recipe and evidence are published by this entry's commit | run: `ph4-e49b-alpine-artifact-01` | peer: none (exact-source build/install gate) | verdict: **PASS — Alpine artifact ready for `ph4-e49b-win-native-02`**
koi state now: APK-owned `koi-1.0.0_rc2_git20260903-r3` plus `koi-openrc-1.0.0_rc2_git20260903-r3`; exactly one supervised `/usr/bin/koi --daemon`, PID `2023` under OpenRC supervisor PID `2022`, installed SHA-256 `095371b9ed13cde254214155aa231db9333342c35255717654299fcd514e9941`. The standing `5651:5654` plan is preserved: operator HTTP is healthy and loopback-only on 5651, Pond desire is false and 5654 is closed. Native mDNS is Ready at generation 1 with publish/explicit-publish/browse routed native and one permanent desired/established publication. Health has zero configured/derived services, proxy has zero listeners, and Avahi remains installed but stopped/disabled. The unchanged APK-owned workbench remains the sole `/usr/bin/koi-desktop --minimized`, PID `28433`, SHA-256 `2521d422dd9c61568a31ecce74e9a8cad89a5ead3432954633e4755a91c464de`.
gates: exact GitHub source archive and Git-tree equivalence; fmt; strict locked workspace/all-target/all-feature clippy; full locked workspace/all-target/all-feature tests and doctests; locked release build; APKBUILD lint; detached abuild optimized build and focused package recipe 28/28; signed APK verification; combined signed repository index; indexed APK/OpenRC upgrade; public installer restart and health check — pass
restoration: config SHA-256 remains `f3efca4fba827cc3a9bec60aeb138f0da41545fe5beea83257ea5e5515e1c374`, local-access policy `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, member identity `5ecf787095e170708b9db9d705db44c4386c1732332f87864fbd7196f357d3eb`, init `821510e1abb5cce853e06e09dc3ffce423e2a9af4026e775b0d5e6283b1bcb7d`, and logrotate `fb260ead3c751912c5ef6e86429f108bb6f917feaabec17ee22d72e1bf78aedb`. `/etc/apk/world` is byte-exact at `cd4a3a6a248c1b0bac45de90d159a439331c97bba2ff106dbc91234d3374132e`, `/etc/group` at `bba50a349020a7f929b02312afdc136234ce832945a92c73f60e6cf9612a2bcf`, repositories at `7293f98cf79a194e28daaee94f51b201527b057ad425abb46c1eebfbfabb02a8`, and network interfaces at `af1cbeb3da4a0448bc120f7255d36d424f4798a70bcf6976eeb8db9a7b554fb2`. `eth0` remains `192.168.1.221/24` with `default via 192.168.1.1 dev eth0 metric 202`. Temporary build packages and dependencies, abuild group membership/key/config, exact-source/cache/target/staging trees, extracted packages, and duplicate staging copies were removed; no repository was added to the host configuration.
findings:

1. The recipe now pins only `e49bfe2b3e403fa87d4b8b237b49f3bb9e5cb5ef`. Its source archive SHA-256 is `00116809f08d469c17a70dba38d7f10f9a4645294d553ab14ab032543bba55fe` and SHA-512 is `6497c06e791546211a28019da3382d559202e53aafcbe2503d2b1d1ff40377d012cdd5f4f69b8011d2a7c5889d3f5db3f3c559717d00ab5b4b2ed4c8354ea1dd`; its extracted tree matched `git archive e49bfe2` exactly. The direct locked release binary hashes to `ebf6dc9893a030f3ed04a75155082ac27c81c43c7173e6ac15baa4b4424a8ae2`; the stripped packaged/installed executable is the `095371b9…` artifact above.
2. Detached abuild produced main APK SHA-256 `59b64883ffb19df1796634009078786ea40d026b57089a05d2c2d1ca82d6d97a`, OpenRC `5684fb0b938a82d03e76125b5fea7c1b0a15d847e1c7e40b7a7794aeee32e4b1`, and docs `b5eb7eab30eb0cce7e873d007e84d81fb312854185ec63b1b6c31acfe545ee0e`. Ownership remains exact: main owns `/usr/bin/koi`; OpenRC owns `/etc/init.d/koi` and `/etc/logrotate.d/koi`; documentation is isolated. The retained signed 27-package x86_64/noarch index hashes to `c090a3998c467f0f5715220fd7d4bc9a057970dd8f0c8055931eba58a9003c82` and advertises all three r3 payloads.
3. The exact-source test target first exhausted Alpine's 11.7 GiB `/tmp` tmpfs while linking the full suite; that attempt changed no product state. Re-running the same locked gates under the home filesystem with incremental compilation disabled passed completely, followed by the detached package build and checks. This was a capacity correction, not a test or product failure.
4. Indexed `apk add --upgrade koi` moved both main and auto-selected OpenRC packages `r2→r3`. `/usr/bin/koi install --operator test` recognized and updated the existing deployment, stopped/restarted it once, preserved the explicit shifted ports and durable bytes, returned `/healthz` healthy, and left the expected singleton supervisor/daemon/workbench shape. APK policy and ownership resolve the installed files to the retained r3 repository artifacts.
5. The source delta is confined to Windows DNS-SD browse/withdrawal code. Per dispatch, no already-green Alpine whole-story, Plasma, lifecycle, Pond, or provider-mutation workbook was repeated. This host is now unchanged and available as the native peer for Windows-owned run `ph4-e49b-win-native-02`; only its run-owned Koi API traffic is permitted, and PH-5 remains prohibited.

## 2026-08-31 12:40 UTC — canary v2 (orchestrator-driven)
commit: da6032b | gates: cargo check -p koi-net →     Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.39s
koi state now: standing daemon on port 5641 (untouched by the canary)
stack: kwin_wayland kwin_wayland_wr plasma_session plasmashell startplasma-way 
ladder (head): {"capabilities":[{"healthy":false,"name":"mdns","summary":"1 registered (1 alive, 0 draining); browse active 27324s on a live LAN but last mDNS was 832s ago — inbound multicast is not reaching this daemon (confirm with `koi mdns discover`)"},{"healthy":true,"name":"certmesh","summary":"ready — r
findings: none (canary run)

## 2026-09-02 01:00 UTC — native musl deployment, workbench, and ADR-042 acceptance

commit: koi product `dbd2466`, portable gate follow-ups through `739b902`; koi-desktop `4c05ed2` | gates: full locked musl workspace all-target/all-feature suite; strict clippy; release build/install; OpenRC lifecycle; desktop Rust/UI/strict-clippy/release gates; real Plasma/AT-SPI; physical Pond
koi state now: exactly one OpenRC-managed `/usr/local/bin/koi --daemon`, PID `14321`, SHA-256 `7ea773c5ce936fe85853ad3d3e81e49f4645674b67ae52f51d50b9e4141ccae7`; it honors the standing shifted configuration at `127.0.0.1:5651` with derived Pond port 5654. Pond is deliberately disabled and 5654 is closed. mDNS is Ready through native Koi (`publish native, browse native, resolve browse fallback`); certmesh reports `authenticated member`. No checkout desktop or second Koi remains.
findings:

1. The former unmanaged dogfood process was retired in favor of the one default-runlevel OpenRC service. The real standing member identity was copied into the empty `/var/lib/koi` slot with root ownership; source and destination `member.json` both hash to `5ecf787095e170708b9db9d705db44c4386c1732332f87864fbd7196f357d3eb`, and the source remains untouched.
2. Physical Pond run `20260902T003409Z-14183` used the installed musl artifact and independent test-01. It passed the complete public allowlist/negative mutation matrix, explicit stop, and serial restart recovery (`14030→14321`) before restoring the original disabled desire. With neither firewalld nor UFW present, Koi reports the host-policy assessment as unknown instead of claiming an open firewall.
3. The full native command `cargo test --workspace --all-targets --all-features --locked` passed after 5m36s, including 170 koi tests, three two-daemon tests, and 66 koi-serve tests; only explicit Docker/real-provider environment gates remained ignored. This is the fleet's complete musl proof, not merely the canary's `cargo check -p koi-net`.
4. koi-desktop now disables Rust's static CRT only for musl targets and links Alpine's shared GTK/WebKitGTK/app-indicator stack. Native Rust tests (9 pass/1 environment-gated), 37 UI tests, strict clippy, and release all passed without an ad-hoc environment override. A real Plasma/Wayland release process exposed the full AT-SPI workbench, discovered the shifted daemon, armed exact URL `http://192.168.1.221:5654/`, passed second-host access, stopped sharing, and exited cleanly.
5. The temporary SDDM autologin file used to establish the physical Plasma seat was deleted immediately after the session began; no validation SDDM configuration remains. Both repositories are clean at shared heads (`739b902` and `4c05ed2`).

## 2026-09-02 03:08 UTC — PH-001 OpenRC desktop service-manager correction

commit: koi product `5c1b96c`; koi-desktop `2c2b74a` | gates: desktop fmt; 12 Rust pass/1 live ignored; strict all-target clippy; 37/37 UI; native musl release; real Plasma/AT-SPI OpenRC assessment and duplicate refusal
koi state now: exactly one default-runlevel OpenRC `/usr/local/bin/koi --daemon`, PID `14321`, SHA-256 `7ea773c5ce936fe85853ad3d3e81e49f4645674b67ae52f51d50b9e4141ccae7`; operator HTTP remains `127.0.0.1:5651`, Pond remains deliberately disabled and 5654 is closed, mDNS is Ready through native Koi, and certmesh reports `authenticated member`. No workbench remains running and `/usr/bin/koi-desktop` is still absent pending the assigned native APK workstream.
findings:

1. koi-desktop `2c2b74a` replaces unconditional Linux `systemctl` calls with one capability-keyed `Systemd | OpenRc | Unavailable` adapter. A live systemd runtime wins; otherwise a complete OpenRC toolset owns status/start/stop, with `pkexec` for graphical elevation and an explicit `doas rc-service koi <action>` remedy when no policy agent exists. Partial manager facts degrade unavailable instead of guessing a distro.
2. Local run `20260902T030349Z-2290` used release candidate `/home/test/repos/github/sylin-org/koi-desktop/target/release/koi-desktop`, SHA-256 `8683dd6ad29680335efe2602b5b9a2e21ee70e39e8e01eda3d8aa9ce310fc646`, only as a checkout investigation, not installed-product acceptance. AT-SPI exposed `Service running` against the real `/etc/init.d/koi` (SHA-256 `5008e479c7d07ee68446dc34000fd0824957189bb2ffc44dc2176d81a5ca2857`); invoking `Run once` left the sole daemon PID at `14321`, proving the OpenRC install is now recognized and a second Koi is refused.
3. The interactive start/stop button path was not claimed: this session had a working Plasma polkit agent but no machine elevation credential, and no package-owned workbench exists yet. The candidate workbench was stopped, no service/provider/firewall/configuration state was mutated, port and PID state matched the baseline, and both repositories were synchronized to their remotes. The next dependency is the already-assigned abuild-owned desktop artifact, followed by its credentialed stop/start gate.

## 2026-09-02 03:55 UTC — PH-001 native APK workbench and credentialed OpenRC controls

commit: koi product unchanged from the prior gate; koi-desktop package recipe `0434df3` pinned to service-manager source `2c2b74a` | gates: detached commit archive through abuild build/check/rootpkg; apkbuild-lint/validate; archive ownership and shared-musl linkage; 12 Rust pass/1 live ignored; strict all-target clippy; 37/37 UI; desktop-file validation; package install/upgrade/removal/reinstall; real Plasma/AT-SPI OpenRC stop/start/duplicate refusal
koi state now: exactly one default-runlevel OpenRC `/usr/local/bin/koi --daemon`, PID `2575`, SHA-256 `7ea773c5ce936fe85853ad3d3e81e49f4645674b67ae52f51d50b9e4141ccae7`; operator HTTP is healthy at `127.0.0.1:5651`, Pond is deliberately disabled and 5654 is closed, mDNS is Ready through native Koi, and certmesh reports `authenticated member`. The installed workbench is APK-owned `koi-desktop-0.1.2_git20260902-r1`; `/usr/bin/koi-desktop` hashes to `6d933c185fc9facd9c3388dde31328602d5702cf81ab2da2551dbd568e7f10e9`. No workbench process or checkout executable remains running.
findings:

1. Alpine package run `20260902T035530Z-2575` used no peer. The commit-pinned source archive built under abuild rather than the checkout; its Rust, UI, and desktop-entry checks passed. The verified main APK hashes to `4426ca933641752caee89bde78d1d8e92dde5c141f77de572cb2074b66bb0800`, links Alpine's shared musl GTK/WebKitGTK stack, and owns exactly `/usr/bin/koi-desktop`, `/usr/share/applications/koi-desktop.desktop`, and `/usr/share/pixmaps/koi.png`; README documentation is isolated in `koi-desktop-doc`.
2. A repository-installed `-r0` with a plain `koi-desktop` world constraint upgraded alone to `-r1` through `apk upgrade --allow-untrusted --repository /home/test/packages/packaging koi-desktop`. A subsequent `apk del` removed all three owned paths while the independent Koi service stayed healthy, and repository reinstall restored the exact `-r1` hash and ownership. This also caught and removed the direct-file checksum pin that would have defeated ordinary repository upgrades; the published README now installs by indexed package name.
3. The installed `/usr/bin/koi-desktop` exposed `Service running` through the real Plasma/Wayland AT-SPI tree. Its Stop action invoked `/usr/bin/pkexec /sbin/rc-service koi stop`, reached OpenRC stopped with no PID and a closed 5651, and rendered `Service installed but stopped`. Its Start action invoked the matching OpenRC start, returned `/healthz = OK` on new PID `2575`, and rendered `Service running`. Invoking Run once while started preserved the sole PID `2575`; no duplicate daemon appeared.
4. Restoration is exact: the temporary `-r0` repository, extracted build-tool/input helpers, staging trees, APK database backup, inspection fixtures, and test abuild signing key were deleted. The retained local repository contains only indexed `-r1` main/doc APKs; no repository was added to `/etc/apk/repositories`, no provider/firewall/configuration state changed, the autostart preference remains disabled, and one healthy Koi remains. The next dependency is the assigned APK-owned daemon/OpenRC supervision transaction with bounded respawn, rollback, logs, upgrade, and removal—not another desktop package gate.

## 2026-09-02 05:13 UTC — PH-001 APK-owned daemon, transactional OpenRC supervision, and lifecycle proof

commit: OpenRC product path `72dc7e1`; Alpine package recipe `10dc388` pinned to that source | run: `20260902T051325Z` | peer: none (host-local install/supervision/package gate) | verdict: pass
koi state now: APK-owned `koi-1.0.0_rc2_git20260902-r1` plus auto-selected `koi-openrc-1.0.0_rc2_git20260902-r1`; exactly one supervised `/usr/bin/koi --daemon`, PID `20751` under supervisor PID `20668`, binary SHA-256 `8cedf10927a75189ac1e98262116b157138a9a994c55e650d292107202af3003`. HTTP is healthy at the standing `127.0.0.1:5651`; mDNS is Ready through native Koi, certmesh is an authenticated member, Pond remains disabled, and no `/usr/local/bin/koi` or checkout daemon exists.
findings:

1. OpenRC now uses `supervise-daemon` around the foreground Koi process with a 5-second delay, three-respawn/60-second burst bound, periodic authenticated local status checks, and a bounded `/var/log/koi/daemon.log` policy (1 MiB, five compressed generations). A physical crash burst reached the configured limit after the third additional kill and left the service failed instead of looping; an explicit OpenRC start returned it healthy. On the final APK artifact, SIGKILL changed daemon PID `20669→20751` in six seconds while supervisor PID `20668` remained, `/healthz` returned `OK`, and forced log rotation produced `daemon.log.1` while the active service stayed healthy.
2. The OpenRC installer now shares the durable file snapshot primitive with systemd and checkpoints binary, init registration, port config, operator policy, rotation policy, and prior active/enabled state. Enable/start/process-identity/health failures return non-zero and restore the old installation. A physical first-start fault returned exit 1, restored the binary/init/config/rotation hashes byte-for-byte, restored a healthy service (`13753→14084`), and left no manifest, backup, or staging debris. Formatting, strict all-target clippy, release build, and 25/25 recipe tests passed; both detached `abuild` revisions independently rebuilt and passed the same 25 tests.
3. The pinned APKBUILD produces a static-pie `koi` package owning `/usr/bin/koi`, a noarch `koi-openrc` package owning `/etc/init.d/koi` and `/etc/logrotate.d/koi`, and a separate documentation package. Final main/openrc APK SHA-256 values are `af61c00cb11ecbbf33b82057aedf2a373a1116c659e6679ce400c27e25fc1fd7` and `da513c6ef7c4504b20caa9e6a38b6db35684bb5b60d88e2da4d77c588dccdebf`. apk-tools 3 required noarch payloads in the repository's sibling `noarch/` directory even though discovery came from the x86_64 index; the retained local repository now has that real layout and only `-r1` Koi artifacts.
4. A plain `koi` world constraint installed both main and OpenRC packages from the index. Ordinary `apk upgrade ... koi` moved both `-r0→-r1`; the package-owned installer then restarted PID `1748→20098` and verified the exact `/usr/bin` path. `apk del koi` ran the OpenRC pre-deinstall, stopped and disabled the service, removed both packages plus all three owned paths, and preserved config, member state, and logs. Indexed reinstall auto-selected `koi-openrc` again. APK audit reports no drift in the three owned files.
5. Restoration is exact around the intended package migration: `/etc/koi/config.toml` remains `f3efca4fba827cc3a9bec60aeb138f0da41545fe5beea83257ea5e5515e1c374`; `/var/lib/koi/certmesh/member.json` remains `5ecf787095e170708b9db9d705db44c4386c1732332f87864fbd7196f357d3eb`; the world-file delta from baseline is only the new `koi` product constraint. Temporary abuild/cargo/rust/atools/strace packages and dependencies, group membership, signing key, source cache, `-r0` artifacts, fault fixture, and root-owned rollback snapshot were removed. Avahi is still installed and stopped, koi-desktop remains APK-owned at hash `6d933c185fc9facd9c3388dde31328602d5702cf81ab2da2551dbd568e7f10e9` with no process, and no repository was added to `/etc/apk/repositories`. The next dependency is item 3's cold-boot and full Plasma session lifecycle gate on this exact artifact.

## 2026-09-03 00:13 UTC — PH-1/PH-2 installed Plasma session lifecycle

commit: Koi installed service source `72dc7e1` (current synchronized product tree `41ad76b`); koi-desktop source `905b0cd`, package recipe `3bf986e` | run: `20260902T232509Z-alpine-session` | peer: none (host-local Plasma/session gate) | verdict: pass
koi state now: APK-owned `koi-1.0.0_rc2_git20260902-r1` and `koi-openrc-1.0.0_rc2_git20260902-r1`; exactly one supervised `/usr/bin/koi --daemon`, PID `2688` under supervisor PID `2687`, binary SHA-256 `8cedf10927a75189ac1e98262116b157138a9a994c55e650d292107202af3003`. The APK-owned `koi-desktop-0.1.2_git20260902-r6` runs once from `/usr/bin/koi-desktop`, PID `28433`, binary SHA-256 `2521d422dd9c61568a31ecce74e9a8cad89a5ead3432954633e4755a91c464de`; its retained APK SHA-256 is `25cadf73987f89054e3ef179254620247e22788113ddab93510092b41a7233c7`. Operator HTTP is healthy at `127.0.0.1:5651`, Pond is deliberately disabled and 5654 is closed, mDNS is Ready through native Koi, and certmesh reports `authenticated member`.
findings:

1. Boot ID `f2f7c246-7c18-496d-a758-d0ecff2efa44` began at `2026-09-02 20:35:55 UTC` and established the real SDDM → Plasma/Wayland → XDG-autostart path without terminal intervention. Two simultaneous session launch attempts converged through the loopback singleton claim to one resident workbench and one SNI item. Closing the native `Koi` frame left that process/item resident; an ordinary `/usr/bin/koi-desktop` launch revealed the existing GTK frame and never created a second process. AT-SPI reported application `koi-desktop`, child `Koi`, role `frame`.
2. The session exposed a real fragmented-request defect: installed r5's own `--poke` could receive its request as `GET ` and return 404 even though a single-write HTTP client succeeded. koi-desktop `905b0cd` now reads one bounded complete request line and writes the client request atomically; fragmented-reader and exact-wire tests cover it. Native format, 20 Rust tests (one environment gate ignored), strict all-target clippy, 38 UI tests, release build, detached abuild checks, APK verification, shared-musl linkage, and desktop-entry validation passed. Installed r6 now returns `poked a running koi ui: acknowledged`; an ordinary second launch reports `Koi is already running; revealed it` while the sole PID and SNI remain unchanged.
3. The installed r6 notification command produced both foreground and close-to-tray background alerts through Plasma's real `org.freedesktop.Notifications.Notify` D-Bus path. The captured calls named application/icon `koi-desktop` and carried the two run-owned lifecycle-gate messages. The watched-fade versioned-event correction in the same artifact is covered by the final Rust/UI gates; runtime is honestly disabled on this host, so no fabricated container event was used as physical evidence.
4. Lock/unlock preserved daemon PID `2688`, workbench PID `22228` at that point, exactly one SNI registration, and responsive UI health. A real RTC-woken suspend ran from `00:07:04` to `00:07:24 UTC`; elogind recorded `PrepareForSleep(true)` and `PrepareForSleep(false)`. After automatic resume lock and explicit unlock, the same daemon and desktop PIDs, singleton SNI, native provider generation 1, one desired/established publication, authenticated certmesh posture, and loopback health remained.
5. Ordinary repository upgrade moved only `koi-desktop` from r5 to r6; the package database owns the binary, desktop entry, and icon with root ownership and canonical modes. The temporary SDDM autologin file/helper/runlevel marker, RTC alarm, WebKit inspector socket, notification monitor, test mDNS registration, `wtype`, abuild tools/cache/staging, signing key, and extracted candidate were removed. `/etc/apk/repositories` was not changed; the retained local repository is indexed and keeps the verified APKs. Avahi is restored to installed-but-stopped, the primary interface remains `eth0` at `192.168.1.221/24` with its original default route, Pond remains disabled, and exactly one healthy installed daemon/workbench remains. The next dependency is the serial Avahi/native/interface workbook with the unchanged Debian peer, followed by Alpine's PH-3 negative boundary gate.

## 2026-09-03 01:56 UTC — PH-2 OpenRC provider and primary-interface recovery

commit: this commit atop synchronized source `5aee72f` | run: `20260903T014605Z-1730` | peer: Debian test-04 `192.168.1.112` | verdict: pass
koi state now: the APK-owned OpenRC service remains exactly one supervised `/usr/bin/koi --daemon`, PID `2688` under supervisor PID `2687`, binary SHA-256 `8cedf10927a75189ac1e98262116b157138a9a994c55e650d292107202af3003`. Operator HTTP is healthy at `127.0.0.1:5651`; the mDNS control plane is Ready at generation 5 with native publish/explicit-publish/browse and its sole permanent publication established. Avahi is restored to installed, stopped, and disabled. The installed workbench remains the sole `/usr/bin/koi-desktop`, PID `28433`, SHA-256 `2521d422dd9c61568a31ecce74e9a8cad89a5ead3432954633e4755a91c464de`, with one Plasma SNI item.
findings:

1. The focused provider gate is now keyed by service-manager/provider capability instead of assuming systemd. Its OpenRC profile captures the same installed-service and peer invariants, starts real Avahi, stops it for native continuity, restores it for promotion, and returns its captured activity and enablement exactly. The existing systemd-resolved profile retains its five-phase late-conflict sequence. BusyBox `setsid` and a peer without temporary curl are supported without weakening authentication or exporting the peer DAT.
2. Physical phases at generations `2→3→4` selected `avahi → native → avahi`. Each phase held `desired=3, established=3, pending=0, failed=0`; ordinary and explicit Alpine publications resolved on Debian, Debian's run-owned publication resolved on Alpine, the long-lived subscription and withdrawal checks passed, and cleanup returned generation 5 native routing. No second daemon, provider helper, alternate endpoint, or peer OS mutation was used.
3. A separately pre-armed privileged recovery process forced `eth0` down and brought it back after 15 seconds. The down-state evidence recorded no address or default route while the installed daemon remained PID `2688`, healthy over loopback, and native-ready. DHCP restored the exact `192.168.1.221/24` lease and `default via 192.168.1.1 dev eth0 metric 202`; `/etc/network/interfaces` remained SHA-256 `af1cbeb3da4a0448bc120f7255d36d424f4798a70bcf6976eeb8db9a7b554fb2`, and bidirectional publication/read/removal passed again.
4. The unchanged Debian peer remained active+enabled PID `24507`, executable SHA-256 `a74edba5e75402fef9f5364b87ef2a60dac2ddd01c432371314a0df313ce491f`, with Avahi service/socket and systemd-resolved active+enabled before and after. Both hosts finished with zero run-owned registrations; Alpine networking remains started in the boot runlevel, and no recovery process remains. Temporary local/SSH askpass helpers and the pinned run-owned known-hosts file were deleted after final peer verification. The next dependency is dispatch item 3's unrelated-UID, hostile-input, resource-bound, and excluded Pond/operator PH-3 gate.

## 2026-09-03 02:04 UTC — PH-3 installed-service negative boundary and hostile input

commit: this journal/surface publication atop `88a5b05`; installed product bytes unchanged | run: `20260903T015848Z-ph3` | peer: Debian test-04 `192.168.1.112` | verdict: pass
koi state now: exactly one APK-owned OpenRC `/usr/bin/koi --daemon`, PID `2688` under supervisor PID `2687`, executable SHA-256 `8cedf10927a75189ac1e98262116b157138a9a994c55e650d292107202af3003`; one APK-owned workbench PID `28433` and one SNI item also remain. Operator HTTP is loopback-only at `127.0.0.1:5651`, Pond desire is false and 5654 is closed, native mDNS is Ready at generation 5 with its permanent publication `desired=1, established=1, pending=0, failed=0`, and Avahi is installed but stopped/disabled.
findings:

1. `/run/koi.sock` is an owner-private `test:root` mode-0600 socket. The intended operator completed the version-1 access exchange and received the exact `http://127.0.0.1:5651` endpoint plus a present DAT without recording it. UID `nobody` received `EACCES` on a direct socket connection and `koi config path` could reveal only its offline `/nonexistent/.config/koi/config.toml`, not the installed-service fact.
2. Debian sent two separately bounded hostile passes. Each delivered 500 malformed/truncated UDP DNS datagrams, 100 length-mismatched TCP DNS sessions, 500 malformed multicast-DNS datagrams, and 200 malformed/raw Pond HTTP sessions. Each also required 100 excluded Pond requests across DNS mutation, certmesh log, Pond control, OpenAPI, and MCP to return 404; 20 attempts to the operator port were refused; and the four allowed read-only health/status/mDNS/DNS routes still returned 200.
3. Both 40-sample half-second windows held descriptors at 21 and threads at 14 while Pond's one extra listener was armed. RSS was 20,732–20,892 KiB in pass one and 20,892–20,896 KiB in pass two. Generation stayed 5 and publications stayed `1/1/0/0` in every sample. After Pond restoration, thirty one-second samples held descriptors at the baseline 20, threads at 14, RSS at 20,892–20,896 KiB, and the same mDNS facts. The scoped daemon log contained zero retry/recovery events and zero panic, fatal, OOM, descriptor-exhaustion, provider-failure, or route-failure events; expected malformed DNS inputs produced bounded `FormErr` information only.
4. Restoration is exact: Pond's final disabled status equals baseline, 5654 is closed, 5651 remains loopback-only, config SHA-256 remains `f3efca4fba827cc3a9bec60aeb138f0da41545fe5beea83257ea5e5515e1c374`, and interface bytes/address/route plus daemon/supervisor/workbench PIDs are unchanged. Debian retained installed Koi PID `24507`, SHA-256 `a74edba5e75402fef9f5364b87ef2a60dac2ddd01c432371314a0df313ce491f`, active+enabled with `NRestarts=0`. No sampler, traffic process, credential helper, control helper, or run-owned host-key file remains. Alpine has closed its local PH-3 gate; the next dependency is dispatch item 4's corrected ownership-aware installer upgrade and exact rollback/Pond/musl validation.

## 2026-09-03 03:00 UTC — PH-1/PH-2/PH-3 convergence candidate upgrade

commit: package source `32172ba`; this recipe/harness/journal publication atop that source | run: `20260903T020713Z-installer` | Pond sub-run: `20260903T024856Z-16330` | peer: Debian test-04 `192.168.1.112` | verdict: pass
koi state now: APK-owned `koi-1.0.0_rc2_git20260903-r0` plus `koi-openrc-1.0.0_rc2_git20260903-r0`; exactly one supervised `/usr/bin/koi --daemon`, PID `16502` under supervisor PID `16501`, executable SHA-256 `42a65b06319f1b760c47fd0c3dbed40368c407c60b7ac8988a67a4b2b170b933`. Operator HTTP is healthy and loopback-only at `127.0.0.1:5651`; the standing explicit `5651:5652:5653` configuration still derives Pond 5654, Pond desire is false and 5654 is closed. Native mDNS is Ready with one desired/established publication, Avahi is installed but stopped/disabled, and the installed workbench remains PID `28433`, SHA-256 `2521d422dd9c61568a31ecce74e9a8cad89a5ead3432954633e4755a91c464de`, with one SNI item.
findings:

1. The dated APKBUILD pins synchronized source `32172ba25cdd1ebab86c302bd12a22249849125f`. Detached abuild produced static-PIE main APK SHA-256 `ab6143018de46d055637b446601c715321cbbe9f553c651609eadcb242756e02`, noarch OpenRC APK `98e801ac091c7615f1d534c56ffd54093d7b9309c9ee1495a18b5647196ff7e6`, and doc APK `26054e77d89ac4dca9a7e5112386c525e498190ec4d2fb95f63af40745497b24`. Package ownership remains exact: main owns `/usr/bin/koi`; OpenRC owns `/etc/init.d/koi` and `/etc/logrotate.d/koi`; docs are isolated. The retained x86_64 index hashes to `97d35bee8ea6d98e2c4f93b94bd0bce3d4746c26b0c27509610bceb78a70a690`.
2. Ordinary indexed APK upgrade moved main and OpenRC from `1.0.0_rc2_git20260902-r1` to the dated r0. Running the package-owned `/usr/bin/koi install --operator test` recognized the existing deployment, preserved the explicit port plan instead of treating Koi as a foreign incumbent, and restarted the accepted binary (`2688→25981`). Config, operator policy, member identity, init script, and logrotate hashes remained byte-identical; the config still contains exactly the three explicit port assignments.
3. A deliberately unhealthy detached candidate, installed through the real product path with `/usr/bin/koi` as its rollback target, failed because OpenRC started zero daemon processes. The installer returned non-zero, reported restoration, and recovered the accepted package binary plus exact config/policy/member/init/logrotate bytes, active+enabled service, loopback health, and closed Pond. No transaction manifest, backup, staging path, or fault worktree remained.
4. Native musl formatting, strict workspace/all-target/all-feature locked clippy, and the full workspace/all-target/all-feature locked test suite passed. The package recipe's 28 focused tests passed. The physical Pond sub-run used the installed `42a65b…` artifact and unchanged Debian PID `24507`, SHA-256 `a74edba5e75402fef9f5364b87ef2a60dac2ddd01c432371314a0df313ce491f`; every public and excluded route, explicit stop, OpenRC restart recovery (`3565→16502`), and restoration of the prior disabled desire passed. The portable gate now uses peer Python 3 rather than assuming curl and supports BusyBox `setsid` without weakening SSH host-key/password handling.
5. Restoration and cleanup are exact around the intentional package upgrade. The package inventory differs from baseline only by the two Koi version replacements, `/etc/apk/world` is byte-identical, APK audit reports no owned-file drift, and no repository was added to `/etc/apk/repositories`. Temporary abuild/cargo/rust/atools packages and dependencies, group membership, signing key, source cache, build/staging trees, fault candidate, peer/root credential helpers, run-owned known-hosts file, and one stale prior notification-gate announcement process were removed. The dated APKs and index alone are retained for PH-4. Alpine has completed every item in its convergence dispatch and now supplies this installed musl candidate to the frozen-revision decision.

## 2026-09-03 05:28 UTC — PH-4 frozen candidate rejected at the installed proxy/health boundary

commit: rejected exact source `f53d568a871e952576988e543126160bfd41aaaa`; corrective source is the commit containing this entry | run: `20260903T043922Z-ph4-alpine` | peer: Debian `stone-halcyon-savanna` at `192.168.1.112`, unchanged installed PID `93306`, SHA-256 `7a1356513791470bdbdbdba66d7ef27106a315914ecd5106980dd30317583091` | verdict: **REJECTED — PH-4 freeze invalidated**
koi state now: the rejected but healthy package remains installed as `koi-1.0.0_rc2_git20260903-r1` plus `koi-openrc-1.0.0_rc2_git20260903-r1`; exactly one supervised `/usr/bin/koi --daemon`, PID `27628` under supervisor PID `27627`, executable SHA-256 `94dd2271f738d753ec24f294622beee4c8afabe008ace9a431a0b9855bfa22a4`. Operator HTTP is loopback-only at `127.0.0.1:5651`, Pond desire is false and 5654 is closed, native mDNS is Ready with one permanent publication, DNS has zero static entries, health has zero configured/derived services, proxy has zero listeners, and Avahi is installed but stopped. The unchanged APK-owned workbench remains the sole `/usr/bin/koi-desktop`, PID `28433`, SHA-256 `2521d422dd9c61568a31ecce74e9a8cad89a5ead3432954633e4755a91c464de`, with one Plasma SNI item.
gates: clean frozen-source archive checksum and detached abuild; APKBUILD lint; focused package recipe 28/28; musl fmt; strict locked workspace/all-target/all-feature clippy; full locked workspace/all-target/all-feature tests before the physical run; corrective focused `koi-health` 6/6, fmt, strict full clippy, and full locked workspace/all-target/all-feature tests — pass
restoration: the run-owned DNS entry and health check are removed; the proxy listener and workload ports 18443/18080 are closed; the mDNS registration was explicitly unregistered and Debian's final resolve timed out; Pond remains at its initial disabled desire with 5654 closed. A restorative OpenRC restart changed only the rejected daemon PID `11448→27628` and cleared its stale in-memory derived health count. Config SHA-256 remains `f3efca4fba827cc3a9bec60aeb138f0da41545fe5beea83257ea5e5515e1c374`, local-access policy `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, member identity `5ecf787095e170708b9db9d705db44c4386c1732332f87864fbd7196f357d3eb`, init `821510e1abb5cce853e06e09dc3ffce423e2a9af4026e775b0d5e6283b1bcb7d`, and logrotate `fb260ead3c751912c5ef6e86429f108bb6f917feaabec17ee22d72e1bf78aedb`. Interface `eth0` remains `192.168.1.221/24` with default route metric 202; no peer or provider state changed.
findings:

1. The signed repository was built from the exact frozen archive. Main APK SHA-256 is `4bffe15091a1402ba0a20eba2a64abcd6b87c1f40efafd9a60a13c811469c7c0`, OpenRC `148348e22400fc93d1899ce62a49a732a3fac1f0d909d5f30406a106581337af`, docs `6500419742c70ea0481c646dced2af981ca14b83c560db8d2dd75d0576da8cd5`, and retained signed x86_64 index `f541d69c4df5e641e121c623b49c8b2c55f1b5f576405d12bc5f169193b3fbcc`. Package ownership remains exact. Indexed apk/OpenRC upgrade and the public installer preserved the declared `5651:5654` plan, config, policy, member identity, and singleton process shape.
2. Before the defect, installed native Koi successfully published `ph4-alpine-043922._koi-ph4._tcp.local.` with explicit `192.168.1.221:18080` and run/TXT facts; both Alpine and Debian resolved it. Debian also resolved `ph4-alpine-043922.internal.` through Alpine DNS. The live TLS listener served the current certmesh member leaf (`CN=test-03`, SANs `test-03` and `test-03.internal`, fingerprint `E9:B0:83:99:0D:FE:56:18:49:66:5B:A9:26:4F:34:32:7A:AC:32:F8:0E:33:F0:03:B5:13:73:41:77:12:A5:30`) and physically returned the backend document to Debian.
3. The documented `koi proxy add ... --backend 127.0.0.1:18080` form exposed a shared contradiction: the proxy ran and passed real traffic, but health derived an HTTP check with the raw scheme-less target and reported `proxy:ph4-alpine-043922` Down with `http_error: builder error`. Removing the proxy then left that derived state counted in the capability summary until restart. The health projection now prepends `http://` to bare proxy endpoints while preserving explicit HTTP(S) URLs, and each tick prunes state absent from the current explicit-plus-derived check set. New tests pin both target forms and removal reconciliation. The neighboring compose status test also now owns and clears the global capability-note fixture, closing the full-suite parallel race discovered during the frozen build.
4. Because finding 3 changes product bytes, no Pond or provider-rotation acceptance was claimed on `f53d568`; the freeze contract requires a new synchronized source SHA, rebuilt APKs, and repetition of this Alpine slice. The rejected r1 artifacts remain identifiable for rollback/evidence only and must not enter PH-5.

## 2026-09-03 05:56 UTC — PH-4 re-frozen musl artifact and installed whole-story accepted

commit: exact re-frozen source `5c89e9de11bf23ab81fd8b5b0778c58477359360`; package recipe and evidence are published by this entry's commit | run: `20260903T043922Z-ph4-alpine`; Pond sub-run `20260903T054652Z-23867` | peer: Debian `stone-halcyon-savanna` at `192.168.1.112`, unchanged installed PID `93306`, SHA-256 `7a1356513791470bdbdbdba66d7ef27106a315914ecd5106980dd30317583091` | verdict: **PASS — Alpine artifact ready**
koi state now: APK-owned `koi-1.0.0_rc2_git20260903-r2` plus `koi-openrc-1.0.0_rc2_git20260903-r2`; exactly one supervised `/usr/bin/koi --daemon`, PID `24201` under the OpenRC supervisor, installed SHA-256 `4db6a257b9303157bd8dff03887b478b2c8f5a20f777166d35a59f77436a95e9`. The declared `5651:5654` run is retained, operator HTTP is loopback-only at 5651, Pond desire is false and 5654 is closed. Native mDNS is Ready with one permanent desired/established publication; DNS has zero run-owned static entries; health has zero services; proxy has zero listeners; certmesh diagnosis is HEALTHY/Authenticated for `test-03`; Avahi is installed but stopped. The unchanged APK-owned workbench remains exactly one PID `28433`, installed SHA-256 `2521d422dd9c61568a31ecce74e9a8cad89a5ead3432954633e4755a91c464de`, with one live Plasma SNI item.
gates: clean exact-source GitHub archive; APKBUILD lint; detached abuild optimized build plus focused package recipe 28/28; package ownership/shared-musl ELF inspection; exact-source-tree equivalence check; fmt; strict locked workspace/all-target/all-feature clippy; full locked workspace/all-target/all-feature tests and doctests; indexed apk/OpenRC install; installed Find → Name → Trust → Serve/Pond with physical peer — pass
restoration: run-owned `ph4-alpine-r2-043922` DNS, health, proxy, mDNS, workload, and listeners are absent. Debian's mDNS resolve timed out after acknowledged withdrawal, its DNS answer is empty, and TCP 18444/5654 are refused. Pond's restoration-gated verdict records false→false and closed listener. Config, local-access policy, member identity, init, and logrotate retain their pre-run hashes; `eth0` remains `192.168.1.221/24` with the original default route metric 202; Avahi remains stopped; no peer/provider/firewall/interface mutation occurred. Final installed inventory differs from baseline only by the intended main/OpenRC r0→r2 version replacements; `/etc/apk/world`, repositories, and network interfaces are byte-exact. Temporary build packages, abuild membership/key/config, source archives/cache, staging trees, root/peer credential helpers, known-hosts file, and cleanup/workload processes were removed.
findings:

1. The final APKBUILD pins only `5c89e9de11bf23ab81fd8b5b0778c58477359360`. Detached abuild produced main APK SHA-256 `1a29d03bc5a3cfef04dbf0367d62103ec47995c19651b879446426b137fd5c16`, OpenRC `3ee03ca7a524bfda717a57cba211cb0af7935abff9f28bfced062c2c9a9e455c`, and docs `03550d3198a443888ff309ed9af1c62f4c2e7c28ae26d926562e439ea60dc71a`. The main archive owns only `/usr/bin/koi`; OpenRC owns the init and logrotate files; docs are isolated. The final 24-package signed index hashes to `e4979779cb5376745dae852c1a451751745da00a79e4b8a2bb7a72fc65de3fda` and includes the sibling `noarch` payloads required by apk-tools 3.
2. The first locally regenerated r2 index mistakenly enumerated only x86_64 payload files, so apk correctly upgraded the main package but removed the now-unresolvable r1 OpenRC dependency and stopped Koi. That repository index was rejected. Rebuilding the signed index over both x86_64 and sibling noarch packages exposed r2 OpenRC to policy; ordinary package installation plus public `/usr/bin/koi install --operator test` restored active+enabled OpenRC on the exact r2 bytes and preserved `5651:5654`. No candidate binary rollback or manual service imitation was used.
3. Authenticated local control reported `/etc/koi/config.toml` through `test:root` mode-0600 `/run/koi.sock`. Native Koi published `ph4-alpine-r2-043922._koi-ph4._tcp.local.` with explicit `192.168.1.221:18081` plus phase/OS/run TXT; both hosts resolved it and Debian timed out after explicit unregister. Debian resolved the run-owned `.internal` DNS record through Alpine, fetched 1,267 backend bytes through TLS port 18444, and observed the certmesh member leaf. Explicit health and the derived `proxy:` health were both Up before and after restart; the formerly broken bare backend is now projected as `http://127.0.0.1:18081`, and removal reconciled the capability summary to `0 services up (0 total)` without another restart.
4. Pond run `20260903T054652Z-23867` used the sole installed daemon and real workbench bundle. Debian loaded the five UI assets and four allowlisted reads, received absence for every excluded/mutation route, observed explicit stop, and repeated the matrix after OpenRC restart changed the daemon PID `23199→24201` without changing installed bytes. DNS, health, proxy listener, and physical TLS traffic all reconstructed across that same restart; final cleanup returned Pond to its initial disabled desire and removed every run-owned object.
5. `/usr/bin/koi-desktop --poke` was acknowledged without duplication. Plasma still exposes exactly `:1.180/org/ayatana/NotificationItem/tray_icon_tray_app_koi`, proving the installed r6 workbench—not a checkout process—remained the live native surface throughout the daemon/package transitions.
6. Alpine's local PH-4 slice is complete on the re-frozen artifact. The serial Windows↔native and Avahi/resolve1↔native rotations remain dependency-gated until the CachyOS driver publishes its artifact-ready state and shared run IDs; this host stays available without changing Avahi/network state in advance.

## 2026-09-04 14:29 UTC — OD-2 observable-boundary OpenRC acceptance

commit: exact corrected product source `e64b50e7146d2aff4fe0546763833098432faae8`; this package recipe, journal, and row closure are published by the containing commit | provider run: `20260904T140752Z-28808` | collector run: `v1-20260904T1413Z-alpine-od2` | peers: test-01 `192.168.1.109`, Bluefin `192.168.1.95` | verdict: **PASS — Alpine OD-2 complete**
koi state now: APK-owned `koi-1.0.0_pre0_git20260904-r2` plus `koi-openrc-1.0.0_pre0_git20260904-r2`; exactly one supervised `/usr/bin/koi --daemon`, child PID `31872` under supervisor PID `31871`, executable SHA-256 `a713968fe5b84d5894588f8eb6be2b37b8a99f8bfb42d4d20c586e1226d8a5e6`. The OpenRC observer reports `started/supervised`, exact `/usr/bin/koi --daemon`, and restart count zero. Operator HTTP is loopback-only at `127.0.0.1:5651`; Pond desire is false and 5654 is closed. Native mDNS is Ready with one desired/established publication, Avahi is installed but stopped/disabled, and `/v1/host` reports the immutable launch identity `test-03`, `test-03.local`, Linux x86_64, and LAN `eth0=192.168.1.221`.
gates: exact GitHub source archive and detached optimized abuild; focused package recipe 44/44; package ownership and payload identity; indexed APK/OpenRC replacement plus public installer; formatting; strict locked workspace/all-target/all-feature clippy; full locked workspace/all-target/all-feature tests; physical Avahi/native provider workbook; local IPC allow/deny and HostIdentity; OpenRC restart reconstruction; 30-second neutral installed-service collector — pass
restoration: the intentional result is only the main/OpenRC package upgrade. Config, data-root config, local-access policy, Pond intent, member identity, init script, and logrotate are byte-exact at SHA-256 `f3efca4fba827cc3a9bec60aeb138f0da41545fe5beea83257ea5e5515e1c374`, `a2c6d181be890c7807604d88a2e2318361bfc443b265f86dc61428f01d41a72a`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`, `5ecf787095e170708b9db9d705db44c4386c1732332f87864fbd7196f357d3eb`, `821510e1abb5cce853e06e09dc3ffce423e2a9af4026e775b0d5e6283b1bcb7d`, and `fb260ead3c751912c5ef6e86429f108bb6f917feaabec17ee22d72e1bf78aedb`. `/etc/apk/world`, `/etc/group`, repositories, and network interfaces are restored to SHA-256 `cd4a3a6a248c1b0bac45de90d159a439331c97bba2ff106dbc91234d3374132e`, `bba50a349020a7f929b02312afdc136234ce832945a92c73f60e6cf9612a2bcf`, `7293f98cf79a194e28daaee94f51b201527b057ad425abb46c1eebfbfabb02a8`, and `af1cbeb3da4a0448bc120f7255d36d424f4798a70bcf6976eeb8db9a7b554fb2`; `eth0` remains `192.168.1.221/24` with default route metric 202. Temporary build packages, abuild group membership/config/key, signing trust, source archives/cache, restore trees, and local/peer credential and host-key helpers were removed.
findings:

1. The neutral collector now has a real OpenRC adapter rather than interpreting systemd text. It reads OpenRC's started marker and supervised child options, proves the init definition, exact executable/argv, supervisor parent, restart counter, `/proc` identity/resources, and explicitly reports task count unavailable because OpenRC has no service-cgroup counter. Shared `/proc` and artifact parsing is used by both Linux observers. The final focused OpenRC/installed-service suites passed 11/11 and 12/12 before the full workspace gate.
2. Physical package replacement exposed a real installer boundary: apk atomically replaces `/usr/bin/koi`, so the still-running prior image appears in procfs as the same absolute path with the kernel's ` (deleted)` suffix. Pre-transaction ownership now accepts only that exact deleted expected image with `--daemon`; the final candidate check remains strict, and lookalike paths are rejected. A bounded OpenRC readiness loop observes the actual supervised child. Regression tests cover exact replacement, final strictness, lookalikes, and delayed child publication.
3. The final APKBUILD pins only `e64b50e7146d2aff4fe0546763833098432faae8` (source archive SHA-256 `04fa71e5e138476f7307e4800adb4c7bc1068434993cb520b5a7b5f999d2262c`). Detached abuild produced main APK SHA-256 `7165d17f58ddd9f42efb9bfd0dd97f96030afcbc07543cef982f39b6a461ae19`, OpenRC `8cc4819c36220edc45b1c1ffa0064042138a18faaa060fe61752f76052e64919`, docs `ffb0a77244913258a721ee54d1be276f8a740cc61b73be262a24a27c7599cc1d`, and signed combined index `f5cda461797a0f1cfeb32a9e6721212ad72812de38dcba9687c255c345a63b08`. The installed and extracted package payloads both hash to the exact binary above. apk-tools 3 required the x86_64 index to enumerate sibling noarch packages; an incomplete intermediate index and its package transaction were rejected before acceptance.
4. The decisive r1→r2 indexed replacement occurred while the prior supervised image was live. The public `/usr/bin/koi install --operator test` accepted only the exact package-replaced prior image, serialized the install, stopped it, staged the exact package binary, preserved config/identity and canonical init/logrotate bytes, enabled the default runlevel, started the supervised r2 child, and returned only after loopback health was ready. A later deliberate OpenRC restart changed PID `27512→31872` and socket inode `2210→2249`; durable state, HostIdentity, native route, and publication reconstructed on the same bytes.
5. `/run/koi.sock` is again `test:root` mode 0600. The intended operator completed authenticated local control; UID `nobody` was denied with `Permission denied` and exit 1. After restart, the public host projection remained exactly `test-03` / `test-03.local` / `192.168.1.221`, while `/v1/status` remained a live Linux daemon at the loopback-only configured bind.
6. Provider run `20260904T140752Z-28808` used unchanged physical test-01 PID `1223362`, installed SHA-256 `26ca480d0cbc0cdb5196500986ca41312a8a4ce089a5c4fc54883dbc4bf78433`. Alpine retained PID `27512` and exact bytes while real routes selected `avahi→native→avahi` at generations `14→18→22`; every phase held `desired=3, established=3, pending=0, failed=0`, both hosts resolved the other's run-owned records, the live subscription observed removal, and cleanup returned Alpine to native generation 24 with its sole permanent publication. Avahi returned exactly to stopped/disabled; test-01 retained its PID/hash and active/enabled service and provider state.
7. Collector `v1-20260904T1413Z-alpine-od2` used Bluefin's one installed Pond surface over the LAN. Seven samples advanced Alpine aggregate revision `1248→1278` and Bluefin Pond revision `3328→3358`, with 7/7 successful reads, zero retry, zero unavailable sample, zero PID/restart/recovery transition, converged native generation 28, and publications `1/1/0/0`. RSS stayed 25,460,736–25,559,040 bytes, descriptors 23, and threads 16; unsupported OpenRC task count remained explicitly unavailable. Bluefin was restored to its original disabled intent hash, closed 5644, and unchanged active/enabled PID `207791` plus installed SHA-256 `298fb7208d7d4eb2f8da4f920e6a608efc4b2e61d88313fff712c7b2588f6750`.
8. No firewall rule or package was changed. The final package database owns the exact r2 main/OpenRC deployment, Koi remains default-runlevel and healthy, Avahi remains stopped/disabled, Pond remains closed, and every run-owned publication plus all credential/build/restore material is absent. OD-3 was not started.

## 2026-09-04 16:21 UTC — OD-3 frozen-candidate readiness

commit: frozen candidate `b3eb47e08817045f9371703d780ada9aab00995d`, synchronized dispatch `c606d9462371412a165d45b0446591c4d689e53d`, campaign `v1-20260904-od3-b3eb47e`; installed APK-owned `/usr/bin/koi` SHA-256 `a713968fe5b84d5894588f8eb6be2b37b8a99f8bfb42d4d20c586e1226d8a5e6`, version `1.0.0-dev.0`, child PID `31872` under `supervise-daemon` PID `31871` — **READY**
koi state now: exactly one default-runlevel `koi-1.0.0_pre0_git20260904-r2` / `koi-openrc-1.0.0_pre0_git20260904-r2` service is started and supervised as root with exact `/usr/bin/koi --daemon`, native OpenRC restart count zero, and the installed executable matching `/proc/31872/exe`. Operator HTTP is the sole matching listener on `127.0.0.1:5651`; Pond remains disabled and 5654 is closed. `/run/koi.sock` remains mode 0600 `test:root`, inode `2249`. Aggregate revision `7766` is healthy except for the truthfully unavailable optional runtime and deliberately disabled Pond; native mDNS is Ready at generation `6`, owns publish/explicit-publish/browse, and holds publications `desired=1, established=1, pending=0, failed=0`. Avahi remains installed but stopped/disabled. `/v1/host` remains `test-03` / `test-03.local`, Linux x86_64, `eth0=192.168.1.221`; DNS remains on port 53 and the default route remains via `192.168.1.1` metric 202.
equivalence: the frozen `packaging/alpine/APKBUILD` is byte-identical to the current recipe and pins only product commit `e64b50e7146d2aff4fe0546763833098432faae8`, `pkgver=1.0.0_pre0_git20260904`, `pkgrel=2`. The only non-documentation/non-recipe source delta from that product commit to the freeze is a three-line completion wait inside `blocking_worker.rs`'s `cfg(test)` module; it cannot affect release bytes. Retained signed artifacts remain main APK SHA-256 `7165d17f58ddd9f42efb9bfd0dd97f96030afcbc07543cef982f39b6a461ae19`, OpenRC `8cc4819c36220edc45b1c1ffa0064042138a18faaa060fe61752f76052e64919`, docs `ffb0a77244913258a721ee54d1be276f8a740cc61b73be262a24a27c7599cc1d`, and combined x86_64 index `f5cda461797a0f1cfeb32a9e6721212ad72812de38dcba9687c255c345a63b08`; extracting the retained main APK reproduced the installed executable hash, while APK ownership resolves `/usr/bin/koi` to main and the init/logrotate files to OpenRC.
restoration/readiness boundary: this reconciliation was read-only. No build, package transaction, service restart, Pond exposure, collector, provider, firewall, peer, or network mutation was repeated because equivalence passed. Config, data-root config, local-access policy, Pond intent, member identity, init, and logrotate remain byte-exact at SHA-256 `f3efca4fba827cc3a9bec60aeb138f0da41545fe5beea83257ea5e5515e1c374`, `a2c6d181be890c7807604d88a2e2318361bfc443b265f86dc61428f01d41a72a`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`, `5ecf787095e170708b9db9d705db44c4386c1732332f87864fbd7196f357d3eb`, `821510e1abb5cce853e06e09dc3ffce423e2a9af4026e775b0d5e6283b1bcb7d`, and `fb260ead3c751912c5ef6e86429f108bb6f917feaabec17ee22d72e1bf78aedb`. APK world, group, repositories, and `/etc/network/interfaces` remain `cd4a3a6a248c1b0bac45de90d159a439331c97bba2ff106dbc91234d3374132e`, `bba50a349020a7f929b02312afdc136234ce832945a92c73f60e6cf9612a2bcf`, `7293f98cf79a194e28daaee94f51b201527b057ad425abb46c1eebfbfabb02a8`, and `af1cbeb3da4a0448bc120f7255d36d424f4798a70bcf6976eeb8db9a7b554fb2`. The temporary credential helper was removed, `/tmp/koi-*` is empty, and no secret entered Git or retained evidence. Alpine will not start a private soak; the next action is the CachyOS coordinator's all-hat readiness barrier and announced UTC T0.

## 2026-09-04 23:35 UTC — OD-3 coordinated OpenRC soak complete

task: inherited Epic 002 handover for Epic 003 R01 | frozen source `b3eb47e08817045f9371703d780ada9aab00995d` | run `v1-20260904-od3-b3eb47e-alp` | peer: Bluefin installed Koi Pond at `192.168.1.95:5644` | verdict: **PASS — 14/14 checks, Alpine handover ready for coordinator reconciliation**
collector: exact frozen detached-worktree `koi-lab` SHA-256 `140f28094aee0243de4074959bb22b7852128dc27f51cecbbde2ace9caa40350` launched as root at `2026-09-04T17:30:22Z`, 22 seconds after the fleet T0, with observer `openrc`, service `koi`, binary `/usr/bin/koi`, 21,600-second duration, 60-second samples, one allowed service restart, and 2/2 unavailable-sample bounds. It completed by duration at `2026-09-04T23:30:22Z` after 21,600.158 seconds and 361 samples. All 14 checks passed: 361/361 valid physical Pond reads with zero retries; zero unavailable samples and zero recovery events; one expected PID transition; every available aggregate was healthy with concrete native provider routes and converged publications `1/1/0/0`; RSS growth was 1,069,056 bytes, descriptor and thread growth were zero, and unsupported OpenRC task count remained explicitly unavailable. Aggregate revisions were `12010` on the first sample and `12759` on the final post-restart process; native provider generation remained `6` on both sides of the process boundary.
fault: the pre-armed scheduler fired at exact `20:00:00Z`, but its safety check combined a regex-shaped `[k]` sentinel with fixed-string matching and therefore conservatively reported `collector_not_running`; it did not touch the service. The corrected guard performed the missed fault immediately at `20:01:43Z`, 103 seconds late as required by the dispatch's missed-fault rule. OpenRC changed child PID `31872→19327`, preserved executable SHA-256 `a713968fe5b84d5894588f8eb6be2b37b8a99f8bfb42d4d20c586e1226d8a5e6`, returned healthy/native/converged by `20:01:45Z`, and did not need the staged stop/start recovery fallback. The collector observed the new PID at elapsed 9,122.721 seconds with a healthy aggregate and successful peer read; its final identity remained started/supervised with exact `/usr/bin/koi --daemon`. OpenRC's per-generation `start_count` was zero before and after, so the canonical report truthfully records restart delta zero plus the independently observed single PID change.
koi state now/restoration: APK-owned `koi-1.0.0_pre0_git20260904-r2` and `koi-openrc-1.0.0_pre0_git20260904-r2` remain the only installed service deployment; exactly one `/usr/bin/koi --daemon` runs as child PID `19327` under supervisor PID `19326`, default-runlevel started/supervised, with the exact accepted binary hash above. Native mDNS is Ready with one desired/established publication, operator HTTP remains loopback-only on 5651, Pond desire remains false and 5654 is closed, Avahi remains installed but stopped/disabled, and `/run/koi.sock` reconstructed as mode 0600 `test:root`, inode `2638`. Config, data-root config, local-access policy, Pond intent, member identity, init, logrotate, APK world, group, repositories, and network-interface configuration remain byte-exact at SHA-256 `f3efca4fba827cc3a9bec60aeb138f0da41545fe5beea83257ea5e5515e1c374`, `a2c6d181be890c7807604d88a2e2318361bfc443b265f86dc61428f01d41a72a`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`, `5ecf787095e170708b9db9d705db44c4386c1732332f87864fbd7196f357d3eb`, `821510e1abb5cce853e06e09dc3ffce423e2a9af4026e775b0d5e6283b1bcb7d`, `fb260ead3c751912c5ef6e86429f108bb6f917feaabec17ee22d72e1bf78aedb`, `cd4a3a6a248c1b0bac45de90d159a439331c97bba2ff106dbc91234d3374132e`, `bba50a349020a7f929b02312afdc136234ce832945a92c73f60e6cf9612a2bcf`, `7293f98cf79a194e28daaee94f51b201527b057ad425abb46c1eebfbfabb02a8`, and `af1cbeb3da4a0448bc120f7255d36d424f4798a70bcf6976eeb8db9a7b554fb2`. Alpine did not mutate Bluefin; its Pond surface was still healthy when this hat completed, pending Bluefin-owned restoration after the other collectors.
evidence/cleanup: the redaction-attested canonical JSON, JUnit, and text bundle is retained at `.lab-runs/v1-20260904-od3-b3eb47e-alp/`, with SHA-256 `2e07bc4f…f181`, `3b4f079e…6e1e`, and `90a6d3f9…67bc`. The collector, restart scheduler, privileged shells, duplicate root-owned evidence, detached worktree, 750.8 MiB build target, and `/tmp/koi-*` pointers/scripts are gone; no credential helper or secret was retained. Repository publication of this entry completes Alpine's inherited-run handover; CachyOS still owns cross-hat reconciliation, Bluefin restoration, and the R01 campaign-activation decision.

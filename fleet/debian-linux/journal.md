# debian-linux — journal

## 2026-08-31 12:40 UTC — canary v2 (orchestrator-driven)
commit: da6032b | gates: cargo check -p koi-net →     Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.31s
koi state now: standing daemon on port 24441 (untouched by the canary)
stack: 
ladder (head): {"capabilities":[{"healthy":false,"name":"mdns","summary":"skipped: avahi-daemon is active"},{"healthy":true,"name":"certmesh","summary":"ready — run certmesh create"},{"healthy":false,"name":"dns","summary":"stopped"},{"healthy":true,"name":"health","summary":"0 services up (0 total)"},{"healthy"
findings: none (canary run)

## 2026-09-02 04:00 UTC — PH-001 public systemd lifecycle and rollback

commit: `c83c01b` rebased onto fleet head `66e6d12` (including concurrent installer hardening `3088b1f`); final installed release SHA-256 `7cca0c569223901900ea4e36715b19775e23aa16645e903763ee5af152b52184` | gates: `cargo fmt --all -- --check`; strict all-target clippy; full locked workspace tests/doctests; release build; focused merged recipe/trust tests 24/24; real install/failure rollback/uninstall/reinstall/crash recovery
koi state now: exactly one enabled+active root `koi.service`, PID `51019`, `/usr/local/bin/koi --daemon`, `NRestarts=1`, matching the release artifact above. Operator `stone` reaches it through mode-0600 `/run/koi.sock`; the root-only breadcrumb remains mode 0600. HTTP is loopback `127.0.0.1:24441`; DNS cooperatively owns UDP+TCP 53 on `127.0.0.1`, `172.17.0.1`, and `192.168.1.112` beside systemd-resolved; Avahi owns mDNS; Pond is intentionally disabled and 24444 is closed.
peers/run: none — local run `debian-ph1-20260902T0310Z`; no peer, Pond, provider, firewall, or external-host mutation
restoration: Avahi and systemd-resolved remained active; nftables/firewall state was not changed. The root-only fail-safe snapshot/timer and deliberately broken test artifacts were disarmed and removed after final health, so they are no longer recoverable. Intended product state is the one installed Koi service above; no temporary transaction manifest, backup, restore-stage, or `.new` file remains.
secret handling: commands and journal evidence printed no DAT, invite, key, certificate body, local-control token, or breadcrumb contents; evidence is redacted to identities, permissions, ports, PIDs, and hashes.
findings:

1. The prior installer could stop and replace the real service, warn on enable/start/health failure, and still return success. The systemd path now persists a phase-marked transaction under the machine data root, checkpoints binary/unit/config/operator policy with ownership and mode, treats reload/enable/start/process identity/health as hard gates, restores exact prior files and active/enabled state, and recovers an armed interrupted install before measuring the next transaction. Corrupt or incomplete recovery fails closed with the manifest/backup path. Concurrent upstream in-process rollback remains in place for non-systemd recipes.
2. A deliberately broken candidate (`4742a6a2edba227a56f587c6719d31e1afef94e29348fe93368f47beaec7d8fe`) failed its real `systemctl start`, returned exit 1, and restored the prior installed binary hash, unit, operator policy, active+enabled state, and a healthy new PID with no installer residue. The final rebased implementation retained that durable transaction and passed its focused snapshot, missing-backup, corrupt-manifest, interrupted-preparation, and port-migration tests.
3. The original legacy `koi.service.d/ports.conf` decision `24441:24442:24443:24444` migrated into `/etc/koi/config.toml`. A real public uninstall removed service/drop-in/socket/breadcrumb and left zero Koi processes while preserving binary, machine state, member identity, operator policy, and config hashes. Reinstalling from `/usr/local/bin/koi` was an ETXTBSY-safe same-file no-op and restored the same ports and authenticated state.
4. Ordinary `koi status` now truthfully reaches the root service. `koi trust diagnose` previously constructed the invoking user's empty default core and falsely reported Open; it now uses authenticated local control for the live `/v1/certmesh/diagnose` result and reports HEALTHY / Authenticated for `stone-halcyon-savanna`, with intact chain, no self-revocation, and healthy renewal. UID `nobody` cannot use the owner-private socket and receives offline state.
5. Final source artifact install moved the daemon to PID `50901`. An injected SIGKILL reconstructed the complete service as PID `51019`, preserved exact artifact hash and singleton identity, and left status/trust/DNS/mDNS healthy across the restart interval. The only warning-level journal entries are the expected injected SIGKILL/result pair.
6. `koi config path/show` remains misleading for an ordinary operator: it names the invoking user's `~/.config/koi/config.toml`, not the active system service's `/etc/koi/config.toml`. This is recorded as `fleet/debian-linux/issues/001-installed-config-discovery.md`; the expected fix is an additive authenticated service-config fact without changing explicit-config or user-service precedence.

## 2026-09-02 04:40 UTC — PH-001 installed config discovery closed

commit: `c8b7887` rebased onto fleet head `72dc7e1`; installed release SHA-256 `b6f2d5ba970dc709bab1e11167e88aa28cc627c0a47226e8491c9d820fbb8986` | gates: `cargo fmt --all -- --check`; strict all-target clippy for `koi-net`, `koi-client`, `koi-common`, and `koi-serve`; full locked workspace tests/doctests; focused local-control/config suites after rebase; final merged installer recipe suite 25/25; exact release rebuild; real system install and restart acceptance
koi state now: exactly one enabled+active root `koi.service`, PID `95615`, `/usr/local/bin/koi --daemon`, matching the release artifact above. Operator `stone` and root both resolve `/etc/koi/config.toml` through mode-0600 `/run/koi.sock` (owner `stone:root`); `koi status` is running and `koi trust diagnose` is HEALTHY / Authenticated for `stone-halcyon-savanna`. HTTP remains loopback `127.0.0.1:24441`; Avahi remains the selected mDNS provider; DNS remains cooperative on the prior address set; Pond remains intentionally disabled.
peers/run: none — local run `debian-ph1-config-20260902T043853Z`; no peer, provider, firewall, network, or external-host mutation
restoration: the first feature candidate exercised PID `51019→85942→86155`; after the final upstream rebase, the supported durable systemd install transaction upgraded `86155→95548` and a deliberate service restart produced final PID `95615`. Both passes preserved the exact config SHA-256 `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, unit SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, and operator-policy SHA-256 `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`. No transaction manifest or installer backup remains; service active/enabled state and singleton process count are exact, and there are no warning-or-worse journal entries since the final installation.
secret handling: commands and journal evidence printed no DAT, local-control token, config body, invite, key, or certificate body. `config show` was verified by header plus SHA-256 comparison only.
findings:

1. The daemon now carries its launch-selected config path from the composition root and exposes it with the resolved data root through a versioned, non-secret authenticated `info` response. This response is independent of DAT/HTTP access and remains available when HTTP is disabled. The existing token-bearing `access` response and owner-private breadcrumb remain unchanged.
2. `koi config path/show` now applies explicit `--config` > authenticated active daemon > invoking platform default. These actions run before loading an unrelated invoking-user config, so stale user files cannot mask installed-service facts and `config init --config <new>` can create its target. Older daemons or absent transports retain the prior default fallback.
3. Physical assertions proved ordinary operator path `/etc/koi/config.toml`, header `# /etc/koi/config.toml`, and a `config show` body hash exactly equal to the durable config hash. Root received the same authenticated fact. Explicit `--config /home/stone/repos/github/sylin-org/koi/Cargo.toml` won. UID `nobody`, with a controlled nonexistent home, received only `/nonexistent/.config/koi/config.toml`, proving it could not obtain the service fact.
4. Unix peer authorization now explicitly admits the installed operator or root, matching the existing Windows SYSTEM/Administrators/operator policy. Unit tests reject an unrelated UID, pin both request/response wire shapes, and prove `info` remains available with HTTP access disabled. The full native suite and post-rebase focused gates are green.

## 2026-09-02 20:41 UTC — PH-2/PH-3 provider, link, Pond, and hostile-input workbook

commit: source baseline `8f3d50b620a64d9db5e8a17fc3564eec70c43393` plus the resolve1 conflict-settlement correction recorded in this entry; installed `/usr/local/bin/koi` SHA-256 `a74edba5e75402fef9f5364b87ef2a60dac2ddd01c432371314a0df313ce491f`, final PID `24507` | gates: `cargo fmt --all -- --check`; strict all-target/all-feature clippy; full locked workspace tests/doctests; exact release rebuild; installed-service provider transition; primary-link churn; Pond independent-peer policy; bounded hostile DNS/mDNS/HTTP and resource sampling — PASS
koi state now: exactly one enabled+active root `koi.service`, PID `24507`, `NRestarts=0`, `/usr/local/bin/koi --daemon`, byte-identical to the final release artifact. Operator HTTP is still loopback-only on `127.0.0.1:24441`; Pond desire is false and 24444 is closed. DNS cooperatively owns UDP+TCP 53 on `127.0.0.1`, `172.17.0.1`, and `192.168.1.112`; mDNS is Ready with Avahi on all routes and desired=established=1, pending=failed=0; trust diagnosis is HEALTHY / Authenticated.
peers/runs: provider `20260902T202909Z-22323` PASS, link `20260902T203225Z-link` PASS, Pond `20260902T203312Z-24421` PASS, hostile input `20260902T203531Z-hostile` PASS. Independent peer test-01 `192.168.1.109` retained its installed system Koi PID `542996`, SHA-256 `8e3b94a9cfcaaa66f8c751bbb10e59f5b2196a2057d848c0ff8020d9395e24c3`, active state, and provider policy.
restoration: Avahi service/socket and systemd-resolved are active+enabled exactly as captured; resolved global and `enp2s0` mDNS remain `no`. `enp2s0` is Up again with DHCP address `192.168.1.112/24` and default gateway `192.168.1.1`; Koi rebound that address's DNS sockets. No `koi-ph001-*` recovery unit/timer remains. The config SHA-256 is still `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, unit `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, and operator policy `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`. The nftables topology is unchanged Docker NAT/forwarding with no host-input chain. Temporary askpass files, the peer sudo helper, UI clone, traffic fixture, recovery tasks, curl, and its newly installed libcurl4 dependency were removed. The pre-existing `libcurl3t64-gnutls` received Debian's current `deb13u4` security revision during dependency installation and was not intentionally downgraded to the no-longer-published `deb13u2` baseline.
secret handling: no DAT, local-control token, peer credential, invite, private key, certificate body, or config body entered evidence, URLs, the journal, or public status. Local-control material stayed in process-local stdin/variables; peer authentication used mode-0700 askpass files deleted on both hosts before completion.
findings:

1. The initial installed-artifact run `20260902T201421Z-17002` passed Avahi, resolved-native, and native-only but exposed false readiness after resolved returned: resolve1 accepted `RegisterService`, then its asynchronous conflict probe withdrew/renamed records while Koi still reported desired=established=3. The Linux adapter now subscribes to each returned `org.freedesktop.resolve1.DnssdService.Conflicted` signal, holds publication inside a five-second bounded settlement window (within the control plane's eight-second operation bound), explicitly unregisters a conflicted object, and returns typed `ProviderFailure::Conflict` for reconciliation. Unit tests pin conflict and quiet-window outcomes. A diagnostic two-second candidate (`20260902T202355Z-20518`) proved the real first conflict arrived after roughly three seconds; its cleanup restored the provider baseline before the five-second candidate was built and installed.
2. Final run `20260902T202909Z-22323` passed `avahi → systemd-resolved+native → native → systemd-resolved+native → avahi`. Every phase kept one installed process and proved both publication classes from test-01, reverse peer publication/resolve/TXT, the long-lived subscription, removal, route generations, synchronized desired/established counts, and exact external-provider restoration.
3. The link gate pre-armed a 15-second `ifup --force enp2s0` timer plus an independent 45-second `networking.service` fallback before `ifdown --force`. IPv4 and the default route disappeared; Koi retained PID `22278`/zero restarts, loopback operator access, and loopback/Docker DNS. DHCP restored the original address and route, Koi rebound `192.168.1.112:53`, and the successful first recovery canceled the fallback before it fired; both transient units unloaded.
4. Pond run `20260902T203312Z-24421` served every allowlisted UI/status read to test-01 on derived port 24444, returned 404 for DNS mutation, certmesh log, Pond control, and OpenAPI, proved explicit stop, reconstructed desired state after the one deliberate system-service restart (`22278→24507`), and restored the initial disabled desire. Neither UFW nor firewalld exists or is active; while enabled Koi truthfully reported firewall `unknown` with detail that other host policy was not assessed, and no firewall mutation was made.
5. Test-01 sent two separately bounded copies of 500 malformed/truncated UDP DNS datagrams, 100 length-mismatched TCP DNS sessions, 500 malformed multicast DNS datagrams, and 200 malformed/raw HTTP sessions. Each copy also required 100 excluded Pond requests across DNS mutation, certmesh log, Pond control, OpenAPI, and MCP to return 404 and proved 20 LAN attempts to operator port 24441 were refused. The first pass's non-privileged descriptor field was invalid and explicitly discarded; a fresh identical pass used privileged sampling. Across forty half-second samples RSS stayed 44,324–44,348 KiB, descriptors 20–20, threads/tasks 8–8, and systemd memory stayed 19,197,952–19,587,072 bytes; after 30 seconds it was 44,348 KiB/20 descriptors/8 threads/8 tasks. PID/restarts, artifact, route generation/routes/publication counts, bind policy, trust, and Pond restoration did not change, and the scoped journal contained no panic, fatal, OOM, descriptor-exhaustion, provider-failure, route-failure, token, or secret-bearing entry.

## 2026-09-02 23:33 UTC — PH-5 installed-service collector dry run

commit: the dry-run checkout reported fleet baseline `a296f42aa8a023ad2d37662c98e999f162e4d940` plus the then-uncommitted collector; the implementation was subsequently rebased onto fleet head `8e3faff` and landed as `8d6d3d6`. Observed installed `/usr/local/bin/koi` SHA-256 `a74edba5e75402fef9f5364b87ef2a60dac2ddd01c432371314a0df313ce491f`, version `1.0.0-rc.2`, PID `24507` | gates: `cargo fmt --all -- --check`; locked `koi-lab` tests 54/54 before and after rebase; strict locked all-target `koi-lab` clippy; locked full-workspace all-target tests; physical collector dry run — PASS
koi state now: exactly one enabled+active root `koi.service`, PID `24507`, `NRestarts=0`, `/usr/local/bin/koi --daemon`; its executable path and bytes matched the requested installed artifact before and after collection. Operator access remained on the mode-0600 local-control socket, HTTP remained loopback-only on `127.0.0.1:24441`, Avahi remained selected for publish/explicit-publish/browse/resolve at generation 1, DNS cache count remained 47, and publications remained desired=established=1 with pending=failed=0.
peers/run: dry run `v1-20260902T235000Z-ph5dry` used catalogued physical peer test-01 at `192.168.1.109:22` as a bounded cross-host TCP liveness sink. Four scoped connections succeeded from four attempts with zero retries; this is collector traffic/liveness evidence, not a claim of the later bidirectional product soak. The redaction-attested JSON/JUnit/text bundle is retained under `.lab-runs/v1-20260902T235000Z-ph5dry/installed-service.*`.
restoration: the collector created no daemon, registration, peer process, firewall rule, provider change, configuration, or durable traffic fixture. Each run-owned socket closed at the end of its probe; no established peer connection remained. The installed service retained PID `24507`, zero restart delta, active/running state, artifact hash, provider routes, and publication state exactly.
secret handling: the collector passes no credential, token, or secret in argv or evidence. It consumes only structured read-only local-control output, catalogued peer address, systemd facts, and PID-scoped `/proc` facts; the bundle has `secrets_redacted=true` and no failed checks.
findings:

1. `koi-lab installed-service-collect` now has hard duration/sample bounds and records initial/final service identity, the running executable's path/size/SHA-256, version, PID and service restart delta/rate, per-sample RSS/descriptors/threads/tasks, daemon health, DNS cache counts, mDNS generation/routes/publications, and bounded peer traffic attempts/retries/latency. Evidence uses the existing atomic JSON/JUnit/text boundary and a red verdict exits nonzero.
2. Debian's procfs policy permits status reads but denies an ordinary operator direct access to the root daemon's executable and descriptor directory. The collector first uses direct reads, then falls back only to non-interactive PID-scoped `sudo readlink/stat/sha256sum/find`; it does not broaden privilege or use a shell. The dry run measured RSS 29,757,440 bytes, 19 descriptors, and eight threads/tasks in all four samples.
3. This was deliberately a 6.2-second dry run, not the PH-5 acceptance soak. The collector is ready for the frozen candidate's bounded six-to-24-hour profile, where product traffic and coordinated controlled faults must supply the stronger mixed-OS verdict.

## 2026-09-03 03:26 UTC — PH-1 ownership-aware systemd acceptance

commit: source baseline `48f8e3e44569a49e20c4349793b101126e7f9536` (including shared ownership correction `41ad76b`); installed `/usr/local/bin/koi` SHA-256 `7a1356513791470bdbdbdba66d7ef27106a315914ecd5106980dd30317583091`, version `1.0.0-rc.2`, final PID `60502` | gates: `cargo fmt --all -- --check`; strict locked all-workspace/all-target clippy; full locked workspace/all-target tests and doctests; locked release build; physical in-place upgrade, failed-candidate rollback, corrupt-transaction rejection, uninstall, foreign-listener fresh install, and accepted reinstall — PASS
koi state now: exactly one enabled+active root `koi.service`, PID `60502`, `NRestarts=0`, exact `ExecStart=/usr/local/bin/koi --daemon`, and no drop-in. HTTP is loopback `127.0.0.1:24441`; the explicit durable run remains `24441:24442:24443:24444`; mDNS is Ready through Avahi on all routes with desired=established=1 and pending=failed=0; certmesh remains authenticated; cooperative DNS remains healthy; Pond remains intentionally disabled.
peers/run: none — local run `20260903T031942Z-debian-ph1` PASS. The serial coexistence fixture was a non-Koi Python process, PID `60166`, holding TCP `0.0.0.0:5641` through `:5644`; it was not a peer and was removed before final acceptance. No provider, peer, firewall, interface, or external-host state changed.
restoration: the accepted binary, unit, config, and operator policy compare byte-for-byte with the pre-test accepted snapshot. Their final SHA-256 values are respectively `7a1356513791470bdbdbdba66d7ef27106a315914ecd5106980dd30317583091`, `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, and `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`; member identity remained `b5f353c3acde70c16d62d9482a2d7d80c51a69513852fe8cfab3505ded1e8ee1`. The recovery timer, root snapshots, unhealthy binary, foreign listener, shifted test config, and local fixtures were removed and are no longer recoverable. No installer manifest, backup, stage file, second Koi, or standard/shifted test listener remains.
secret handling: no DAT, local-control token, invite, key, certificate body, breadcrumb contents, or config body entered the journal or command evidence. Evidence is limited to non-secret service facts, hashes, ports, PIDs, modes, and health summaries.
findings:

1. In-place installation from the new release replaced the sole real service (`24507→59228`) while preserving the declared `24441:24444` run. Config, unit, and operator-policy hashes did not move, and the installer explicitly reported that the existing config decision won; it neither probed the running Koi as foreign nor manufactured a new config.
2. A deliberately unhealthy candidate SHA-256 `73de8d08648b80cd277b848c59c97ab4dbd409bf130442c249ce5bd8c0876ef2` exited under `--daemon`. The public installer returned exit 1 after the real `systemctl start` failure, restored the exact accepted files and enabled/active state, started healthy PID `59822`, and left no durable transaction residue.
3. A deliberately malformed `/var/lib/koi/state/systemd-install-transaction.json` made the next install fail closed with the exact unreadable-manifest path and JSON error. PID `59822`, every accepted hash, activity, and enablement remained unchanged; only the run-owned corrupt file was then removed.
4. After a real uninstall left zero Koi processes, the foreign listener held the whole standard run. A fresh product install selected `5651:5652:5653:5654`, persisted that choice, passed its `/healthz` gate, and ran exactly one Koi beside the non-Koi fixture. A second uninstall, fixture teardown, and restore of the accepted config made reinstall choose the explicit `24441:24442:24443:24444` run without probing. Final hashes, identity, singleton service, and health all match the accepted deployment.

## 2026-09-03 06:03 UTC — PH-4 refrozen candidate Debian artifact-ready slice

commit: exact frozen source `5c89e9de11bf23ab81fd8b5b0778c58477359360`; installed `/usr/local/bin/koi` SHA-256 `51682d682f155139f0e0d4c7f2bdce6b2e237f6bbf464ccdbfc7b30a7251cbbb`, 54,098,960 bytes, version `1.0.0-rc.2`, final PID `133634` | gates: clean detached locked formatting; strict workspace/all-target clippy; full locked workspace/all-target tests; locked release build; public systemd install; installed Find/Name/Trust/Serve journey; restart persistence and derived-state removal; 30-second installed-service collector — PASS. Independent-peer Pond is not claimed because no catalogued peer credential or SSH identity is available on this host.
koi state now: exactly one enabled+active root `koi.service`, PID `133634`, `NRestarts=0`, exact `ExecStart=/usr/local/bin/koi --daemon`, matching the detached release artifact above. The explicit durable run remains `24441:24442:24443:24444`; operator HTTP is loopback-only, Pond desire is false and 24444 is closed, DNS has zero static entries, health has zero service checks, proxy has zero listeners, mDNS is Ready through Avahi with desired=established=1 and pending=failed=0, and certmesh remains HEALTHY / Authenticated.
peers/run: local journey `20260903T054500Z-debian-ph4-refreeze`; collector `v1-20260903T060300Z-debianph4` PASS against physical CachyOS peer test-01 at `192.168.1.109:22`: 7/7 bounded connections, zero retries, zero service restarts, stable PID/artifact, and seven healthy samples (RSS 42,029,056–42,364,928 bytes; 19 descriptors; 8 threads/tasks). The peer's SSH service was reachable, but BatchMode authentication was denied and the catalogued `KOI_TEST01_PASSWORD` was unset, so the restoration-gated Pond workbook could not cross its prerequisite boundary and no peer or Pond state was mutated.
restoration: the accepted binary is the intentional candidate change. Unit, `/etc/koi/config.toml`, operator policy, member identity, data-root config, and Pond intent are restored to SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `b5f353c3acde70c16d62d9482a2d7d80c51a69513852fe8cfab3505ded1e8ee1`, `46017703d090561b05d2b87a134a93d2a1cda1bbee74b6b6f42410d09b009d3a`, and `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`. The run-owned mDNS publication withdrew; its bounded CLI exited. DNS/health state files, proxy config, port 24445 listener, installer manifest/stages, and recovery timer are absent.
secret handling: no DAT, local-control token, peer credential, invite, private key, certificate body, or config secret entered repository evidence. Certificate verification recorded only TLS parameters and `CN=stone-halcyon-savanna`; the collector bundle declares `secrets_redacted=true`.
findings:

1. The first frozen artifact derived every proxy health check as HTTP but passed a documented bare backend through unchanged. `127.0.0.1:24441` therefore reported `http_error: builder error` while its TLS listener returned HTTP 200. The shared health boundary in the fleet's authoritative concurrent correction now normalizes a bare backend to `http://127.0.0.1:24441` and reconciles removed proxy state; tests cover valid derived configs and removal. This invalidates both earlier PH-4 revisions and re-freezes the fleet at the exact source above. A narrower local diagnostic commit was deliberately dropped during rebase rather than competing with the shared fix.
2. The frozen candidate also includes the shared-config correction from `9134b32`. A bare-backend proxy persisted `[proxy]`, survived a real `systemctl restart` (`132916→133174`), served TLS 1.3 with successful CA verification and the expected CN, and reconstructed its listener. Its normalized derived HTTP check was Up before and after restart and the proxy returned HTTP 200. After product removal, the listener closed and the derived service list became empty within two seconds without a restart.
3. Find/Name/Trust used authenticated local control and public product paths: a run-owned `_koi-ph4._tcp` publication resolved with exact TXT through Avahi, while status truthfully reported resolve1 unavailable because host policy is `MulticastDNS=no` and native ready as fallback; a static `.internal` record resolved through CLI and `dig`; trust diagnosis reported signed/not-encrypted HEALTHY identity, intact chain, non-revoked leaf, and healthy renewal. All objects were removed through their product APIs before exact file restoration.
4. This host is artifact-ready and can remain the stable observer/collector peer. Debian's independent-peer Pond read/denial/restart transaction remains the sole local PH-4 item pending an authenticated physical peer; the six-to-24-hour PH-5 soak remains prohibited until the fleet records PH-4 green.

## 2026-09-03 12:23 UTC — PH-4 Debian Pond credential preflight

commit: fleet dispatch `6d9bffedde3f60edbaa643d472dc9d1015c73820`; installed frozen artifact remains source `5c89e9de11bf23ab81fd8b5b0778c58477359360`, `/usr/local/bin/koi` SHA-256 `51682d682f155139f0e0d4c7f2bdce6b2e237f6bbf464ccdbfc7b30a7251cbbb` | gates: coordination `ph4-5c89-debian-pond-01` local/peer preflight — BLOCKED before the first mutation because the referenced machine-local credential source is absent
koi state now: unchanged exact collector baseline: one enabled+active root `koi.service`, PID `133634`, `NRestarts=0`, exact `ExecStart=/usr/local/bin/koi --daemon`; operator HTTP is loopback-only on `127.0.0.1:24441`, Pond desire is false and 24444 is closed. Unit, `/etc/koi/config.toml`, operator policy, member identity, data-root config, and Pond intent remain SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `b5f353c3acde70c16d62d9482a2d7d80c51a69513852fe8cfab3505ded1e8ee1`, `46017703d090561b05d2b87a134a93d2a1cda1bbee74b6b6f42410d09b009d3a`, and `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`.
peers/run: no Pond run was started. The catalogued CachyOS reader `test@192.168.1.109:22` is reachable, and its stored ED25519 host key resolves to the exact pinned `SHA256:xw/JKOjleL/nhvx06mWtXQSlec9Ue/ydUHbmFHopdNw`; key-only authentication is denied as expected. `tools/koi-lab/lab.json` supplies only the `KOI_TEST01_PASSWORD` variable name. That variable is unset, and an exhaustive read-only search of the mounted local filesystem found no `local/NOTES.md` or alternate approved handoff. The repo path is intentionally ignored but does not exist on this host.
restoration: no Koi, peer, service, provider, firewall, UI, package, configuration, credential, or network state was mutated. The accepted five-file UI bundle remains byte-exact: `app.js` `43c8334407a2186faa89f76ebe73aabe69c41fd4adbe4e6a61b205ac5d40dbdb`, `index.html` `aaf26404a0269acb9613028f107fb34f567a5bbc2bffced5311f05c5947b7e2a`, `koi.png` `91aea43e2587f53242b9dbc4bf794d8147dcd915a7e356fa3943422900fdd33c`, `sentences.js` `17edaa62a634d8c619bc9fe16f6861f2c8f51f78c5b4d6b0ca2e1f783a61c332`, and `styles.css` `2a4450f321e9cc9270733f5e110b500ee88cd356340314b93ea7faf7724caf8a`.
secret handling: no password was guessed, requested interactively, placed in argv, or written to evidence. No askpass helper was created because no approved secret source was available. The missing credential handoff must be provisioned outside Git at the brief's ignored `local/NOTES.md` path (or an equivalently explicit approved local source) before this transaction can cross its preflight boundary.
findings:

1. The convergence dispatch correctly says that an unset convenience environment variable is not itself a blocker, but its named fallback source is not installed on this Debian machine. The remaining Pond acceptance is blocked by that absent machine-local input, not by product behavior; no product defect is claimed.

## 2026-09-03 13:29 UTC — PH-4 frozen-candidate independent-peer Pond complete

commit: exact frozen source `5c89e9de11bf23ab81fd8b5b0778c58477359360`, exercised under synchronized fleet dispatch `f137480f6668247897985c72ad35f791a3baf641`; installed `/usr/local/bin/koi` SHA-256 `51682d682f155139f0e0d4c7f2bdce6b2e237f6bbf464ccdbfc7b30a7251cbbb`, version `1.0.0-rc.2`, PID `133634→137916` | gates: `scripts/integration/pond-lan.sh` coordination `ph4-5c89-debian-pond-01`, run `20260903T132626Z-137828` — PASS
koi state now: exactly one enabled+active root `koi.service`, PID `137916`, `NRestarts=0`, exact `ExecStart=/usr/local/bin/koi --daemon`, and health 200. Operator HTTP remains loopback-only on `127.0.0.1:24441`; Pond is restored desired=false/running=false/disabled and 24444 is closed. mDNS is Ready with Avahi selected for publish/browse/resolve and one alive registration; certmesh is authenticated, DNS has zero static/certmesh entries, health has zero service checks, and proxy has zero listeners.
peers/run: physical CachyOS reader `test@192.168.1.109:22`, hostname `test-01`, Python 3.14.7, pinned ED25519 identity `SHA256:xw/JKOjleL/nhvx06mWtXQSlec9Ue/ydUHbmFHopdNw`. The enabled and post-systemd-restart phases each returned 200 for `/`, the four fixed assets, `/healthz`, `/v1/status`, `/v1/mdns/browser/snapshot`, and `/v1/dns/entries`; POST `/v1/dns/add` plus GET `/v1/certmesh/log`, `/v1/pond`, and `/openapi.json` each remained absent. Explicit stop and final restoration both closed the peer surface, and an independent final peer probe found both Debian 24444 and operator 24441 refused/unreachable. The peer performed only bounded Python reads; no peer file, service, provider, firewall, or Koi state was mutated. Evidence is retained at `.lab-runs/ph4-5c89-debian-pond-01/20260903T132626Z-137828/`.
restoration: the gate reconstructed desired Pond state across the one deliberate `systemctl restart` and then restored the initial false desire. Binary, unit, `/etc/koi/config.toml`, operator policy, member identity, data-root config, and Pond intent are byte-exact at SHA-256 `51682d682f155139f0e0d4c7f2bdce6b2e237f6bbf464ccdbfc7b30a7251cbbb`, `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `b5f353c3acde70c16d62d9482a2d7d80c51a69513852fe8cfab3505ded1e8ee1`, `46017703d090561b05d2b87a134a93d2a1cda1bbee74b6b6f42410d09b009d3a`, and `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`. The accepted UI remains exact: `app.js` `43c8334407a2186faa89f76ebe73aabe69c41fd4adbe4e6a61b205ac5d40dbdb`, `index.html` `aaf26404a0269acb9613028f107fb34f567a5bbc2bffced5311f05c5947b7e2a`, `koi.png` `91aea43e2587f53242b9dbc4bf794d8147dcd915a7e356fa3943422900fdd33c`, `sentences.js` `17edaa62a634d8c619bc9fe16f6861f2c8f51f78c5b4d6b0ca2e1f783a61c332`, and `styles.css` `2a4450f321e9cc9270733f5e110b500ee88cd356340314b93ea7faf7724caf8a`. Neither UFW nor firewalld is installed/active, so Koi truthfully reported firewall `unknown`; no firewall result or mutation was manufactured.
secret handling: the operator-supplied lab login entered only a process environment consumed by a mode-0700 local askpass helper; it never entered SSH argv, Git, the journal, or retained evidence. The helper, single-key known-hosts file, readable UI copy, root snapshot, recovery script/timer, two pre-mutation empty attempt directories, `curl`, and the run-added `libcurl4t64` were removed. A bounded evidence scan found no credential variable, token header, authorization header, bearer value, askpass reference, or private key.
findings:

1. No product defect was found. Two local harness invocations exited before their mutation boundary because the ordinary operator cannot traverse `/var/lib/koi` for UI source files and this sudo policy permits commands but rejects bare `sudo -v`; the passing run used a hash-verified run-scoped UI copy and the same gate through passwordless root execution. Only the successful bundle was retained.
2. Debian's required frozen-candidate Pond slice is green. This host has returned to its stable collector baseline and did not start PH-5.

## 2026-09-03 14:44 UTC — PH-4 exact-source replacement and Pond acceptance

commit: exact frozen source `e49bfe2b3e403fa87d4b8b237b49f3bb9e5cb5ef`, exercised under synchronized fleet dispatch `767288ec6add579ddb216c8fcba4f5e5f46c9a95`; clean Debian release and installed `/usr/local/bin/koi` are byte-identical at SHA-256 `51682d682f155139f0e0d4c7f2bdce6b2e237f6bbf464ccdbfc7b30a7251cbbb`, 54,098,960 bytes, version `1.0.0-rc.2`, final PID `170890` | gates: formatting; strict locked workspace/all-target/all-feature clippy; full locked workspace/all-target/all-feature tests and doctests; locked release build; public systemd install; `ph4-e49b-debian-pond-01` physical gate — PASS
koi state now: exactly one enabled+active root `koi.service`, PID `170890`, `NRestarts=0`, exact `ExecStart=/usr/local/bin/koi --daemon`, health 200 after more than one mDNS browse interval, and no warning-or-worse run-scoped journal event. The explicit durable run remains `24441:24442:24443:24444`; operator HTTP is loopback-only on `127.0.0.1:24441`, Pond is desired=false/running=false/disabled and 24444 is closed. mDNS is Ready with Avahi selected for publish/browse/resolve and one alive registration; certmesh is HEALTHY / Authenticated for `stone-halcyon-savanna`; DNS has zero static/certmesh entries, health has zero service checks, and proxy has zero listeners.
peers/run: product install changed the sole service PID `137916→170569` while preserving the declared shifted run. Pond run `20260903T144303Z-170802` then used unchanged physical CachyOS reader `test@192.168.1.109:22`, hostname `test-01`, Python 3.14.7, pinned ED25519 identity `SHA256:xw/JKOjleL/nhvx06mWtXQSlec9Ue/ydUHbmFHopdNw`; the required systemd reconstruction changed Debian PID `170569→170890`. Both enabled and recovered phases returned 200 for `/`, four fixed assets, `/healthz`, `/v1/status`, `/v1/mdns/browser/snapshot`, and `/v1/dns/entries`; POST `/v1/dns/add` and GET `/v1/certmesh/log`, `/v1/pond`, and `/openapi.json` remained absent. Explicit stop and final restoration closed the peer surface; an independent final peer probe found both 24444 and operator 24441 refused/unreachable. CachyOS remained one active Koi, PID `962933`, `NRestarts=0`, exact installed SHA-256 `f0e999b0077eb25935f1ad563aee33f2e659ffde915d3f3c55f5aa568691682b`. Evidence is retained at `.lab-runs/ph4-e49b-debian-pond-01/20260903T144303Z-170802/`.
restoration: unit, `/etc/koi/config.toml`, operator policy, member identity, data-root config, and Pond intent remain byte-exact at SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `b5f353c3acde70c16d62d9482a2d7d80c51a69513852fe8cfab3505ded1e8ee1`, `46017703d090561b05d2b87a134a93d2a1cda1bbee74b6b6f42410d09b009d3a`, and `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`. The accepted UI remains exact: `app.js` `43c8334407a2186faa89f76ebe73aabe69c41fd4adbe4e6a61b205ac5d40dbdb`, `index.html` `aaf26404a0269acb9613028f107fb34f567a5bbc2bffced5311f05c5947b7e2a`, `koi.png` `91aea43e2587f53242b9dbc4bf794d8147dcd915a7e356fa3943422900fdd33c`, `sentences.js` `17edaa62a634d8c619bc9fe16f6861f2c8f51f78c5b4d6b0ca2e1f783a61c332`, and `styles.css` `2a4450f321e9cc9270733f5e110b500ee88cd356340314b93ea7faf7724caf8a`. No installer manifest, backup, stage, firewall change, or external-host mutation remains. The detached build tree, root snapshot, recovery timer/script, askpass and single-key helpers, readable UI copy, `curl`, and run-added `libcurl4t64` were removed; only the successful Pond bundle remains.
secret handling: the approved lab credential existed only in the private process environment consumed by the mode-0700 askpass helper; it did not enter SSH argv, Git, journal text, or retained evidence. A bounded evidence scan found no credential variable, token/authorization header, bearer value, askpass reference, or private key.
findings:

1. The Windows-only `e49bfe2` correction correctly compiles to the same Debian binary bytes as the superseded source. Exact provenance was nevertheless established by the clean detached full gate/build and real product install, not inferred from byte equality.
2. No product defect was found. Debian's exact-source PH-4 artifact and independent-peer Pond lane are green; this host is back at the stable collector baseline and PH-5 was not started.

## 2026-09-03 18:02 UTC — PH-5 headless correction and systemd canary

commit: corrected product source `14fb5dffb9209aa16b49b5fd4b12bc4cd0090707`, collector peer-identity follow-up `6304cef568787527ad8367da7e3b2162fd689d84`, and accepted report source `f0e97f19d3a317e9652e76c4ea4317d5340dbb88`; release and installed `/usr/local/bin/koi` are byte-identical at SHA-256 `ee85030e8f000a34c54ea0ca56af4ee4250701fdec71646e623bc1d6af75714b`, 54,118,208 bytes, version `1.0.0-rc.2`, PID `198321` | gates: `cargo fmt --all -- --check`; locked workspace all-target strict clippy; full `cargo test --locked` plus doctests; locked release build; public systemd install; neutral-shell installed entry; `v1-ph5-debian-systemd-canary-02` — PASS
koi state now: exactly one enabled+active root `koi.service`, PID `198321`, `NRestarts=0`, exact `ExecStart=/usr/local/bin/koi --daemon`, with only loopback operator HTTP listening on `127.0.0.1:24441`. The durable `24441:24442:24443:24444` config remains selected; Pond is again desired=false/running=false and 24444 is closed. Unit, `/etc/koi/config.toml`, operator policy, member identity, data-root config, and Pond intent are byte-exact at SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `b5f353c3acde70c16d62d9482a2d7d80c51a69513852fe8cfab3505ded1e8ee1`, `46017703d090561b05d2b87a134a93d2a1cda1bbee74b6b6f42410d09b009d3a`, and `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`; no warning-or-worse event appeared after installation.
headless entry: from an empty `/tmp` directory with only ordinary `HOME`, `USER`, `LOGNAME`, and `PATH`, the installed CLI discovered authenticated local control without checkout, Cargo, alternate data root, endpoint, or token input. `koi status` reported the live capability ladder; `koi mdns admin status` reported Avahi Ready and gave the actionable unavailable explanation `resolve1 MulticastDNS=no; publication API or announce mode unavailable`; `koi trust diagnose` was HEALTHY / Authenticated. `koi pond status` reported disabled state and port 24444, `koi pond start` returned exact URL `http://192.168.1.112:24444/` with the truthful no-UFW/no-firewalld assessment, physical CachyOS read `/healthz` as HTTP 200 `OK`, and `koi pond stop` restored the exact initial intent bytes.
peers/run: independently installed CachyOS peer `test-01` at `192.168.1.109`, pinned ED25519 identity `SHA256:xw/JKOjleL/nhvx06mWtXQSlec9Ue/ydUHbmFHopdNw`, remained active/running at systemd PID `721`, `NRestarts=0`, installed SHA-256 `f0e999b0077eb25935f1ad563aee33f2e659ffde915d3f3c55f5aa568691682b`. A run-scoped local forward exposed only that peer's loopback Koi `/healthz` to the collector. The schema-2 systemd report captured five samples over 20.246 seconds: installed PID/image and restart count stayed fixed; every local-control health/provider/publication sample was available and converged at generation 1 with routes on Avahi and counts `1/1/0/0`; RSS grew 45,056 bytes; descriptors stayed 19, threads/tasks stayed 8; five peer Koi health reads succeeded with zero retries. All 15 bounded verdicts passed. Evidence is retained at `.lab-runs/v1-ph5-debian-systemd-canary-02/` as canonical JSON, JUnit, and text.
restoration: the physical peer performed only read-only Koi and Python health requests; no peer service, provider, firewall, file, or Koi state changed. The SSH forward, control socket, mode-0700 askpass helper, single-key known-hosts file, neutral entry directory, and byte-exact root rollback copy were removed. An independent final CachyOS probe received connection refusal on Debian 24444; local port 29441 is closed. The product-owned installer transaction committed cleanly with no manifest, stage, or backup residue.
secret handling: the operator-supplied test-host credential was consumed only by the removed mode-0700 askpass helper, never placed in SSH argv, Git, journal text, or retained evidence. The report attests `secrets_redacted=true` and contains only the peer label plus loopback forwarded surface.
findings:

1. The installed `e49bfe2` CLI had no Pond command, forcing a headless operator below the supported product boundary to discover sharing state and obtain its URL. The correction adds authenticated `koi pond status|start|stop`; start prints the daemon-returned socket URL and exits non-zero with its exact recovery explanation when sharing cannot become reachable. This shipped product change reopens PH-4 and requires an explicit new freeze; no `e49bfe2` evidence is relabeled.
2. The former collector combined systemd parsing, cadence, verdicts, and generic TCP port-22 reachability. The replacement keeps one neutral bounded sampler/verdict/evidence core, moves native facts into a thin systemd observer, leaves explicit OpenRC/SCM seams for their owning hats, records unsupported counters as unavailable instead of zero, and requires a semantic Koi `/healthz` peer surface. Debian's short canary is green; the coordinated duration soak remains blocked on the new freeze and remaining adapters/canaries.

## 2026-09-04 15:48 UTC — OD-2 headless systemd boundary accepted

commit: current OD-2 product baseline `e64b50e7146d2aff4fe0546763833098432faae8`, synchronized evidence tree `107f2db4953b8a146f50dc4f8d8db78b68152591`, and test-only collector provenance commit `ae097c53b19c77bf0e591ef267dfb91835e86bf7`; release and installed `/usr/local/bin/koi` are byte-identical at SHA-256 `899d83efe76b6ba82aa38484f828f6879384cd321d11db49662cdb2fec95a1cc`, 62,433,248 bytes, version `1.0.0-dev.0`, final PID `50905` | gates: `cargo fmt --all -- --check`; locked workspace/all-target/all-feature strict clippy; full `cargo test --locked` plus doctests; locked `koi-net` release and current `koi-lab` builds; public systemd install; neutral-shell CLI and aggregate inventory; real restart reconstruction; physical Pond gate; schema-3 systemd collector — **PASS, DEBIAN OD-2 COMPLETE**
koi state now: exactly one enabled+active root `koi.service`, PID `50905`, `NRestarts=0`, exact `/usr/local/bin/koi --daemon`. The durable shifted run remains `24441:24442:24443:24444`; operator HTTP is the sole matching listener on `127.0.0.1:24441`, Pond is desired=false/running=false/disabled and 24444 is closed. Avahi and systemd-resolved remain active+enabled. Authenticated aggregate inventory is healthy at mDNS generation 6 with concrete Avahi publish/explicit-publish/browse/resolve routes and publications `1/1/0/0`; the final observed aggregate revision was 184 before the provenance-only collector rerun. The owner-private socket remains `0600 stone:root`, and the root breadcrumb remains `0600 root:root`.
headless boundary: from a neutral empty directory with only ordinary `HOME`, `USER`, `LOGNAME`, and `PATH`, the installed CLI found the service through authenticated local control. `koi status --json` reported the live ten-rung aggregate, `koi config path` returned `/etc/koi/config.toml`, `koi trust diagnose --json` reported HEALTHY / Authenticated for `stone-halcyon-savanna`, and `koi pond status --json` reported the persisted disabled port 24444 plus UI revision `sha256:e054291c96b3f07a376b94b9740b5c4770f66133aa35036ecc2a80420ee97593`. `/v1/host` returned the explicit `HostIdentity` `{hostname: stone-halcyon-savanna, hostname_fqdn: stone-halcyon-savanna.local, os: linux, arch: x86_64}` with LAN interface `enp2s0` / `192.168.1.112`; `/v1/inventory` carried the same installed aggregate truth rather than independently reconstructed defaults. A direct systemd restart changed PID `50215→50314`; the service returned healthy and converged from the transient empty publication view to generation 6, Avahi routes, and `1/1/0/0` without changing artifact or restart count.
peers/runs: Pond run `20260904T154318Z-50711` used independently installed CachyOS Koi `test-01` at `192.168.1.109`, PID `1223362`, `NRestarts=0`, SHA-256 `26ca480d0cbc0cdb5196500986ca41312a8a4ce089a5c4fc54883dbc4bf78433`, built from product tree `ab84782bec70400603088172901976f75cadd010` in its published `a94c4f7d4447bdf0870f75d649b81e890e64ba72` tree. The run served two immutable generations (`sha256:84485e21b38e0164102683812e92a7882038c5c43186e8386ed5d73ede0e1638` and restored current `sha256:e054291c96b3f07a376b94b9740b5c4770f66133aa35036ecc2a80420ee97593`), proved every allowed UI/status/inventory read and every excluded/mutation denial from the physical peer, explicit stop, and desired-state reconstruction through systemd PID `50314→50905`. Its verdict is PASS with baseline/final desire false, inactive firewall adapter, no temporary firewall mutation, and exact restoration; evidence is retained at `.lab-runs/od2-e64-debian-pond-01/20260904T154318Z-50711/`.
collector: canonical run `v1-od2-e64-debian-systemd-canary-02` records exact source commit `ae097c53b19c77bf0e591ef267dfb91835e86bf7`, observer `systemd`, service node `stone-halcyon-savanna`, and peer label `test-01`. Seven samples over 34.727 seconds held PID `50905`, artifact, active/running state, and `NRestarts=0`; authenticated aggregate revisions advanced `298→334`, every sample was healthy at provider generation 6 with concrete Avahi routes and `1/1/0/0` publications, and all carried 46 mDNS cache entries. The run made seven valid reads of the peer's real Pond `/v1/status`, peer revisions `9068→9098`, with zero retries. RSS grew 53,248 bytes; descriptors remained 20 and threads/tasks remained 10. All fourteen verdict checks passed with zero PID changes, unavailable samples, recovery events, or threshold failures; the redaction-attested JSON/JUnit/text bundle is retained at `.lab-runs/v1-od2-e64-debian-systemd-canary-02/`.
restoration: unit, `/etc/koi/config.toml`, operator policy, member identity, data-root config, and Pond intent are byte-exact at SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `b5f353c3acde70c16d62d9482a2d7d80c51a69513852fe8cfab3505ded1e8ee1`, `46017703d090561b05d2b87a134a93d2a1cda1bbee74b6b6f42410d09b009d3a`, and `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`. Neither UFW nor firewalld exists locally; the existing nftables topology was not mutated. CachyOS retained its exact PID/artifact/restart identity and returned to Pond desired=false/running=false at revision 35/generation 14; its UFW policy was never changed. No installer transaction/stage/backup, local forward, control socket, helper, readable UI copy, second Koi, or warning-or-worse run-scoped journal event remains. Run-added `curl` and `libcurl4t64` were removed, while the pre-existing `libcurl3t64-gnutls 8.14.1-2+deb13u4` remains. Superseded diagnostic bundles from the earlier pre-synchronization artifact and stale collector executable were removed; only the exact current-tree Pond and collector bundles are retained.
secret handling: the operator-supplied lab login existed only in a process environment consumed by a removed mode-0700 askpass helper. It never entered SSH argv, Git, journal text, or retained evidence. Bounded scans of both canonical bundles found no credential variable, token/authorization header, bearer value, askpass reference, or private key.
findings:

1. No product defect was found. A full-suite run exposed a test-only scheduling race: the cancellation test awaited every oneshot reply but could inspect the completion probe before the worker's immediately following increment. The test now waits for the already-required completion count, matching the neighboring cancellation test. The focused case passed ten consecutive isolation repeats and then passed under the complete locked workspace run; formatting and strict clippy remain green. Because the change is under `cfg(test)`, the OD-2 product baseline and installed bytes are unchanged.
2. Debian closes the fifth and final OD-2 native row. The service remains the stable headless systemd peer; no release candidate or long soak is claimed by this entry.

## 2026-09-04 16:19 UTC — OD-3 frozen-candidate readiness

commit: frozen candidate `b3eb47e08817045f9371703d780ada9aab00995d`, synchronized dispatch `c606d9462371412a165d45b0446591c4d689e53d`, campaign `v1-20260904-od3-b3eb47e`; installed `/usr/local/bin/koi` and retained accepted `target/release/koi` are byte-identical at SHA-256 `899d83efe76b6ba82aa38484f828f6879384cd321d11db49662cdb2fec95a1cc`, 62,433,248 bytes, version `1.0.0-dev.0` — **READY**
koi state now: exactly one enabled+active system `koi.service`, PID `50905`, `NRestarts=0`, exact `/usr/local/bin/koi --daemon`, with only operator HTTP on `127.0.0.1:24441` among the shifted `24441:24442:24443:24444` ports. The owner-private socket is `0600 stone:root`; the root breadcrumb is `0600 root:root`. Pond remains intentionally desired=false/running=false/disabled on 24444. Avahi and systemd-resolved remain active+enabled; the installed aggregate is healthy at mDNS generation 6 with publish/explicit-publish/browse/resolve all routed through Avahi and publications `1/1/0/0`. Trust is HEALTHY / Authenticated for `stone-halcyon-savanna`; `/v1/host` returned the expected Linux x86_64 identity and LAN interface `enp2s0` / `192.168.1.112`, and `/v1/inventory` carried the same aggregate provider and publication facts.
equivalence: the complete `e64b50e7146d2aff4fe0546763833098432faae8..b3eb47e08817045f9371703d780ada9aab00995d` change set is fleet/docs evidence, the Alpine-only `APKBUILD` correction, and a three-line wait inside `blocking_worker.rs`'s `cfg(test)` module; no Debian product source, dependency, installer, unit, or shipped asset changed. Current dispatch head differs from the frozen commit only in the active epic. Unit, `/etc/koi/config.toml`, operator policy, member identity, data-root config, and Pond intent remain byte-exact at SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `b5f353c3acde70c16d62d9482a2d7d80c51a69513852fe8cfab3505ded1e8ee1`, `46017703d090561b05d2b87a134a93d2a1cda1bbee74b6b6f42410d09b009d3a`, and `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`.
restoration/readiness boundary: this reconciliation was read-only. No build, install, restart, Pond exposure, collector, provider, firewall, peer, or network mutation was repeated because equivalence passed. There are no systemd jobs, installer transaction/stage/backup artifacts, or warning-or-worse Koi journal entries since the accepted OD-2 run. Debian is ready at the exact final PID/hash/state above and will not start a private soak; the next action is the CachyOS coordinator's all-hat readiness barrier and announced UTC T0.

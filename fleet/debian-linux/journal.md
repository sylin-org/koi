# debian-linux — journal

## 2026-08-31 12:40 UTC — canary v2 (orchestrator-driven)
commit: da6032b | gates: cargo check -p koi-net →     Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.31s
koi state now: standing daemon on port 24441 (untouched by the canary)
stack: 
ladder (head): {"capabilities":[{"healthy":false,"name":"mdns","summary":"skipped: avahi-daemon is active"},{"healthy":true,"name":"certmesh","summary":"ready — run certmesh create"},{"healthy":false,"name":"dns","summary":"stopped"},{"healthy":true,"name":"health","summary":"0 services up (0 total)"},{"healthy"
findings: none (canary run)

## 2026-09-02 04:00 UTC — PH-001 public systemd lifecycle and rollback

commit: `c83c01b` rebased onto fleet head `66e6d12` (including concurrent installer hardening `3088b1f`); final installed release SHA-256 `7cca0c569223901900ea4e36715b19775e23aa16645e903763ee5af152b52184` | gates: `cargo fmt --all -- --check`; strict all-target clippy; full locked workspace tests/doctests; release build; focused merged recipe/trust tests 24/24; real install/failure rollback/uninstall/reinstall/crash recovery
koi state now: exactly one enabled+active root `koi.service`, PID `51019`, `/usr/local/bin/koi --daemon`, `NRestarts=1`, matching the release artifact above. Operator `stone` reaches it through mode-0600 `/run/koi.sock`; the root-only breadcrumb remains mode 0600. HTTP is loopback `127.0.0.1:24441`; DNS cooperatively owns UDP+TCP 53 on `127.0.0.1`, `172.17.0.1`, and `192.168.1.112` beside systemd-resolved; Avahi owns mDNS; Pond is intentionally disabled and 24444 is closed.
peers/run: none — local run `debian-ph1-20260902T0310Z`; no peer, Pond, provider, firewall, or external-host mutation
restoration: Avahi and systemd-resolved remained active; nftables/firewall state was not changed. The root-only fail-safe snapshot/timer and deliberately broken test artifacts were disarmed and removed after final health, so they are no longer recoverable. Intended product state is the one installed Koi service above; no temporary transaction manifest, backup, restore-stage, or `.new` file remains.
secret handling: commands and journal evidence printed no DAT, invite, key, certificate body, local-control token, or breadcrumb contents; evidence is redacted to identities, permissions, ports, PIDs, and hashes.
findings:

1. The prior installer could stop and replace the real service, warn on enable/start/health failure, and still return success. The systemd path now persists a phase-marked transaction under the machine data root, checkpoints binary/unit/config/operator policy with ownership and mode, treats reload/enable/start/process identity/health as hard gates, restores exact prior files and active/enabled state, and recovers an armed interrupted install before measuring the next transaction. Corrupt or incomplete recovery fails closed with the manifest/backup path. Concurrent upstream in-process rollback remains in place for non-systemd recipes.
2. A deliberately broken candidate (`4742a6a2edba227a56f587c6719d31e1afef94e29348fe93368f47beaec7d8fe`) failed its real `systemctl start`, returned exit 1, and restored the prior installed binary hash, unit, operator policy, active+enabled state, and a healthy new PID with no installer residue. The final rebased implementation retained that durable transaction and passed its focused snapshot, missing-backup, corrupt-manifest, interrupted-preparation, and port-migration tests.
3. The original legacy `koi.service.d/ports.conf` decision `24441:24442:24443:24444` migrated into `/etc/koi/config.toml`. A real public uninstall removed service/drop-in/socket/breadcrumb and left zero Koi processes while preserving binary, machine state, member identity, operator policy, and config hashes. Reinstalling from `/usr/local/bin/koi` was an ETXTBSY-safe same-file no-op and restored the same ports and authenticated state.
4. Ordinary `koi status` now truthfully reaches the root service. `koi trust diagnose` previously constructed the invoking user's empty default core and falsely reported Open; it now uses authenticated local control for the live `/v1/certmesh/diagnose` result and reports HEALTHY / Authenticated for `stone-halcyon-savanna`, with intact chain, no self-revocation, and healthy renewal. UID `nobody` cannot use the owner-private socket and receives offline state.
5. Final source artifact install moved the daemon to PID `50901`. An injected SIGKILL reconstructed the complete service as PID `51019`, preserved exact artifact hash and singleton identity, and left status/trust/DNS/mDNS healthy across the restart interval. The only warning-level journal entries are the expected injected SIGKILL/result pair.
6. `koi config path/show` remains misleading for an ordinary operator: it names the invoking user's `~/.config/koi/config.toml`, not the active system service's `/etc/koi/config.toml`. This is recorded as `fleet/debian-linux/issues/001-installed-config-discovery.md`; the expected fix is an additive authenticated service-config fact without changing explicit-config or user-service precedence.

## 2026-09-02 04:40 UTC — PH-001 installed config discovery closed

commit: `c8b7887` rebased onto fleet head `72dc7e1`; installed release SHA-256 `b6f2d5ba970dc709bab1e11167e88aa28cc627c0a47226e8491c9d820fbb8986` | gates: `cargo fmt --all -- --check`; strict all-target clippy for `koi-net`, `koi-client`, `koi-common`, and `koi-serve`; full locked workspace tests/doctests; focused local-control/config suites after rebase; final merged installer recipe suite 25/25; exact release rebuild; real system install and restart acceptance
koi state now: exactly one enabled+active root `koi.service`, PID `95615`, `/usr/local/bin/koi --daemon`, matching the release artifact above. Operator `stone` and root both resolve `/etc/koi/config.toml` through mode-0600 `/run/koi.sock` (owner `stone:root`); `koi status` is running and `koi trust diagnose` is HEALTHY / Authenticated for `stone-halcyon-savanna`. HTTP remains loopback `127.0.0.1:24441`; Avahi remains the selected mDNS provider; DNS remains cooperative on the prior address set; Pond remains intentionally disabled.
peers/run: none — local run `debian-ph1-config-20260902T043853Z`; no peer, provider, firewall, network, or external-host mutation
restoration: the first feature candidate exercised PID `51019→85942→86155`; after the final upstream rebase, the supported durable systemd install transaction upgraded `86155→95548` and a deliberate service restart produced final PID `95615`. Both passes preserved the exact config SHA-256 `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, unit SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, and operator-policy SHA-256 `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`. No transaction manifest or installer backup remains; service active/enabled state and singleton process count are exact, and there are no warning-or-worse journal entries since the final installation.
secret handling: commands and journal evidence printed no DAT, local-control token, config body, invite, key, or certificate body. `config show` was verified by header plus SHA-256 comparison only.
findings:

1. The daemon now carries its launch-selected config path from the composition root and exposes it with the resolved data root through a versioned, non-secret authenticated `info` response. This response is independent of DAT/HTTP access and remains available when HTTP is disabled. The existing token-bearing `access` response and owner-private breadcrumb remain unchanged.
2. `koi config path/show` now applies explicit `--config` > authenticated active daemon > invoking platform default. These actions run before loading an unrelated invoking-user config, so stale user files cannot mask installed-service facts and `config init --config <new>` can create its target. Older daemons or absent transports retain the prior default fallback.
3. Physical assertions proved ordinary operator path `/etc/koi/config.toml`, header `# /etc/koi/config.toml`, and a `config show` body hash exactly equal to the durable config hash. Root received the same authenticated fact. Explicit `--config /home/stone/repos/github/sylin-org/koi/Cargo.toml` won. UID `nobody`, with a controlled nonexistent home, received only `/nonexistent/.config/koi/config.toml`, proving it could not obtain the service fact.
4. Unix peer authorization now explicitly admits the installed operator or root, matching the existing Windows SYSTEM/Administrators/operator policy. Unit tests reject an unrelated UID, pin both request/response wire shapes, and prove `info` remains available with HTTP access disabled. The full native suite and post-rebase focused gates are green.

## 2026-09-02 20:41 UTC — PH-2/PH-3 provider, link, Pond, and hostile-input workbook

commit: source baseline `8f3d50b620a64d9db5e8a17fc3564eec70c43393` plus the resolve1 conflict-settlement correction recorded in this entry; installed `/usr/local/bin/koi` SHA-256 `a74edba5e75402fef9f5364b87ef2a60dac2ddd01c432371314a0df313ce491f`, final PID `24507` | gates: `cargo fmt --all -- --check`; strict all-target/all-feature clippy; full locked workspace tests/doctests; exact release rebuild; installed-service provider transition; primary-link churn; Pond independent-peer policy; bounded hostile DNS/mDNS/HTTP and resource sampling — PASS
koi state now: exactly one enabled+active root `koi.service`, PID `24507`, `NRestarts=0`, `/usr/local/bin/koi --daemon`, byte-identical to the final release artifact. Operator HTTP is still loopback-only on `127.0.0.1:24441`; Pond desire is false and 24444 is closed. DNS cooperatively owns UDP+TCP 53 on `127.0.0.1`, `172.17.0.1`, and `192.168.1.112`; mDNS is Ready with Avahi on all routes and desired=established=1, pending=failed=0; trust diagnosis is HEALTHY / Authenticated.
peers/runs: provider `20260902T202909Z-22323` PASS, link `20260902T203225Z-link` PASS, Pond `20260902T203312Z-24421` PASS, hostile input `20260902T203531Z-hostile` PASS. Independent peer test-01 `192.168.1.109` retained its installed system Koi PID `542996`, SHA-256 `8e3b94a9cfcaaa66f8c751bbb10e59f5b2196a2057d848c0ff8020d9395e24c3`, active state, and provider policy.
restoration: Avahi service/socket and systemd-resolved are active+enabled exactly as captured; resolved global and `enp2s0` mDNS remain `no`. `enp2s0` is Up again with DHCP address `192.168.1.112/24` and default gateway `192.168.1.1`; Koi rebound that address's DNS sockets. No `koi-ph001-*` recovery unit/timer remains. The config SHA-256 is still `8535f64049d1a63a1d08648e9ad3b633be219c382e8fc58bbdfaca5b85a0d9d5`, unit `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, and operator policy `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`. The nftables topology is unchanged Docker NAT/forwarding with no host-input chain. Temporary askpass files, the peer sudo helper, UI clone, traffic fixture, recovery tasks, curl, and its newly installed libcurl4 dependency were removed. The pre-existing `libcurl3t64-gnutls` received Debian's current `deb13u4` security revision during dependency installation and was not intentionally downgraded to the no-longer-published `deb13u2` baseline.
secret handling: no DAT, local-control token, peer credential, invite, private key, certificate body, or config body entered evidence, URLs, the journal, or public status. Local-control material stayed in process-local stdin/variables; peer authentication used mode-0700 askpass files deleted on both hosts before completion.
findings:

1. The initial installed-artifact run `20260902T201421Z-17002` passed Avahi, resolved-native, and native-only but exposed false readiness after resolved returned: resolve1 accepted `RegisterService`, then its asynchronous conflict probe withdrew/renamed records while Koi still reported desired=established=3. The Linux adapter now subscribes to each returned `org.freedesktop.resolve1.DnssdService.Conflicted` signal, holds publication inside a five-second bounded settlement window (within the control plane's eight-second operation bound), explicitly unregisters a conflicted object, and returns typed `ProviderFailure::Conflict` for reconciliation. Unit tests pin conflict and quiet-window outcomes. A diagnostic two-second candidate (`20260902T202355Z-20518`) proved the real first conflict arrived after roughly three seconds; its cleanup restored the provider baseline before the five-second candidate was built and installed.
2. Final run `20260902T202909Z-22323` passed `avahi → systemd-resolved+native → native → systemd-resolved+native → avahi`. Every phase kept one installed process and proved both publication classes from test-01, reverse peer publication/resolve/TXT, the long-lived subscription, removal, route generations, synchronized desired/established counts, and exact external-provider restoration.
3. The link gate pre-armed a 15-second `ifup --force enp2s0` timer plus an independent 45-second `networking.service` fallback before `ifdown --force`. IPv4 and the default route disappeared; Koi retained PID `22278`/zero restarts, loopback operator access, and loopback/Docker DNS. DHCP restored the original address and route, Koi rebound `192.168.1.112:53`, and the successful first recovery canceled the fallback before it fired; both transient units unloaded.
4. Pond run `20260902T203312Z-24421` served every allowlisted UI/status read to test-01 on derived port 24444, returned 404 for DNS mutation, certmesh log, Pond control, and OpenAPI, proved explicit stop, reconstructed desired state after the one deliberate system-service restart (`22278→24507`), and restored the initial disabled desire. Neither UFW nor firewalld exists or is active; while enabled Koi truthfully reported firewall `unknown` with detail that other host policy was not assessed, and no firewall mutation was made.
5. Test-01 sent two separately bounded copies of 500 malformed/truncated UDP DNS datagrams, 100 length-mismatched TCP DNS sessions, 500 malformed multicast DNS datagrams, and 200 malformed/raw HTTP sessions. Each copy also required 100 excluded Pond requests across DNS mutation, certmesh log, Pond control, OpenAPI, and MCP to return 404 and proved 20 LAN attempts to operator port 24441 were refused. The first pass's non-privileged descriptor field was invalid and explicitly discarded; a fresh identical pass used privileged sampling. Across forty half-second samples RSS stayed 44,324–44,348 KiB, descriptors 20–20, threads/tasks 8–8, and systemd memory stayed 19,197,952–19,587,072 bytes; after 30 seconds it was 44,348 KiB/20 descriptors/8 threads/8 tasks. PID/restarts, artifact, route generation/routes/publication counts, bind policy, trust, and Pond restoration did not change, and the scoped journal contained no panic, fatal, OOM, descriptor-exhaustion, provider-failure, route-failure, token, or secret-bearing entry.

## 2026-09-02 23:33 UTC — PH-5 installed-service collector dry run

commit: the dry-run checkout reported fleet baseline `a296f42aa8a023ad2d37662c98e999f162e4d940` plus the then-uncommitted collector; the implementation was subsequently rebased onto fleet head `8e3faff` and landed as `8d6d3d6`. Observed installed `/usr/local/bin/koi` SHA-256 `a74edba5e75402fef9f5364b87ef2a60dac2ddd01c432371314a0df313ce491f`, version `1.0.0-rc.2`, PID `24507` | gates: `cargo fmt --all -- --check`; locked `koi-lab` tests 54/54 before and after rebase; strict locked all-target `koi-lab` clippy; locked full-workspace all-target tests; physical collector dry run — PASS
koi state now: exactly one enabled+active root `koi.service`, PID `24507`, `NRestarts=0`, `/usr/local/bin/koi --daemon`; its executable path and bytes matched the requested installed artifact before and after collection. Operator access remained on the mode-0600 local-control socket, HTTP remained loopback-only on `127.0.0.1:24441`, Avahi remained selected for publish/explicit-publish/browse/resolve at generation 1, DNS cache count remained 47, and publications remained desired=established=1 with pending=failed=0.
peers/run: dry run `v1-20260902T235000Z-ph5dry` used catalogued physical peer test-01 at `192.168.1.109:22` as a bounded cross-host TCP liveness sink. Four scoped connections succeeded from four attempts with zero retries; this is collector traffic/liveness evidence, not a claim of the later bidirectional product soak. The redaction-attested JSON/JUnit/text bundle is retained under `.lab-runs/v1-20260902T235000Z-ph5dry/installed-service.*`.
restoration: the collector created no daemon, registration, peer process, firewall rule, provider change, configuration, or durable traffic fixture. Each run-owned socket closed at the end of its probe; no established peer connection remained. The installed service retained PID `24507`, zero restart delta, active/running state, artifact hash, provider routes, and publication state exactly.
secret handling: the collector passes no credential, token, or secret in argv or evidence. It consumes only structured read-only local-control output, catalogued peer address, systemd facts, and PID-scoped `/proc` facts; the bundle has `secrets_redacted=true` and no failed checks.
findings:

1. `koi-lab installed-service-collect` now has hard duration/sample bounds and records initial/final service identity, the running executable's path/size/SHA-256, version, PID and service restart delta/rate, per-sample RSS/descriptors/threads/tasks, daemon health, DNS cache counts, mDNS generation/routes/publications, and bounded peer traffic attempts/retries/latency. Evidence uses the existing atomic JSON/JUnit/text boundary and a red verdict exits nonzero.
2. Debian's procfs policy permits status reads but denies an ordinary operator direct access to the root daemon's executable and descriptor directory. The collector first uses direct reads, then falls back only to non-interactive PID-scoped `sudo readlink/stat/sha256sum/find`; it does not broaden privilege or use a shell. The dry run measured RSS 29,757,440 bytes, 19 descriptors, and eight threads/tasks in all four samples.
3. This was deliberately a 6.2-second dry run, not the PH-5 acceptance soak. The collector is ready for the frozen candidate's bounded six-to-24-hour profile, where product traffic and coordinated controlled faults must supply the stronger mixed-OS verdict.

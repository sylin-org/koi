# bluefin-linux — journal

## 2026-09-01 16:52 EDT — installed-service provider transition

commit: `d72a9f3` tree; installed artifact SHA-256 `8a0a14dda27b49dbd72f0bfeb79efc3e73cb3392c144c74924f2443f73bb6b27` | gates: physical transition PASS `20260901T205042Z-45196`
koi state then: exactly one enabled system `koi.service`, PID `37751`, `/usr/local/bin/koi --daemon`, standard ports; Avahi and systemd-resolved active+enabled; one permanent self-publication.
findings:

1. Bluefin replaced the former Omarchy installation on the same test-02 hardware/address. The measured host is Bluefin 44.20260714, GNOME Shell 50.3, Wayland; old Omarchy facts are historical only.
2. The one installed PID completed generations 9–13: `avahi → systemd-resolved+native → native → systemd-resolved+native → avahi`. Every phase held desired=established=3 and pending=failed=0. The unchanged test-01 peer was system PID `404624`, SHA-256 `1a994a78b8b40218bd27abf76f992db60b9fc42124187a43cf782c8ca887581c`.
3. Both Koi APIs performed bidirectional publish/resolve/TXT/removal in every phase, including explicit-address publication and one subscription across all generations. Cleanup restored Avahi service/socket, resolved service/activation sockets, enablement, and global/link mDNS facts exactly.

## 2026-09-01 17:29 EDT — native immutable-workstation acceptance

commit: koi `d72a9f3`; koi-desktop `f1e2cdc` | gates: desktop UI 34/34; Rust 8 pass/1 ignored; strict clippy; release build; native RPM install; fresh-login runtime
koi state then: one enabled system daemon and one XDG-autostarted `/usr/bin/koi-desktop --minimized`; `koi-0.1.0-1.x86_64` was the only rpm-ostree local package.
findings:

1. Build-only dependencies stayed in the Fedora 44 `koi-dev` toolbox. A native RPM installed the binary, desktop entry, and icon durably. Tauri's bundled linuxdeploy/strip cannot read Fedora 44 RELR sections, so AppImage is not a truthful artifact from this toolchain.
2. A real reboot and GNOME login launched exactly one workbench. The SNI registered at `/org/ayatana/NotificationItem/tray_icon_tray_app_koi`; its real `Open Workbench` menu item revealed the window without creating another process, and `--poke` also preserved singleton ownership.
3. GNOME/Wayland measured a 1100×37 titlebar over exact 1100×720 content; the earlier oversized decoration was absent. Discover, Browser, Diff, DNS, Trust, About, Glance, tray, service controls, and autostart were exercised against the real daemon.
4. The Status data-root tile incorrectly displayed the literal Windows placeholder `%ProgramData%\koi`. This became the authenticated local-control correction below.

## 2026-09-01 18:06 EDT — authenticated data root and 0.1.1 upgrade

commit: koi `ac9a98e`; koi-desktop `bd216cf` | gates: core local-control/client/serve tests; koi-net all-target + Windows GNU checks; strict clippy; UI 35/35; desktop Rust 9 pass/1 ignored; strict clippy; release build; Bluefin toolbox repeat; native RPM; reboot; AT-SPI; two-host mDNS
koi state now: exactly one enabled system daemon, PID `2237`, `/usr/local/bin/koi --daemon`, SHA-256 `e85e7ae89b33b68d97bcf8028b9078d75c4fb11fbf7d9d6dac0827796ed0a4dd`; exactly one `/usr/bin/koi-desktop --minimized`, PID `1738`; rpm-ostree local package `koi-0.1.1-1.x86_64`; Avahi owns all four mDNS routes at generation 1 with desired=established=1 and pending=failed=0.
findings:

1. The daemon composition root now hands its resolved data directory through the authenticated, versioned local-control response. It is absent from public `/v1/status`; old clients ignore it and new clients against old daemons render “not reported” instead of guessing. The installed desktop's live accessibility text interface returned exactly `/var/lib/koi` (12 characters).
2. The desktop RPM upgraded atomically from 0.1.0 to 0.1.1. A real boot launched one daemon and one minimized UI; both loopback health endpoints passed. Temporary GDM autologin was used only to create the physical Wayland seat, then `/etc/gdm/custom.conf` was restored byte-for-byte (SHA-256 `04022d30d148bf53461bc0fc6f8c5df8fbfcf590c06b684ce975e1e057894541`). Credential and accessibility helpers were deleted after validation.
3. A post-upgrade two-host gate published one run-owned `_koi-upgrade._tcp` record per machine. Bluefin resolved test-01's host/address/43101/TXT and test-01 resolved Bluefin's host/address/43102/TXT; both withdrawals were observed, leaving only each daemon's permanent registration.
4. The Pond bundle contains no machine-local path, its read-only CSS hides the data-root tile, and public status has no `data_root`. A second-host curl exposed a separate defect: Phone returns a LAN URL while the standard service is bound to `127.0.0.1:5641`. See `issues/001-pond-needs-read-only-listener.md`.
5. `systemd-remount-fs.service` (overlay reconfigure) and `ublue-nvctk-cdi.service` (NVIDIA driver not loaded) are degraded in both the pre-upgrade and current boots. They are base-image/hardware facts, not deployment regressions; Koi's journal has no warnings.

## 2026-09-01 20:55 EDT — ADR-042, native RPM 0.1.2, and fresh-boot acceptance

commit: koi product `dbd2466`, gate portability through `739b902`; koi-desktop source through `4c05ed2` | gates: full toolbox core gates; desktop Rust/UI/strict-clippy/release gates; native RPM upgrade; real GNOME login/SNI/AT-SPI; three-host Pond; current-candidate mDNS peer
koi state now: exactly one enabled system `koi.service`, PID `9720`, `/usr/local/bin/koi --daemon`, SHA-256 `89bc5ed0f0edfa7fd9163847a5cab0b23fa3a03fed9fc32789e39ea4d690658f`; operator HTTP remains `127.0.0.1:5641`; Pond is disabled and 5644 is closed; Avahi owns all mDNS routes. Exactly one installed `/usr/bin/koi-desktop --minimized`, PID `7087`, runs from rpm-ostree local package `koi-0.1.2-1.x86_64`.
findings:

1. Bluefin physical run `20260902T004557Z-9528` exercised ADR-042 from independent peers test-01 and test-03. Every allowlisted asset/read, refused mutation/excluded route, explicit stop, and restart recovery passed. The sole service changed serially from PID `5889→9720`; the executable hash stayed fixed and the original disabled desire returned.
2. Firewalld's existing FedoraWorkstation policy already permits the derived high TCP port, so no host firewall mutation was needed. The gate initially exposed that immutable hosts may have neither `socat` nor a traditional `nc`; the shared harness now talks to authenticated Unix control with the Python standard library when needed, without adding a host package or weakening the product path.
3. The fresh boot beginning `2026-09-01 20:11:49 EDT` produced one real GNOME session and one XDG-autostarted workbench at `20:41:59`. GNOME's live StatusNotifier watcher contains exactly Koi's Ayatana item, and `Open Workbench` revealed the native window. Status and Phone used authenticated local control; Stop sharing closed the listener.
4. Temporary GDM autologin existed only to establish the earlier physical seat and was removed; `/etc/gdm/custom.conf` still matches its byte-exact original SHA-256 `04022d30d148bf53461bc0fc6f8c5df8fbfcf590c06b684ce975e1e057894541`. No validation drop-in remains. The RPM owns the executable, desktop entry, and icon.
5. In current provider run `20260902T004825Z-546815`, this one installed Koi was the peer at unchanged PID `9720` and hash `89bc5e…658f` while test-01 completed all five plans. Bluefin's Koi unit, Avahi service/socket, and resolved service returned to their captured active+enabled facts.

## 2026-09-01 23:26 EDT — PH-1 transactional systemd lifecycle and immutable rollback

commit: this commit, based on `4c9f27e`; installed koi SHA-256 `d50ebc19e3f32b722dac0cebb4ab233665044d2a7892e743ee297ffa3df93fc9` | gates: fmt; strict all-target clippy; full locked workspace tests/doctests; release build; real upgrade/uninstall/reinstall
koi state now: exactly one enabled system `koi.service`, PID `16294`, `/usr/local/bin/koi --daemon`, matching the release artifact above; operator HTTP is `127.0.0.1:5641`, Pond is disabled/5644 closed, DNS cooperatively owns UDP+TCP on `127.0.0.1:53` and `192.168.1.95:53`, and Avahi owns every mDNS route. Exactly one booted `/usr/bin/koi-desktop --minimized`, PID `3046`, remains healthy from the rpm-ostree rollback deployment `koi-0.1.1-1`; the native `0.1.2` RPM (SHA-256 `562b214998c9a2bcc23b30bce575b65458fc5cdaa026342104f09d2ec9a03054`) is staged for the next boot.
peers/run: none — local PH-1 lifecycle; no cross-host assertion or peer mutation
restoration: pre-mutation archive `/tmp/koi-bluefin-ph1-pre.tar`, SHA-256 `23fbada56ad8719cb276623ada1d8a01d9566cd33b61d8d84415ed2f5ade7e2b`; Avahi, resolved, and firewalld remained active+enabled; rpm-ostree's refused live package replacement was reset, leaving `/usr/bin/koi-desktop` byte-identical to the booted 0.1.1 artifact (`f7e535f7104fd54149a87231e28f77907129b14ed53fe295c08648a4c7100393`).
findings:

1. The shared systemd installer previously stopped the real service, replaced product files, printed warnings for enable/start/health failure, and returned success. Installation now snapshots binary, unit, config, and local-operator policy bytes/modes; treats stop, reload, enable, start, and health as hard gates; and restores prior files, enablement, and running state on failure. Focused snapshot tests cover both replacement restoration and removal of a newly introduced file.
2. The one installed daemon completed an in-place upgrade (`2225→15464`), real uninstall (unit absent, service inactive, installed binary intentionally preserved), and reinstall from `/usr/local/bin/koi` (`16294`). The operator remained UID 1000, the artifact hash matched throughout the final install, the desktop stayed singleton, and a 150-second final soak crossed the provider retry interval with no Koi warnings.
3. The booted immutable deployment proved rollback from desktop RPM 0.1.2 to 0.1.1. rpm-ostree refused an unsafe same-name live replacement; its reset retained the booted 0.1.1 bytes and staged a normal 0.1.2 deployment. Activating and verifying that deployment requires the next real reboot and is not claimed by this entry.
4. Measured trust state is Open (`CA not initialized`), contrary to the older authenticated-member journal state. No invite was available or reused, so this session preserved the measured state instead of inventing enrollment evidence.

## 2026-09-02 00:21 EDT — PH-1 native RPM 0.1.2 reboot activation checkpoint

commit: koi source `c658cde`; installed daemon SHA-256 `d50ebc19e3f32b722dac0cebb4ab233665044d2a7892e743ee297ffa3df93fc9`; koi-desktop source `4c05ed2`; installed RPM `koi-0.1.2-1.x86_64`; installed desktop SHA-256 `89c74984af61f35d5a5d06905547963c67936364356b5f084c3d15d41ebd7ca2` | gates: rpm-ostree staged-deployment activation; real boot; systemd readiness; GNOME XDG autostart; live SNI registration; CLI/HTTP health
koi state now: boot ID `dfaf8186bef14923b35e124c938c09a8`; exactly one enabled system daemon, PID `2211`, `/usr/local/bin/koi --daemon`, with zero systemd restarts; exactly one `/usr/bin/koi-desktop --minimized`, PID `2975`, launched by the real GNOME session from product-owned XDG autostart; operator HTTP is `127.0.0.1:5641`; Pond is disabled and 5644 is closed; Avahi owns publish/browse/resolve; cooperative DNS owns UDP+TCP on `127.0.0.1:53` and `192.168.1.95:53`.
peers/run: none — local post-reboot PH-1 activation checkpoint; no cross-host assertion or peer mutation
restoration: no mutation in this continuation; the previously staged rpm-ostree deployment is now booted, no further staged deployment remains, and no temporary login, provider, firewall, credential, or test state was introduced.
findings:

1. The deferred 0.1.2 activation from the preceding entry is complete. `rpm -q` and ownership checks resolve `/usr/bin/koi-desktop` to `koi-0.1.2-1.x86_64`; the executable, desktop entry, and icon are package-owned, while the system daemon remains the recorded product-owned `/usr/local/bin/koi` candidate.
2. The real user GNOME/Wayland session launched one workbench at session initialization. GNOME Shell's live `org.kde.StatusNotifierWatcher` reports exactly `:1.48@/org/ayatana/NotificationItem/tray_icon_tray_app_koi`, with the host registered; no duplicate Koi process or item exists.
3. The daemon became ready at boot with PID `2211`, reports Koi `v1.0.0-rc.2`, healthy authenticated local control, Avahi routes, cooperative DNS, and disabled Pond. The only startup warning is the truthful absence of an optional Docker/Podman runtime backend.
4. This entry is the durable post-reboot checkpoint: the 0.1.2 activation must not be repeated by a later agent continuation.

## 2026-09-02 00:32 EDT — PH-2 GNOME lock recovery and shared-candidate native gates

commit: this commit, based on `a98c8d7`; installed daemon SHA-256 `d50ebc19e3f32b722dac0cebb4ab233665044d2a7892e743ee297ffa3df93fc9`; installed desktop RPM `koi-0.1.2-1.x86_64`, SHA-256 `89c74984af61f35d5a5d06905547963c67936364356b5f084c3d15d41ebd7ca2` | gates: fmt; strict all-target clippy; full locked workspace tests/doctests; release `koi-net` build; real GNOME lock/unlock; singleton UI poke; post-retry-interval service/UI health
koi state now: exactly one enabled system daemon, unchanged PID `2211`, with zero systemd restarts; exactly one `/usr/bin/koi-desktop --minimized`, unchanged PID `2975`; the unlocked GNOME shell reports exactly one Koi StatusNotifier item; operator HTTP is healthy on `127.0.0.1:5641`; Pond remains disabled and 5644 closed; Avahi owns publish/browse/resolve; cooperative DNS retains `127.0.0.1:53` and `192.168.1.95:53`.
peers/run: none — local PH-2 session recovery and native candidate gates; no cross-host assertion or peer mutation
restoration: GNOME session 2 returned to active and `LockedHint=no`; daemon, desktop, service unit, installed artifacts, Avahi, resolved, firewalld, interfaces, firewall rules, and Pond desire were not mutated. The attempted privileged crash injection was refused before mutation because this non-interactive session has no administrator authorization.
findings:

1. Locking the real GNOME/Wayland session changed `LockedHint` to yes while daemon PID `2211` and desktop PID `2975` remained unchanged. GNOME Shell's StatusNotifier watcher was unavailable while the shell was locked, then immediately restored the same single `:1.48@/org/ayatana/NotificationItem/tray_icon_tray_app_koi` registration after unlock. `koi-desktop --poke` acknowledged the existing process without creating a duplicate.
2. Bluefin independently found that shared commit `c658cde` failed `cargo fmt --check` in the new Windows Bonjour label decoder. The shared correction is rustfmt-only (one expression, no semantic or wire change); strict clippy, the complete locked workspace suite, doctests, and the optimized `koi-net` build pass in the Fedora 44 `koi-dev` toolbox afterward.
3. Crash, provider-service, firewalld, primary-interface, and suspend mutations require administrator authorization unavailable to this agent session. No gate is claimed for them, and no host policy was weakened to manufacture one.

## 2026-09-02 00:46 EDT — PH-2 crash, provider, and primary-interface recovery checkpoint

commit: `6f55416`; installed daemon SHA-256 `d50ebc19e3f32b722dac0cebb4ab233665044d2a7892e743ee297ffa3df93fc9`; installed desktop RPM `koi-0.1.2-1.x86_64`, SHA-256 `89c74984af61f35d5a5d06905547963c67936364356b5f084c3d15d41ebd7ca2` | gates: systemd SIGKILL recovery; Avahi/resolved/native local route loss and restoration; NetworkManager primary-interface loss and restoration
koi state now: exactly one enabled system daemon, PID `19596`, after the expected single systemd crash restart `2211→19596`; exactly one unchanged `/usr/bin/koi-desktop --minimized`, PID `2975`; operator HTTP is healthy on `127.0.0.1:5641`; Pond remains disabled; Avahi owns publish/browse/resolve; cooperative DNS owns `127.0.0.1:53` and restored `192.168.1.95:53` with its mDNS projection repopulated.
peers/run: none — local PH-2 fault recovery; no cross-host assertion or peer mutation
restoration: Avahi service/socket, resolved service/monitor/varlink sockets, and firewalld are active+enabled as captured; all runtime masks are absent; NetworkManager restored `Wired connection 1`, `192.168.1.95/24`, and the `192.168.1.1` default route; both root-owned timed restoration units completed or were canceled and no timer remains.
findings:

1. SIGKILL of the sole installed daemon produced systemd result `signal`; the unit's configured five-second recovery started exactly one replacement at PID `19596`, incremented `NRestarts` once, restored HTTP/local control/DNS/mDNS, and did not disturb desktop PID `2975`.
2. A plain Avahi stop was truthfully ineffective because socket/D-Bus activation restarted it. With interruption-safe runtime masks in place, Koi changed `Ready→Reconciling→Ready` and promoted from Avahi to native publish/browse with browse-based resolve fallback in five seconds, without a Koi restart. Masking resolved left the native route healthy. Restoring resolved did not promote it because the measured Bluefin policy is globally and per-link `-mDNS`; restoring Avahi reclaimed all three routes in nine seconds.
3. Disconnecting primary interface `enp0s31f6` removed LAN reachability while loopback health remained `OK`, Koi PID `19596` stayed unchanged, and discovery drained to zero records. The pre-armed NetworkManager restoration recovered the same connection, IPv4 address, route, Avahi ownership, cooperative DNS listener set, and 58-record projection without restarting Koi.
4. This is the durable pre-suspend checkpoint. A later continuation must perform post-suspend verification first and must not suspend the machine again merely because the initiating agent session ended.

## 2026-09-02 00:50 EDT — PH-2 suspend recovery and PH-3 Unix-control boundary

commit: `3901c64`; installed daemon SHA-256 `d50ebc19e3f32b722dac0cebb4ab233665044d2a7892e743ee297ffa3df93fc9`; installed desktop RPM `koi-0.1.2-1.x86_64`, SHA-256 `89c74984af61f35d5a5d06905547963c67936364356b5f084c3d15d41ebd7ca2` | gates: 32-second RTC suspend/resume; network/provider/DNS/UI recovery; intended-operator Unix socket access; unrelated-UID negative access
koi state now: boot ID remains `dfaf8186bef14923b35e124c938c09a8`; exactly one system daemon, unchanged PID `19596`, with only the earlier intentional crash restart; exactly one desktop, unchanged PID `2975`; GNOME session 2 is active and unlocked with exactly one Koi StatusNotifier item; HTTP, Avahi, cooperative DNS, and disabled Pond are healthy.
peers/run: none — local PH-2/PH-3 boundaries; the unchanged installed artifact retains prior three-host Pond/firewalld run `20260902T004557Z-9528`
restoration: no reboot occurred; Ethernet, address, route, Avahi/resolved/firewalld active+enabled facts, FedoraWorkstation zone bytes/semantics, and unlocked session state match baseline; temporary account `koi-boundary-probe`, runtime masks, and every run-owned restoration timer/unit are absent.
findings:

1. `rtcwake -m mem -s 30` suspended from `00:47:28` to `00:48:00` EDT. The first post-resume poll found the original boot ID, daemon PID `19596`, desktop PID `2975`, connected Ethernet, loopback health `OK`, Avahi ownership, 58-record DNS projection, and the same single tray registration; neither Koi process duplicated or restarted.
2. `/var/run/koi.sock` is `test:root 0600`. The intended operator opened it successfully. A run-owned locked system account at UID 992 received `PermissionError: EACCES` when opening the socket, proving an unrelated local identity cannot reach authenticated local control before protocol parsing.
3. The probe account was deleted immediately and its ten-minute root cleanup failsafe canceled. Final audit found no temporary users, timers, runtime masks, firewall rules, credentials, or test processes. No secret entered argv, journal evidence, URLs, or public status.
4. Firewalld remains the unchanged active+enabled FedoraWorkstation policy that admits the derived high Pond port; because the installed artifact is byte-identical to the earlier green physical Pond/firewalld gate, that destructive cross-host gate was not repeated.

## 2026-09-02 01:53 EDT — PH-3 hostile-input and resource-bound physical gate

commit: source under test `27b3d03`; installed daemon `/usr/local/bin/koi`, SHA-256 `d50ebc19e3f32b722dac0cebb4ab233665044d2a7892e743ee297ffa3df93fc9`, PID `19596`; installed desktop RPM `koi-0.1.2-1.x86_64`, SHA-256 `89c74984af61f35d5a5d06905547963c67936364356b5f084c3d15d41ebd7ca2`, PID `2975` | gates: fmt; focused strict clippy; locked `koi-dns`/`koi-mdns`/`koi-serve` tests and doctests; physical malformed DNS/mDNS/HTTP and denied-route load PASS
koi state now: exactly one enabled system daemon, unchanged PID `19596` and restart count `1`; exactly one unchanged desktop PID `2975`; operator HTTP is loopback-only on 5641; Pond desire is restored disabled and 5644 is closed; mDNS generation remains `3` with Avahi owning every route and desired=established=1, pending=failed=0; cooperative DNS remains healthy on `127.0.0.1:53` and `192.168.1.95:53`.
peers/run: `20260902T055318Z-19596` PASS; independent physical peer test-01 `192.168.1.109`, installed Koi PID `542996`, SHA-256 `8e3b94a9cfcaaa66f8c751bbb10e59f5b2196a2057d848c0ff8020d9395e24c3`
restoration: initial Pond desire was false; it is false again and the listener closed on the first poll. The root-owned ten-minute cleanup timer and helper, SSH control socket/session, peer traffic process, and all test state were removed. Firewalld, Avahi, resolved, NetworkManager, login, rpm-ostree, installed artifacts, DNS records, provider routes, and process identities were unchanged.
findings:

1. Test-01 sent a bounded corpus of 500 malformed/truncated UDP DNS datagrams, 100 truncated or length-mismatched TCP DNS sessions, 500 malformed multicast DNS datagrams, and 200 malformed/truncated raw HTTP sessions. It also required 100 excluded Pond requests across DNS mutation, certmesh log, Pond control, OpenAPI, and MCP to return 404, and proved 20 attempts to reach operator port 5641 from the LAN were refused.
2. Before load, Bluefin measured RSS 40,984 KiB, 17 descriptors, 8 threads, systemd memory 15,626,240 bytes, and 8 tasks. Across forty half-second samples, RSS peaked at 41,048 KiB (+64 KiB), descriptors transiently peaked at 19 then returned to 17, threads stayed 8, and tasks transiently peaked at 9 then returned to 8. After a 30-second settle, RSS was 41,088 KiB (+104 KiB); a later final sample was 40,020 KiB, below baseline.
3. The daemon never crashed or restarted, provider generation/routes/publication counts did not change, no resource or privilege widened, and no manual re-arming was needed. Logs contained no panic, fatal error, segmentation fault, out-of-memory, descriptor exhaustion, provider failure, route failure, token, or secret-bearing entry.
4. No access token or supplied machine credential entered argv, traffic, evidence, URLs, the journal, or public status. A cleanup invocation initially ran the root-owned helper as the unprivileged operator and was correctly denied; direct authenticated local control immediately restored disabled Pond before the session continued, and final cleanup found no residue.

## 2026-09-02 19:08 EDT — PH-0 advertised-contract reconciliation

commit: documentation reconciliation based on `a296f42`; installed daemon `/usr/local/bin/koi`, SHA-256 `d50ebc19e3f32b722dac0cebb4ab233665044d2a7892e743ee297ffa3df93fc9`; installed desktop RPM `koi-0.1.2-1.x86_64`, SHA-256 `89c74984af61f35d5a5d06905547963c67936364356b5f084c3d15d41ebd7ca2` | gates: PH-0 public-surface and installed-evidence audit
koi state now: exactly one active system daemon, unchanged PID `19596` and restart count `1`; exactly one unchanged `/usr/bin/koi-desktop --minimized`, PID `2975`; operator HTTP remains loopback-only, Pond remains disabled, Avahi owns mDNS routes, and firewalld retains the FedoraWorkstation policy measured by the prior physical gates.
peers/run: none — read-only local PH-0 reconciliation; no peer assertion or machine mutation
restoration: no product, service, provider, network, firewall, login, or rpm-ostree mutation was performed. An unrelated automatic Bluefin base-OS deployment is staged; it was not activated because this audit supplies no reason to repeat a lifecycle reboot.
findings:

1. The public surface ledger omitted Bluefin from platform supervision even though this journal already proves the immutable composition: toolbox-only build dependencies, transactional `/usr/local/bin/koi` systemd lifecycle, rpm-ostree desktop install/upgrade/rollback/activation, and singleton GNOME autostart/SNI. The ledger now records that evidence and its journal guard.
2. The README described Linux service integration as systemd-only despite the shipped OpenRC recipe. Its platform matrix now advertises both systemd and OpenRC `supervise-daemon`; the service guide's stale “all three” count now describes every supported recipe without inventing a platform count.
3. Workbench, provider, firewall, local-control, Pond, and lifecycle claims otherwise match the installed Bluefin evidence. The ownership-aware Linux installer correction remains a shared CachyOS-owned blocker, so no new rpm-ostree/systemd upgrade proof or PH-4 soak is claimed here.

## 2026-09-02 23:20 EDT — PH-1 ownership-aware installer pre-reboot checkpoint

commit: source `48f8e3e44569a49e20c4349793b101126e7f9536`; accepted release/installed daemon SHA-256 `d5faa755a15b27985992e296d9c8ffc0ddfaec1ab3668b19961cfe9dff7c047f` | gates: fmt; locked all-target/all-feature strict clippy; locked all-target/all-feature workspace tests; release build; real installed upgrade; forced failed-candidate rollback; fresh foreign-listener planning
koi state now: exactly one enabled system `koi.service`, PID `71224`, `NRestarts=0`, `/usr/local/bin/koi --daemon`; standard `5641:5644`; `/etc/koi/config.toml` absent; exactly one unchanged `/usr/bin/koi-desktop --minimized`, PID `2975`, from `koi-0.1.2-1.x86_64`.
peers/run: none — host-local serial PH-1 installer acceptance
restoration: the non-Koi incumbent, fault fixture/drop-in, shifted test deployment, transaction files, and test-created config are absent. The accepted standard-port candidate is active and matches the release artifact. Root rollback snapshot `/var/tmp/koi-bluefin-prefreeze` remains until post-reboot verification.
findings:

1. Upgrading the standing legacy no-config service recognized product ownership, retained `5641:5644`, created no config, and changed the sole daemon from PID `19596` to the accepted PID lineage without overlapping Koi processes.
2. A distinct executable candidate (SHA-256 `2e9a708597ea278732ae99124becb49cc1341c424fb0916f881562912d885a5c`) was made unhealthy only after its transaction armed: systemd started it on 6541 while verification expected 5641. Install returned 1 and restored the exact accepted binary, unit, local-operator policy, config absence, active/enabled state, and standard endpoint; no manifest, backup, staging file, or fault drop-in remains.
3. With the Koi unit serially uninstalled, a real Python process owned TCP `5641:5644`. A genuinely fresh install selected `5651:5654`, persisted that decision, and ran beside the incumbent. The shifted service was then uninstalled, the incumbent stopped, its test-created config removed, and the accepted candidate reinstalled on the original standard run.
4. Bluefin has an automatic base-image deployment `44.20260901` staged with the same native desktop RPM. The next continuation must verify activation after one reboot and must not repeat the installer, rollback, or foreign-listener gates recorded above.

## 2026-09-03 00:04 EDT — PH-1 immutable reboot activation complete

commit: accepted daemon source `48f8e3e44569a49e20c4349793b101126e7f9536`, synchronized evidence tree `40e21fa`; installed daemon SHA-256 `d5faa755a15b27985992e296d9c8ffc0ddfaec1ab3668b19961cfe9dff7c047f`; installed desktop RPM `koi-0.1.2-1.x86_64`, SHA-256 `89c74984af61f35d5a5d06905547963c67936364356b5f084c3d15d41ebd7ca2` | gates: real rpm-ostree deployment activation; systemd readiness; GNOME XDG autostart; live SNI registration; singleton poke; CLI/HTTP health
koi state now: boot ID `099e4f90-08ea-4ead-9f4a-c318a90279e7`; Bluefin `44.20260901`; exactly one enabled `/usr/local/bin/koi --daemon`, PID `2332`, `NRestarts=0`; exactly one `/usr/bin/koi-desktop --minimized`, PID `3347`; standard 5641 operator HTTP is loopback-only; Pond is disabled; Avahi owns publish/browse/resolve; cooperative DNS owns loopback and `192.168.1.95`.
peers/run: none — local post-reboot PH-1 activation; no peer or provider mutation
restoration: the intended staged deployment is booted and no further deployment is staged. No installer fixture, foreign listener, fault drop-in, shifted config, transaction file, or test process exists. The retained root rollback snapshot was removed only after these checks passed.
findings:

1. The reboot activated rpm-ostree deployment `36a5bab224c2f92f137d5c048acfe575aa9adefd46974e6e27b410b312dbf00d` while retaining the native Koi RPM. Systemd started the accepted product-owned daemon from `/usr/local/bin/koi`; its hash, unit hash, standard-port decision, config absence, enablement, and operator-private socket survived exactly.
2. The real GNOME/Wayland login started one package-owned workbench from XDG autostart. GNOME Shell reported one live item, `:1.53@/org/ayatana/NotificationItem/tray_icon_tray_app_koi`, and a registered StatusNotifier host. `--poke` acknowledged the resident process without creating a duplicate.
3. `/healthz` returned `OK`; status reported the expected Avahi routes, one established publication, trusted local control, disabled Pond, and cooperative DNS. The boot journal contains no Koi error; its only warning is the truthful absence of optional Docker/Podman.
4. This closes Bluefin PH-1. The accepted deployment is now held unchanged for PH-4 after CachyOS records the frozen source revision; none of Bluefin's lifecycle or environmental gates should be repeated before that dispatch.

## 2026-09-03 00:57 EDT — PH-4 frozen immutable-workstation slice

commit: exact frozen source `f53d568a871e952576988e543126160bfd41aaaa`; release/installed daemon SHA-256 `bc1a78878207ca9df3ba0b45992b87bcbbb504e2d3bee0a720bda06338dd5dfd`; unchanged desktop source `4c05ed2`, RPM `koi-0.1.2-1.x86_64`, installed SHA-256 `89c74984af61f35d5a5d06905547963c67936364356b5f084c3d15d41ebd7ca2` | gates: frozen clean detached-worktree fmt; locked all-target/all-feature strict clippy; locked all-target/all-feature workspace tests; release build; installed Find → Name → Trust → Serve/Pond slice
koi state now: exactly one enabled `/usr/local/bin/koi --daemon`, PID `44458`, `NRestarts=0`, on standard loopback operator HTTP 5641; `/etc/koi/config.toml` absent; Avahi owns publish/browse/resolve with one permanent desired/established publication; DNS has zero static/certmesh entries; health and proxy have zero configured services/listeners; Pond desire is false and 5644 is closed. Exactly one `/usr/bin/koi-desktop --minimized`, PID `3347`, and one GNOME StatusNotifier item remain on booted Bluefin `44.20260901` rpm-ostree deployment `36a5bab224c2f92f137d5c048acfe575aa9adefd46974e6e27b410b312dbf00d`.
peers/run: Pond `20260903T045638Z-44327` PASS; independent reader CachyOS test-01 `192.168.1.109`; run-owned discovery/DNS/health identity `ph4-bluefin-20260903T045518Z`
restoration: the run-owned DNS entry and health check are removed; the mDNS session closed and the peer's later resolve timed out; Pond returned to its initial disabled desire and 5644 closed. No peer mutation, alternate Koi, config, firewall rule, credential helper, or test process remains. The detached build worktree was clean and removed after the installed hash was verified.
findings:

1. The frozen candidate was built inside the Fedora 44 `koi-dev` toolbox from a detached worktree whose HEAD was exactly the frozen SHA. Public systemd install replaced the sole service serially, retained the no-config `5641:5644` decision and operator UID 1000, and changed PID `2332→43972` with the installed bytes matching the release artifact.
2. The standing service published a run-owned `_koi-ph4._tcp` service with explicit `192.168.1.95`, port 5641, and `phase=ph4 os=bluefin`. Both Bluefin and CachyOS resolved it through Avahi; after the owning CLI session closed, CachyOS received a bounded resolve timeout. A run-owned static DNS record resolved locally and was absent after removal. A five-second HTTP health check reached Up and was removed. Current trust diagnosis is HEALTHY/Open; proxy truthfully has no listener because this open node has no certificate identity.
3. Restoration-gated Pond run `20260903T045638Z-44327` used the one installed service and real UI bundle. CachyOS loaded every allowlisted read, rejected excluded/mutation routes, observed explicit stop and restart recovery, and the service alone changed PID `43972→44458`; artifact hash and disabled desire were unchanged. An initial harness invocation correctly refused before mutation while the long-lived mDNS CLI made two Koi processes visible; the publication was closed before the accepted run.
4. Authenticated local control remained owner-private and supplied the workbench/Pond paths without exposing the operator API to the LAN. `koi-desktop --poke` acknowledged PID `3347` without duplication, and GNOME retained exactly `:1.53@/org/ayatana/NotificationItem/tray_icon_tray_app_koi`. Bluefin is artifact-ready and must remain unchanged until the CachyOS driver names the shared provider run.

## 2026-09-03 08:37 EDT — PH-4 re-frozen immutable-workstation slice

commit: exact re-frozen source `5c89e9de11bf23ab81fd8b5b0778c58477359360`; release/installed daemon SHA-256 `288362b8ae909d655f683f290f2b34bd5d8cb802a1184a0cebf27a1b3219e200`; unchanged desktop source `4c05ed2`, RPM `koi-0.1.2-1.x86_64`, installed SHA-256 `89c74984af61f35d5a5d06905547963c67936364356b5f084c3d15d41ebd7ca2` | gates: clean detached-worktree fmt; locked all-target/all-feature strict clippy; locked all-target/all-feature workspace tests; release build; installed Find → Name → Trust → Serve/Pond slice
koi state now: exactly one enabled `/usr/local/bin/koi --daemon`, PID `80259`, `NRestarts=0`, on standard loopback operator HTTP 5641; `/etc/koi/config.toml` absent; Avahi owns publish/browse/resolve with one permanent desired/established publication; DNS and health contain no run-owned entries; proxy has no listeners; Pond desire is false and 5644 is closed. Exactly one unchanged `/usr/bin/koi-desktop --minimized`, PID `3347`, and its one GNOME SNI item remain.
peers/run: coordination `ph4-5c89-bluefin-local-02`; Pond `20260903T123627Z-80124` PASS; independent reader CachyOS test-01 `192.168.1.109`
restoration: the run-owned DNS entry and health check are removed; the mDNS owning session closed and the peer no longer resolved it; Pond returned to its initial disabled desire and 5644 closed. No peer mutation, alternate Koi, config, firewall rule, credential helper, or test process remains. The clean detached build worktree was removed after installed-hash verification.
findings:

1. The superseding candidate was built inside `koi-dev` from detached HEAD exactly `5c89e9de11bf23ab81fd8b5b0778c58477359360`. Public systemd install replaced only the standing service, retained its no-config standard-port plan and operator UID 1000, and changed PID `44458→79985`; release and installed hashes matched.
2. The installed service published `ph4-5c89-bluefin-local-02._koi-ph4._tcp.local` with explicit address `192.168.1.95`, port 5641, and `source=5c89 os=bluefin`; both Bluefin and CachyOS resolved it through Avahi before withdrawal. Its static DNS entry resolved locally and was absent after removal. Its five-second HTTP health check reached Up and was removed. Trust remained HEALTHY/Open; certificate-backed proxy remained explicitly unavailable with no listener because this host has no identity to invent.
3. Restoration-gated Pond run `20260903T123627Z-80124` loaded every allowlisted route from CachyOS, rejected excluded and mutation routes, exercised explicit stop and service reconstruction, and restored false desire before PASS. The sole daemon changed `79985→80259`; its exact artifact and standard run were unchanged.
4. Authenticated local control and GNOME integration remained healthy. `koi-desktop --poke` acknowledged the unchanged package-owned PID `3347` without duplication. This replaces the invalidated `f53d568` evidence and leaves Bluefin ready, unchanged, for the CachyOS-coordinated provider matrix; PH-5 has not begun.

## 2026-09-03 10:38 EDT — PH-4 exact-source replacement accepted

commit: exact frozen source `e49bfe2b3e403fa87d4b8b237b49f3bb9e5cb5ef`; release/installed daemon `/usr/local/bin/koi`, SHA-256 `288362b8ae909d655f683f290f2b34bd5d8cb802a1184a0cebf27a1b3219e200`, 54,101,048 bytes; unchanged desktop source `4c05ed2`, RPM `koi-0.1.2-1.x86_64`, installed SHA-256 `89c74984af61f35d5a5d06905547963c67936364356b5f084c3d15d41ebd7ca2` | gates: clean detached-worktree fmt; locked all-target/all-feature strict clippy; locked all-target/all-feature workspace tests; release `koi-net` build; serial public systemd install; post-retry stability
koi state now: exactly one enabled `/usr/local/bin/koi --daemon`, PID `114286`, `NRestarts=0`, with unit SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`; standard operator HTTP is loopback-only on 5641 and 5644 is closed; `/etc/koi/config.toml` and the install transaction manifest are absent; local-access policy SHA-256 remains `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, root-owned mode 0600; `/var/run/koi.sock` remains `test:root` mode 0600. Avahi owns publish/browse/resolve with one alive permanent registration; cooperative DNS has zero static/certmesh entries; health and proxy have zero configured services/listeners; Pond remains disabled. Exactly one package-owned `/usr/bin/koi-desktop --minimized`, unchanged PID `3347`, remains in the GNOME session; its installed desktop entry is `/usr/share/applications/Koi.desktop`, SHA-256 `66cd3e230b1ef429cf7706ad264e4274cb425606fd544d83b8f89fd76c78d14a`.
peers/run: none — exact-source replacement only; the dispatch explicitly retained Bluefin's earlier green local whole-story, lifecycle, desktop, provider, and Pond evidence
restoration: the public installer replaced the sole service serially, changing PID `80259→114286`, and retained the exact standard ports, operator, policy, no-config plan, enablement, and healthy state. No staging file, transaction manifest, alternate Koi, credential helper, config, test process, or detached build worktree remains.
findings:

1. The candidate was built inside the Fedora 44 `koi-dev` toolbox from a clean detached worktree whose HEAD was exactly `e49bfe2b3e403fa87d4b8b237b49f3bb9e5cb5ef`. Every required native gate passed before installation, and the installed bytes match the release artifact.
2. The Linux artifact is byte-identical to the superseded `5c89e9de11bf23ab81fd8b5b0778c58477359360` build because the intervening correction is Windows-only. Bluefin nevertheless rebuilt and installed the exact frozen source rather than carrying the old provenance forward by inference.
3. The installed service remained active through the post-install retry interval with zero restarts, one daemon, one unchanged desktop, no Koi error journal entries, localhost-only operator access, healthy Avahi/cooperative DNS, and disabled Pond. Per dispatch, the unchanged destructive and cross-host gates were not repeated.
4. This replacement invalidates only the prior source label, not the earlier physical Linux results. Bluefin remains an unchanged PH-4 peer; PH-5 has not begun.

## 2026-09-03 13:15 EDT — PH-5 installed entry and diagnostics accepted

commit: fleet evidence based on `ea9d4a6`; installed daemon remains exact frozen source `e49bfe2b3e403fa87d4b8b237b49f3bb9e5cb5ef`; installed workbench source/release `2503ab2` (`koi-desktop`), native RPM `koi-0.1.3-1.x86_64`, RPM SHA-256 `cafe511e42face8a9fffa729b5b9cea81f26340ef7a8f531b92d02652a6964dd`, installed executable SHA-256 `7eebdf1a435f688c9b0b7c65f74635a4cbd869830d5d688f14102cec82b30a39` | run: `ph5-bluefin-entry-01` | verdict: **PASS — neutral installed journey complete; systemd observer canary waits for Debian's shared collector contract**
koi state now: exactly one enabled system daemon, unchanged PID `114286`, `NRestarts=0`, `/usr/local/bin/koi` SHA-256 `288362b8ae909d655f683f290f2b34bd5d8cb802a1184a0cebf27a1b3219e200`; standard operator HTTP remains loopback-only on 5641; Avahi owns publish/browse/resolve with one alive permanent registration; cooperative DNS has zero static/certmesh entries; health/proxy have no configured work; runtime is disabled; Pond desire is false and 5644 is closed. Exactly one package-owned `/usr/bin/koi-desktop --minimized`, PID `122952`, `NRestarts=0`, runs through generated `app-Koi@autostart.service`; GNOME reports exactly `:1.171@/org/ayatana/NotificationItem/tray_icon_tray_app_koi`.
gates: desktop source fmt; 38/38 browser-bundle tests; locked Rust suite 20 passed/1 environment gate ignored; locked strict all-target clippy; optimized Tauri/RPM build inside `koi-dev`; rpm metadata/payload ownership; rpm-ostree dry-run and atomic local-package replacement; live installed normal-entry reveal; singleton process/SNI; post-retry service/workbench stability — pass
restoration: rpm-ostree staged deployment `default-9561a4619009a8cdb24f9a235f0c857fc8fdafa103055a29035cceda625e74a8.0` owns `koi-0.1.3-1.x86_64`, and its exact package diff is live-applied to the running filesystem without reboot. The former 0.1.2 deployment remains the automatic rollback. The run-owned transient desktop service was stopped and collected; the product's generated XDG-autostart unit owns the sole final workbench. No duplicate UI, alternate Koi, endpoint/token/root override, config, credential helper, test listener, or host build-package layer remains.
findings:

1. From neutral `/var/home/test` with no `KOI_*` variables, the old installed 0.1.2 desktop entry reproduced a real onboarding defect: `gtk-launch Koi` created duplicate PID `116640` beside resident PID `3347`. The duplicate was terminated immediately and the accepted one-workbench baseline restored before source work.
2. Synchronized `koi-desktop` already contained the shared atomic listener claim and ordinary-launch reveal corrections (`48f45a3` through fragmented-request guard `905b0cd`). Bluefin retained that implementation rather than creating a parallel singleton path, versioned the complete source as 0.1.3 at `2503ab2`, ran its full native gates, and published that commit directly to `koi-desktop/main`.
3. rpm-ostree's first dry run truthfully rejected two simultaneously requested local NEVRAs. The supported atomic transaction removed the 0.1.2 request while adding 0.1.3; `apply-live --allow-replacement` then updated `/usr` without restarting the machine. Restarting only the workbench through its generated XDG unit changed desktop PID `3347→122952`; daemon PID/hash/restart count, providers, ports, Pond desire, and host build-package inventory did not change.
4. A normal package entry now reports `Koi is already running; revealed it`, keeps PID `122952`, and leaves one SNI item. The installed CLI's first status result finds the daemon through authenticated local control without endpoint or token input, names Avahi routing and disabled Pond, and reports the actionable Open-node trust remedy: `koi certmesh join <endpoint>` or `koi certmesh create`. Config discovery independently returns the active daemon location `/etc/koi/config.toml` while truthfully reporting that no file exists.
5. This completes Bluefin's PH-5 installed-entry/diagnostics slice. Debian's dispatched neutral collector evolution is not yet on `origin/dev`; per contract, no Bluefin-specific collector or premature soak was created. The next work is the short systemd observer canary only after that shared seam lands.

## 2026-09-04 09:06 EDT — OD-2 pre-rebase systemd/provider/Pond checkpoint (artifact superseded; collector pending)

commit: physical candidate assembled from synchronized `baab1c2` plus this entry's alias-feedback and harness corrections; OD-2 product baseline `2f967e4`; release/installed `/usr/local/bin/koi` SHA-256 `aaf5468e9974300c72f662eac0dd60525468d174938009300da65deffd83d410`, 62,513,008 bytes | gates: Fedora 44 toolbox fmt; locked strict all-target clippy; complete locked workspace tests/doctests; release `koi-net` and `koi-lab` builds; serial public systemd install; real Avahi/resolve1/native transition; real two-host Pond; Unix socket denial; post-retry alias-feedback observation
koi state now: exactly one active+enabled system `koi.service`, PID `195866`, `NRestarts=0`, serving operator HTTP only on `127.0.0.1:5641`; `/run/koi.sock` inode `10348`, `test:root` mode 0600, and `/run/koi.endpoint` inode `10349`, `root:root` mode 0600. HostIdentity remains `bluefin` / `bluefin.local` / Linux x86_64 / `enp0s31f6=192.168.1.95`. Avahi owns all four mDNS routes with one desired/established permanent publication and no pending/failure; DNS owns `127.0.0.1:53` and `192.168.1.95:53`; Pond desire is false and 5644 is closed. Exactly one unchanged package-owned `/usr/bin/koi-desktop --minimized`, PID `122952`, SHA-256 `7eebdf1a435f688c9b0b7c65f74635a4cbd869830d5d688f14102cec82b30a39`, retains the sole GNOME StatusNotifier item `:1.171@/org/ayatana/NotificationItem/tray_icon_tray_app_koi`.
peers/run: provider `20260904T130231Z-193383` **PASS**, CachyOS test-01 `192.168.1.109`, unchanged enabled system PID `1197576`, SHA-256 `0cfb11caedc8b69528d24e83fa14158c7219dd1d924ca851ef062ce083dd2458`; Pond `20260904T130505Z-195581` **PASS**, independent reader test-01; systemd collector **PENDING** because the same peer's UFW-owned Pond admission is blocked
restoration: provider run returned Avahi service/socket, resolved service/monitor/varlink sockets, their enablement, and global/link `MulticastDNS=no` exactly; no runtime mask/drop-in remains. Pond returned from the captured false desire to false, closed 5644, and changed only the planned installed-service PID `191024→195866`; firewalld required no mutation. Test-01's run-owned publications are gone, Koi PID/hash/provider facts are unchanged, and its temporarily armed Pond was returned to disabled after UFW refused TCP 5644. Bluefin's `koi-0.1.3-1.x86_64` live/staged rpm-ostree state, desktop process, package bytes, host network, and firewall are unchanged; no reboot occurred. The verified 52 MiB rollback archive, local/peer credential helpers, pinned-host scratch file, and preflight captures were deleted only after final health and restoration passed; the peer-local sudo helper is also absent.
findings:

1. The provider harness initially rejected the real peer because its noninteractive SSH `ps` invocation inspected only the session. `ps -e` now attests the host-wide process table. With that portability fix, the exact installed candidate held PID `191024` and hash `aaf5468e…d410` across generations `28→55`: Avahi `32`, resolved/native `37`, native-only `43`, resolved restored `47`, Avahi restored `53`, and cleanup `55`. Every exercised phase held `3/3/0/0` publications, both Koi APIs proved bidirectional ordinary/explicit publish, TXT, resolve, subscription continuity, withdrawal, and late resolve1-conflict recovery, then both machines returned to `1/1/0/0`. One prior corrected-byte attempt (`20260904T125959Z-191476`) restored cleanly after a single peer unregister HTTP timeout; the complete rerun passed.
2. Pond run `20260904T130505Z-195581` served and retained immutable generations `sha256:f2eb671393903d96d5a24501fe8a16a0bf3117581c362bce8f0b1a3412df2432` and `sha256:a60e0ecfdd84210ceca998d387371c525895aad64153546542b90ac0bd8c5182`. Test-01 loaded the allowlisted status/health/inventory/assets, rejected excluded and mutation routes, observed stop and restart reconstruction, and the gate recorded exact disabled-state restoration. Fedora's existing firewalld policy admitted the listener without a temporary rule.
3. The new aggregate inventory and direct domain surfaces survived both service generations without reconstructing state in presentations. The final coherent capture was aggregate revision 319 with Koi `1.0.0-dev.0`, daemon true, Avahi generation 6 with `1/1/0/0`, 25 discovered records/13 types/no unavailable browse, and DNS revision 23 with 49 mDNS, zero static, and zero certmesh entries. The reconstructed local-control inode changed as expected while owner/mode and HostIdentity stayed fixed; an unrelated `nobody` process was denied at socket connect with `EACCES` before protocol parsing.
4. Physical logs exposed a shared composition defect: every ordinary LAN mDNS host outside Certmesh's roster became an endlessly retryable DNS alias-feedback command. The bridge now acknowledges typed `CertmeshError::NotFound` as a terminal not-applicable hint while preserving retries for every other failure. Focused tests prove both sides. On the corrected installed bytes, a real `http.internal` mDNS lookup queued the 27-record LAN view and crossed the 60-second worker interval with zero alias-feedback warning; the only service warning remains the truthful absence of Docker/Podman.
5. Bluefin cannot honestly start the neutral systemd collector yet. Its required `--peer-surface` must be a physical peer's live Pond URL. Test-01 returns `desired=true`, `running=true`, `state=waiting`, and `firewall.state=blocked` because UFW's default incoming policy does not admit TCP 5644; Bluefin's direct preflight timed out, then test-01 was restored to `desired=false`, `state=disabled`. Fleet ownership forbids changing another hat's firewall or substituting loopback, a tunnel, proxy, alternate service, or local self-Pond. Therefore this entry does not close Bluefin's OD-2 row or update the epic; the remaining dependency is a peer-hat-owned reachable Pond window followed by the real short `installed-service-collect --observer systemd` canary.
6. Final publication rebased these corrections over Windows SCM/data-root commits `c515100` and `6bb836f`. The resulting Linux artifact was not byte-identical, so this checkpoint is retained as historical evidence only and cannot close the current row; the exact published-tree rerun follows in a separate journal entry.

## 2026-09-04 09:24 EDT — OD-2 published-tree provider/Pond acceptance (collector pending)

commit: exact published product source `ab84782bec70400603088172901976f75cadd010`, including synchronized durability/OpenRC baseline `04e7b27`; release/installed `/usr/local/bin/koi` SHA-256 `298fb7208d7d4eb2f8da4f920e6a608efc4b2e61d88313fff712c7b2588f6750`, 62,508,256 bytes; release `koi-lab` SHA-256 `2b19baca117b392ade701e590bc287cd54e5e6a5e63642c88986b92ed3c4c2bd`, 14,299,656 bytes | gates: clean published-tree fmt; locked strict all-target clippy; focused alias-feedback regression; optimized `koi-net`/`koi-lab`; serial public systemd install; exact-byte provider and Pond reruns
koi state now: exactly one active+enabled system `koi.service`, PID `207791`, `NRestarts=0`; release and `/usr/local/bin/koi` hashes match. Operator HTTP remains loopback-only on 5641; `/run/koi.sock` inode `11493` is `test:root` mode 0600 and endpoint inode `11494` is `root:root` mode 0600. HostIdentity is unchanged. Avahi owns all routes with one desired/established publication; Pond is disabled and 5644 closed. Desktop PID `122952`, package/RPM bytes, and its sole GNOME SNI item remain unchanged.
peers/run: provider `20260904T132001Z-205263` **PASS**, unchanged CachyOS test-01 PID `1197576`, SHA-256 `0cfb11caedc8b69528d24e83fa14158c7219dd1d924ca851ef062ce083dd2458`; Pond `20260904T132300Z-207507` **PASS**, independent test-01 reader; collector peer preflight: test-01, Alpine, Debian, and Windows Pond surfaces all closed
restoration: provider generations `6→31` returned Avahi service/socket, resolved service/activation sockets, enablement, global/link mDNS policy, publications, and both installed Koi identities exactly. Pond's planned reconstruction changed only Bluefin Koi PID `205221→207791`, restored its captured false desire and prior UI selection, and needed no firewalld mutation. Test-01 stayed on its captured PID/hash/providers and disabled Pond. No reboot, peer service/provider/firewall mutation, rpm-ostree/package change, alternate Koi, run-owned publication, runtime mask/drop-in, or test listener remains.
findings:

1. The exact published artifact repeated every physical provider assertion: Avahi generation 10, resolved/native 14, native-only 20, resolved restored 24, Avahi restored 29, and cleanup 31. Each active phase converged to `3/3/0/0`, ordinary and explicit publications resolved bidirectionally through real Koi APIs, the subscription survived every generation, the late resolve1 conflict recovered, and withdrawal returned both machines to `1/1/0/0` without restarting either installed service.
2. Pond served immutable generations `sha256:facba72ad2c1550f59431c786a58c0a0ff433cac6f289d6178a0f1c9a0b287b3` and `sha256:a60e0ecfdd84210ceca998d387371c525895aad64153546542b90ac0bd8c5182`. The independent peer accepted every allowlisted read, rejected excluded/mutation routes, retained the first generation after the second publication, and repeated both reads after installed-service reconstruction before exact disabled-state restoration.
3. The required neutral collector remains dependency-blocked rather than failed: no catalogued independent fleet host currently exposes a Pond surface, and test-01's explicit surface remains peer-UFW-blocked as recorded above. Bluefin therefore remains one short systemd `installed-service-collect` canary away from closing OD-2. This documentation-only checkpoint does not change the tested product tree or artifact.
4. Final aggregate revision 122 reported daemon true, Avahi generation 6 with `1/1/0/0`, 25 records/13 service types/no unavailable browse, and DNS revision 19 with 49 mDNS entries and no static/certmesh entries. A real `http.internal` lookup crossed 79 seconds on final PID `207791` without an alias-feedback warning, the unrelated-UID socket probe again failed with `EACCES`, and every final service/provider enablement check passed.

## 2026-09-04 11:04 EDT — OD-2 systemd observer accepted; Bluefin complete

commit: collector evidence source `242022dd2c9b8aaab9c3ec349e5af66ad341aafe`; installed product remains exact published tree `ab84782bec70400603088172901976f75cadd010`, `/usr/local/bin/koi` SHA-256 `298fb7208d7d4eb2f8da4f920e6a608efc4b2e61d88313fff712c7b2588f6750`, 62,508,256 bytes; current-tree release `koi-lab` remained byte-identical at SHA-256 `2b19baca117b392ade701e590bc287cd54e5e6a5e63642c88986b92ed3c4c2bd` | collector run: `v1-20260904T1503Z-bluefin-od2` | peer: Alpine test-03 `192.168.1.221` | verdict: **PASS — Bluefin OD-2 complete**
koi state now: exactly one active+enabled system `koi.service`, PID `207791`, `NRestarts=0`, still running the exact artifact above. Operator HTTP remains loopback-only on 5641, Bluefin Pond desire remains false and 5644 is closed. The collector's final sample reported daemon healthy, Avahi generation 21 with concrete publish/explicit-publish/browse/resolve routes, one desired/established publication and none pending/failed. No local service, package, provider, firewall, network, desktop, or session state changed.
gates: current-tree optimized neutral collector build; privileged native systemd identity and `/proc` resource observation; 30-second authenticated aggregate collection; seven physical Alpine Pond reads; exact local/peer restoration — pass
restoration: Alpine's authenticated Koi control changed only its run-owned Pond desire `false→true→false`, status revision/generation `0/0→3/1→5/2`, and listener state. Port 5654 was reachable only while armed and is again unreachable from Bluefin. Alpine remains OpenRC-started with the same daemon PID `31872`, supervisor PID `31871`, `/usr/bin/koi` SHA-256 `a713968fe5b84d5894588f8eb6be2b37b8a99f8bfb42d4d20c586e1226d8a5e6`, unchanged disabled desire, and no active UFW/firewalld. Bluefin retained PID/hash/restart count and its own disabled Pond state. No peer service/provider/firewall/package mutation, alternate Koi, tunnel, proxy, test listener, or credential helper remains.
findings:

1. The accepted schema-3 evidence is retained under `.lab-runs/v1-20260904T1503Z-bluefin-od2/`. Seven samples over 30.325 seconds advanced aggregate revision `6415→6445`; all fourteen verdict checks passed with 7/7 peer reads, zero retries, zero unavailable or consecutive-unavailable samples, zero PID/restart/recovery transitions, and stable converged `1/1/0/0` publications.
2. Resource observations remained bounded: RSS grew 4,096 bytes, while descriptor, thread, and systemd task counts had zero growth. The initial and final systemd identities both bound `koi.service` to PID `207791`, `/usr/local/bin/koi --daemon`, exact hash `298fb7…f6750`, and restart count zero.
3. The current `e64b50e` OD-2 baseline differs from Bluefin's accepted product tree only in the OpenRC installer, exactly as the epic's invalidation rule records. The neutral collector rebuilt from synchronized head to the same previously validated bytes; no systemd product replacement or repeated physical provider/Pond gate was warranted.
4. A first unprivileged collector invocation was rejected before sampling because `/proc/<pid>/exe` attestation required sudo. The accepted run used the native privileged observation path; only its JSON/JUnit/text evidence exists. With this final dependency satisfied, Bluefin's OD-2 row is closed and the fleet remains at 4/5 pending Debian.

## 2026-09-04 12:41 EDT — OD-3 frozen-candidate systemd readiness

commit: exact frozen source `b3eb47e08817045f9371703d780ada9aab00995d`; release/installed `/usr/local/bin/koi` SHA-256 `ddf603d78627e36f741ea5589ce76182a88a6137fa5b1f4aaef054a367b1a6c3`, 64,846,304 bytes; exact-source release `koi-lab` SHA-256 `59ed9ec3b26ba6ac309f22b116bd5ee6ad352ad088606ccfcfc5ff1b67e7e871`, 14,303,608 bytes | campaign: `v1-20260904-od3-b3eb47e` | readiness collector: `v1-20260904-od3-b3eb47e-bluefin-ready` | peer: Alpine test-03 `192.168.1.221` | verdict: **READY — frozen Bluefin matrix slice complete**
koi state now: exactly one active+enabled root `/usr/local/bin/koi --daemon`, PID `258776`, `NRestarts=0`, running bytes identical to the detached frozen build. Operator HTTP remains loopback-only on 5641; cooperative DNS retains `127.0.0.1:53` and `192.168.1.95:53`; Avahi owns publish, explicit publish, browse, and resolve at generation 6 with one desired/established publication and none pending/failed. Pond desire is false and 5644 is closed. HostIdentity remains `bluefin`, `bluefin.local`, Linux x86_64, `enp0s31f6=192.168.1.95`. The unchanged RPM-owned workbench remains the sole `/usr/bin/koi-desktop --minimized`, PID `122952`.
gates: clean detached-source formatting; locked workspace/all-target/all-feature strict clippy; locked full workspace/all-target/all-feature tests and doctests; optimized `koi-net`/`koi-lab`; serial public systemd install; authenticated aggregate/HostIdentity/provider/publication readiness; 30-second native systemd collector against physical Alpine Pond — pass
restoration: the intended frozen-candidate replacement changed only the system daemon PID `207791→258776` and executable SHA-256 `298fb720…f6750→ddf603d7…a6c3`. The systemd unit remains SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`; config, local-access, DNS, health, Pond intent, and UI-selection hashes remain `46017703…09d3a`, `338c07cf…9893`, `498ca50a…d8aa`, `7204c446…02da`, `c5c8be6a…769`, and `e1e16c79…32fa`. Firewalld stayed running with the same active zone and file hashes `1ebeef14…5162a`, `d1c181dd…eb46`, `d3897598…ab57`, and `d6235a1e…0889`; rpm-ostree deployments and desktop PID were unchanged. The successful installer left no transaction, backup, or staging debris.
findings:

1. The exact frozen source was built only from a detached `b3eb47e` worktree under Fedora 44 with Rust/Cargo 1.98.0. The complete native suite and strict clippy passed before installation. A root-owned mode-0600 rollback archive, SHA-256 `f054416f750e276d873e411eb83b1b853d05b82060878344afc9b7c9d595c468`, protected the prior binary/unit/config/data state until final reconciliation, then was deleted with the detached worktree and credential material.
2. The product installer stopped the sole prior service, staged the exact candidate, retained the standard ports and operator UID 1000, enabled/restarted systemd, and returned only after `/healthz` was healthy. Initial and final collector identities both bound `koi.service` to PID `258776`, `/usr/local/bin/koi --daemon`, the exact frozen hash, active/running state, and restart count zero.
3. Collector evidence is retained under `.lab-runs/v1-20260904-od3-b3eb47e-bluefin-ready/` and names source commit `b3eb47e` directly. Seven samples over 30.343 seconds advanced aggregate revision `108→138`, held provider generation 6 and converged `1/1/0/0` publications, and completed 7/7 Alpine reads with zero retries, unavailable samples, PID/restart/recovery transitions, or descriptor/thread/task growth; RSS growth was 311,296 bytes.
4. Alpine's authenticated Koi control changed only its run-owned Pond desire `false→true→false`, revision/generation `5/2→8/3→10/4`, and port 5654 listener. It finished unreachable from Bluefin with the same OpenRC daemon/supervisor PIDs `31872`/`31871`, installed SHA-256 `a713968fe5b84d5894588f8eb6be2b37b8a99f8bfb42d4d20c586e1226d8a5e6`, and disabled Pond. No peer firewall, service, provider, package, or session mutation occurred.
5. Bluefin is ready at the OD-3 matrix barrier. It must not begin a private soak: after all five readiness publications, CachyOS will schedule the shared UTC `T0`; Bluefin's later role is only to arm and finally restore its Pond/firewall state, with no scheduled service fault.

## 2026-09-04 19:49 EDT — OD-3 coordinated Pond peer lifetime complete

task: inherited Epic 002 handover for Epic 003 R01 | frozen source `b3eb47e08817045f9371703d780ada9aab00995d`; synchronized collector evidence head `85d1ba3c081b7041d7642184c95def705f6f1d9d` | campaign `v1-20260904-od3-b3eb47e` | installed `/usr/local/bin/koi` SHA-256 `ddf603d78627e36f741ea5589ce76182a88a6137fa5b1f4aaef054a367b1a6c3`, 64,846,304 bytes | Bluefin peer verdict: **PASS — stable for every collector; exactly restored**
koi state now: exactly one enabled+active root `/usr/local/bin/koi --daemon`, unchanged PID `258776`, `NRestarts=0`, unit SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`. Operator HTTP is again the only Koi listener in the pair, on loopback `127.0.0.1:5641`; Pond revision/generation `5/2` is desired=false, running=false, disabled, and `192.168.1.95:5644` refuses connections. Persisted Pond intent is byte-identical to baseline at SHA-256 `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`. The unchanged RPM-owned workbench remains the sole `/usr/bin/koi-desktop --minimized`, PID `122952`.
peer lifetime: the product-owned Pond command armed the installed surface at the scheduled `2026-09-04T17:25:00Z` boundary and returned `http://192.168.1.95:5644/`, revision/generation `3/1`, with the native Pond status surface reachable before `T0`. A root-owned observer sampled `17:26:51Z..23:47:22Z`: all `380/380` checks found the URL reachable, the same PID/hash, active/running service and zero restarts; there were zero HTTP failures, unhealthy samples, or re-arm attempts. Pond stayed available until all three canonical journals were present, then `koi pond stop` restored false desire at `23:47:57Z`; a physical-LAN-address curl failed with connection refusal and `ss` showed no 5644 listener.
collectors: Windows `v1-20260904-od3-b3eb47e-win` completed 361/361 valid Bluefin reads with zero retries but **failed 12/14** because native DNS-SD false-type accumulation grew handles `+1329` and threads `+280`; its owner filed issue 004 at `85d1ba3`. Alpine `v1-20260904-od3-b3eb47e-alp` **passed 14/14** with 361/361 reads and exact OpenRC restoration. Debian `v1-20260904-od3-b3eb47e-deb` **passed 14/14** with 361/361 reads and exact systemd restoration. Bluefin does not reinterpret the red Windows verdict; CachyOS owns whole-run reconciliation and the Epic 003 R01 disposition.
restoration: firewalld stayed running on the original active `FedoraWorkstation` zone and `enp0s31f6`, with services `dhcpv6-client samba-client ssh`, ports `1025-65535/{udp,tcp}`, forwarding enabled, and no rich/forward/source rules. Persisted file hashes and original mode/owner/size/mtime all matched: workstation config `1ebeef14…5162a`, zone and `.old` `d1c181dd…eb46`, standard config `d3897598…ab57`, server config `d6235a1e…0889`. No firewall mutation was needed because the standing zone admitted 5644. The three rpm-ostree deployment checksums and staged/booted shape remained unchanged. The arm, observer, and emergency-stop transient units are absent; the mode-0600 baseline archive `6ecac18d…85fd`, guardian script, closed-port probe, and volatile askpass helper were deleted after verification. No service restart, provider/package/session/network change, alternate Koi, retained credential, or run-owned file remains.
findings:

1. Bluefin's inherited OD-3 responsibility is complete and exactly restored. Its stable physical peer served every one of the three collectors' 1,083 total samples without retry or failure while preserving the frozen service identity.
2. Epic 003 product implementation remains gated: R02 is not eligible until CachyOS reconciles the Windows resource failure and accepts or precisely redispatches R01. Bluefin must not begin a private fix, rerun, or deployment under that pending handover.

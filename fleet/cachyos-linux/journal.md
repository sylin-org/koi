# cachyos-linux — journal

## 2026-08-31 12:40 UTC — canary v2 (orchestrator-driven)
commit: da6032b | gates: cargo check -p koi-net →     Finished `dev` profile [unoptimized + debuginfo] target(s) in 47.87s
koi state now: standing daemon on port 5641 (untouched by the canary)
stack: kwin_wayland kwin_wayland_wr plasmalogin plasmalogin-hel plasmashell startplasma-way 
ladder (head): {"capabilities":[{"healthy":false,"name":"mdns","summary":"disabled"},{"healthy":true,"name":"certmesh","summary":"ready — run certmesh create"},{"healthy":false,"name":"dns","summary":"stopped"},{"healthy":true,"name":"health","summary":"0 services up (0 total)"},{"healthy":true,"name":"proxy","s
findings: none (canary run)

## 2026-08-31 09:52 EDT — session 1, lived-in workbook
commit: c36511a + working fixes | gates: koi fmt/clippy/test/build pass before final role fix; desktop fmt/clippy/34 JS tests/release build pass
koi state now: exactly one root systemd daemon, enabled and active at `/usr/local/bin/koi --daemon` on standard 5641:5643; installed/release SHA-256 `0d343c08e6100beaf4dc7ae81484cd4ab8fbdaa87dbdb8b9be70163e2b9eb313`; Authenticated identity `test-01` retained across in-place `koi install` and explicit service restart. Exactly one live desktop workbench in the active Plasma Wayland session; SNI registered and `--poke` acknowledged without a second UI.
rollback: `/tmp/koi-test01-pre-fleet.tar`, 46131200 bytes, SHA-256 `85ab8f8ccd0a54c6a4301a087fae98d48f39ef7be80fe78d19a51d0e4cd7e9e9` (old binary, unit, `/var/lib/koi`)
findings:
1. Baseline brief was wrong: the standing shape was already a root system service and its member identity was healthy. The ladder confused leaf-only membership with Open; fixed to report `authenticated member` with a regression test.
2. Full parallel tests exposed a destructive shared-test-root race in installer detection. Cleanup now stays in a test-owned child; 20 repeated focused runs and the full suite passed.
3. The desktop did not compile on Linux because Windows process APIs were unconditional. Linux service inspection/control and a standard-endpoint guard against `Run once` duplicating the daemon were added.
4. The first native Wayland launch aborted with `Gdk-Message: Error 71` on this Intel/NVIDIA machine. The workbench now selects WebKitGTK's DMA-BUF compatibility renderer on Wayland unless the operator already chose a value; the release window and SNI remain live.
5. The first successful window used Tao 0.35.3's divergent custom Wayland header and looked much thicker than expected. Koi now pins the exact upstream Tao decoration fix already proven by Ghostlight here; capture shows KDE's roughly 28-pixel native server-side frame.
6. AppImage packaging is not viable from this CachyOS host: Tauri's bundled linuxdeploy strip rejects Arch `.relr.dyn` sections. Autostart, watched-fade notification, Pond browser parity, and real fresh-login proof remain pending until a durable Linux desktop install artifact/path exists; no checkout-targeting autostart entry was created.
7. Restart log soak found an enrolled member entering the CA self-enroll retry every five seconds. The trust-plane supervisor now requires local CA ownership, not merely Authenticated posture. Final full gates passed; the corrected binary was installed in place and a 26-second post-restart soak crossed five retry intervals with no recurrence.

## 2026-08-31 15:05 EDT — session 2, ADR-038 runtime mDNS providers
commit: ADR-038 implementation in this commit, based on 677e7c8 | gates: fmt; koi-mdns 124 passed/8 real gates ignored; koi-compose 25 passed; koi-net 168 unit + 3 real-binary integration tests; strict clippy; live Avahi + resolve1 detector tests; Windows GNU provider-crate cross-target check; release build; installed-service transition pass
koi state now: exactly one root `koi.service`, PID 213016, `/usr/local/bin/koi --daemon`, release/installed SHA-256 `4392457d91a2e493c41026a70a32bcbcf0f8c5f06d2aeec852c82aa03214db72`; standard API 5641 healthy; fresh generation 1 uses `avahi`; Avahi service/socket and systemd-resolved are active+enabled exactly as at baseline.
findings:
1. Native Koi is now an ordinary full provider that the supervisor always appends at its reserved lowest priority; platform catalogs cannot shadow or undercut it. Adapters own installed/configured/running/readiness evidence and native arming, armed providers must explicitly declare capabilities/API/health, and composition only supplies the platform catalog.
2. One live installed Koi process (PID 193296, SHA-256 `c280e9ed041fde880d225591eef088aa45aa2c427515ebbed43ddf0b5419c266`) completed `avahi (generation 1) → systemd-resolved+native (2) → native (3) → systemd-resolved+native (4) → avahi (5)` without a Koi restart or binary change. Status named every route and candidate fact at each boundary. A later candidate (PID 211179) independently completed `avahi (1) → systemd-resolved+native (2) → avahi (3)` under fail-safe Avahi restoration before Koi's serial upgrade path installed the final log-noise-corrected binary recorded above.
3. During the split plan, resolve1 created the real `/org/freedesktop/resolve1/dnssd/koi_2d193296_2d1` registration; native owned continuous browse and explicit-address publication. Receive-event counts advanced after every transition.
4. The first workbook attempt correctly made no mutation, but exposed that an old ambient browse crossed the generic 90-second anti-silence window on a merely quiet Avahi event stream. This violated adapter-owned health and produced a false red capability. Browse age/event count are now telemetry only; adapter probes own health, while explicit peer traffic remains the physical reception assertion. The final installed PID remained healthy with its browse active for 345 seconds and its last observed event 139 seconds old.
5. `scripts/integration/mdns-provider-transition.sh` now coordinates unique peer publications and observations in every provider phase, preserves a long-lived subscription, asserts one Koi PID/hash, and restores provider state from an installed cleanup trap. This machine lacks authenticated SSH access to an independent fleet peer, so the new cross-server transition lane is prepared but honestly pending; local/self observation is not counted as its substitute.
6. Lived-in logs exposed one stale bootstrap claim (`mDNS engine: mdns-sd`) before the real provider plan armed. It now says runtime provider selection is enabled; the following plan log and status are authoritative. The final installed journal contains no stale engine claim.
7. A real Avahi transition exposed one warning per active browse type during the expected break-before-make window. Route-level retry logs are now debug-only; adapter evidence and the supervisor's one plan transition remain the operator-facing signals. The correction is installed in the final PID above; its repeat mutation was not run after the authorization prompt remained unanswered, so the earlier transition—not that cancelled no-op—is the physical transition evidence.

## 2026-08-31 20:35 EDT — session 3, ADR-039 control-plane ownership
commit: ADR-039 implementation in this commit, based on 5576097 | gates: fmt/diff/script syntax; koi-mdns 111 passed/5 environment-gated; real Avahi assess + publish/browse/resolve/remove; real native socket open/shutdown; real unprivileged resolve1 assessment; real root resolve1 publish/resolve/withdraw; Windows GNU provider-crate cross-target check; full workspace all-target test suite; full workspace strict clippy; release build; installed two-host provider transition PASS
koi state now: exactly one root `koi.service`, PID 280122, `/usr/local/bin/koi --daemon`, release/installed SHA-256 `05f15f4fcd80ff720c044698f7d2eff545823e7803ca59e0e1e4170e15c8e369`; API healthy; generation 5 uses Avahi for all routes with one desired/established permanent self-publication. Avahi service/socket, resolved service/activation sockets, global/link mDNS, and all enablement match baseline.
findings:
1. ADR-039 replaces the overloaded supervisor/provider facade with four owners: `RegistrationRegistry` for desired intent, `DiscoveryHub` for browse demand/cache/fan-out, `MdnsControlPlane` for routing and real materializations, and stateful provider sessions for native resources/recovery/shutdown. Registration and withdrawal now acknowledge real provider leases transactionally; structured status is canonical.
2. Live resolve1 testing exposed a distinction introspection cannot answer: `RegisterService` was present but polkit denied the unprivileged test identity. The resolve1 adapter now checks its own system-bus identity non-interactively. The user context correctly retains direct resolution while suppressing publication; the root installed-service context completed real publish/resolve/withdraw.
3. The physical run `20260901T004947Z-280219` kept test-01 PID 280122 and peer test-02 PID 89908 unchanged. Generations 1→5 selected `avahi → systemd-resolved+native → native → systemd-resolved+native → avahi`. Each phase held desired=established=3, pending=failed=0; bidirectional Koi API resolution carried TXT and explicit-address data; one subscription observed every peer lifecycle and all withdrawals.
4. The workbook's first preflight found OpenSSH collapsing an empty optional peer-sudo argument; an explicit sentinel now preserves the remote argv. A later one-shot peer status sample raced an otherwise established publication; the gate now uses a bounded structured convergence verdict and retains the accepted peer status. Neither correction weakens product assertions.
5. Cleanup restored the exact service/socket activity and enablement plus resolved global/link configuration. Both hosts remain on the same artifact with one healthy installed Koi each. Evidence lives at `target/mdns-provider-transition/20260901T004947Z-280219`.

## 2026-09-01 11:55 EDT — session 4, installed-workstation control and cooperative DNS
commit: `8347bd4` + `8b26776`; desktop `f1e2cdc` | gates: workspace all-features tests and doctests; strict all-target clippy; Windows GNU workspace check; Rust 1.92 workspace check; rustdoc with warnings denied; cargo audit; lean-embedded, documentation-leak, publish-list, and surface-ledger checks; release builds; installed live exercises
koi state now: exactly one enabled root `koi.service`, PID 404624, `/usr/local/bin/koi --daemon`, release/installed SHA-256 `1a994a78b8b40218bd27abf76f992db60b9fc42124187a43cf782c8ca887581c`; Avahi owns all mDNS routes; DNS cooperatively serves UDP+TCP on `127.0.0.1:53` and `192.168.1.109:53` beside systemd-resolved; protected local IPC is healthy. Exactly one desktop Workbench remains live as PID 389470 and reconnected after the serial daemon upgrade.
findings:
1. ADR-040 replaces endpoint guessing and ambient bearer-token files with one installed local-operator boundary. The installer records the intended Unix UID or Windows SID; the adapter owns peer identity and socket/pipe protection; the application exchanges a short-lived access document only after that check. mDNS is no longer required for local control, explicit remote endpoints never inherit local authority, and the desktop consumes the same discovered endpoint instead of assuming port 5641.
2. Local IPC registrations now belong to an explicit `RegistrationSession`; dropping the connection drains its leases. The installed Avahi route published and resolved a live `_koi-live._tcp` record with the expected address/TXT, then withdrew it when the owning CLI exited. The permanent MCP registration remained the sole desired registration afterward.
3. ADR-041 makes DNS one desired-state runtime with exact UDP/TCP listener pairs, wildcard-first binding, cooperative per-address fallback only on contention, and automatic reconciliation as interfaces or competing listeners change. A live temporary record resolved through API plus UDP and TCP on both loopback and LAN, then returned NXDOMAIN after removal.
4. The installed control boundary is materially enforced: `/run/koi.sock` is mode 0600 for UID 1000, the recorded local-access policy is root-owned mode 0600, the operator succeeds through local discovery, and an unrelated UID cannot read the socket. The service restart log contains no warnings beyond the expected unavailable Docker/Podman capability.
5. The Windows ADR-039 provider contribution was rebased and evaluated. The official DNS-SD and runtime-loaded Bonjour adapters compile in the full Windows workspace; the Windows-only physical probe now has a truthful non-Windows entry point so native all-target gates remain portable.
6. The exact-revision test-02 deployment and repeat five-phase cross-host transition remain pending solely because test-02 is powered down or disconnected: IPv4 ARP, IPv4/IPv6 probes, SSH, ordinary Wake-on-LAN, and direct Ethernet magic frames to its previously observed NIC `70:4d:7b:84:76:73` all received no response. No peer state was mutated. The already-published branches are ready for a fast-forward pull when the host is powered on; this local run is not counted as a substitute for the physical gate.

## 2026-09-01 13:12 EDT — session 5, Windows Bonjour ownership review
commit: this commit, based on `4e49e6c` | gates: fmt/diff/locked metadata; koi-mdns 112 passed/5 environment-gated; full native workspace all-target/all-feature tests; native and Windows GNU full-workspace strict clippy; Windows GNU full-workspace all-target/all-feature check and koi-mdns test compile
koi state now: installed workstation deployment untouched throughout: exactly one root `/usr/local/bin/koi --daemon`, PID 404624, on the previously validated artifact; no second core, service, alternate port, or isolated data root was started.
findings:
1. The Windows Bonjour update had no source-merge or dependency-layer conflict, but its browse pump processed and deallocated the same `DNSServiceRef` concurrently despite Bonjour's no-internal-locking contract. Timeout paths could then reclaim callback contexts while native code still referenced them. Every Bonjour connection is now created, readiness-polled, callback-driven, and retired by one owner thread; leases carry signal-only controls.
2. `DNSServiceRefSockFD` plus finite `WSAPoll` slices now make registration and resolution deadlines real. Daemon-death teardown quarantines the owner and callback context together instead of racing them, while a healthy teardown remains acknowledged. Deterministic owner-signal, late-shutdown registration, and finite-readiness tests compile into the Windows suite.
3. Failed `dnssd.dll` loads are no longer cached for the process, so a live Bonjour install can promote on a later assessment. Successful loads are retained; incomplete candidates are freed; System32 loading is restricted so a working-directory DLL cannot be selected.
4. The standalone control-plane example and its unnecessary composition dependencies were removed. ADR-039, the transition gate, and fleet coordination now require the exact candidate to replace and exercise the one installed Windows service; direct native API probes may diagnose but cannot substitute for that gate. The Windows agent still owns and must replace its historical journal evidence after pulling this commit.

## 2026-09-01 20:53 EDT — session 6, ADR-042 Pond and current-candidate fleet acceptance

commit: koi product `dbd2466`, portable gate follow-ups through `739b902`; koi-desktop native Arch package from `d2951f6` | gates: full workspace all-target/all-feature tests; strict clippy; fmt/docs/surface gates; Windows GNU workspace check; release install; native Arch package; real Plasma/AT-SPI; three-host Pond; two-host mDNS provider transition
koi state now: exactly one enabled system `koi.service`, PID `542996`, `/usr/local/bin/koi --daemon`, SHA-256 `8e3b94a9cfcaaa66f8c751bbb10e59f5b2196a2057d848c0ff8020d9395e24c3`; operator HTTP remains `127.0.0.1:5641`; Pond is deliberately disabled and port 5644 is closed. mDNS is Ready through Avahi; certmesh reports `authenticated member`. Exactly one packaged Plasma workbench remains live as PID `549703` from `/usr/bin/koi-desktop`.
findings:

1. ADR-042 adds one in-process `PondRuntime`, not a helper or second domain. Authenticated operator routes own bundle/desire; the derived fourth-port router is a fixed read-only allowlist. Desired state persists, bind/interface/firewall facts reconcile dynamically, and the desktop uses only the exact bound URL returned by Koi.
2. Physical run `20260902T002655Z-542877` used this sole installed artifact and independent peers test-02 (`192.168.1.95`) and test-03 (`192.168.1.221`). Both peers loaded all five UI assets plus health, reduced status, mDNS snapshot, and DNS entries; excluded reads and every mutation remained absent. The required serial service restart changed PID `542572→542996` without changing the artifact and restored the same narrow surface. Explicit stop closed the socket and the original disabled desire was restored.
3. UFW initially and truthfully blocked LAN port 5644 while Koi still held the desired listener. A temporary run-scoped rule made the already-reconciling runtime reachable without another PUT or daemon restart. Cleanup restored `/etc/ufw/user.rules` and `user6.rules` byte-for-byte (SHA-256 `ee39d3ed4c36016803847fc47857dd160b2d0c1cc205db2407d42b665144afff` and `98df0b16012a952b013c98b74e46133367cad99bf2ff1e10bc97635d50d59e66`) and reloaded the original active/default-deny policy.
4. Current provider gate `20260902T004825Z-546815` kept test-01 PID `542996` and Bluefin PID `9720` unchanged on hashes `8e3b94…24c3` and `89bc5e…658f`. Generations 1–5 selected `avahi → systemd-resolved+native → native → systemd-resolved+native → avahi`; all bidirectional publication, TXT/address resolution, subscription, withdrawal, and count assertions passed. Avahi, resolved, activation sockets, global/link mDNS configuration, both Koi units, PIDs, and hashes matched their captured baseline afterward.
5. `koi-desktop-git 0.1.2.r43.gd2951f6-1` built natively and pacman owns `/usr/bin/koi-desktop`, the desktop entry, and icon. The real Status autostart switch created an XDG entry with `Exec=/usr/bin/koi-desktop --minimized`, then removed it while the process count stayed one; the original disabled-autostart state remains. A package-only uninstall proved every owned runtime file disappeared, then the exact `.pkg.tar.zst` reinstalled and launched as the sole PID `549703`. This closes the checkout-path/AppImage packaging issue with a native Arch contract.

## 2026-09-02 20:14 EDT — session 7, PH-001 installer and resolve1 lifetime convergence

commit: `41ad76b` | gates: fmt/diff; full workspace check/test/strict clippy; koi-mdns 114 passed/5 environment-gated; koi-net 188 passed; Windows GNU koi-net cross-target check; release build; real installed upgrade and forced rollback; two-host provider transition with late-conflict recovery
koi state now: exactly one enabled root `koi.service`, PID `635293`, `/usr/local/bin/koi --daemon`, release/installed SHA-256 `686dd03bb1e03161a3e5eca98d7e44474e3bea3ae329d61c54f36cf30453cdc6`; `NRestarts=0`; standard `5641:5644`; `/etc/koi/config.toml` remains absent; Avahi owns all mDNS routes with desired=established=1.
findings:

1. One shared installation policy now distinguishes a fresh deployment from replacement of Koi-owned durable registration before any mutation. Systemd system/user, OpenRC, SCM, and launchd consume the final decision. Replacing this live legacy no-config service retained the standard run without probing its own listeners; the installed binary exactly matches the release candidate.
2. A run-scoped systemd drop-in deliberately made candidate health answer on `6541` while the armed transaction expected `5641`. Installation failed and restored the exact prior binary, unit, operator policy, config absence, active/enabled state, and standard endpoint, with no transaction or drop-in residue.
3. Resolve1 now owns one epoch-scoped `Conflicted` stream before registration and continuously through lease lifetime. Signal EOF/error and owner change retire unprovable materializations before adapter-owned reconstruction; control-plane counts include leases only while their session is Ready.
4. Physical run `20260903T000933Z-837819` kept test-01 PID `635293` and test-02 PID `19596` unchanged. Generations 1–5 completed `avahi → systemd-resolved+native → native → systemd-resolved+native → avahi` with bidirectional publication, resolution, TXT/address data, subscription, and withdrawal. During the resolve1 phase, the peer emitted a real conflicting SRV answer and link-level mDNS reprobe produced the late `Conflicted` signal: status truthfully became degraded/recovering with desired=3, established=1, pending=2, then recovered to ready with 3/3/0 without restarting Koi.
5. Cleanup restored Avahi, resolved, both activation sockets, global/link mDNS modes, unit enablement, PIDs, hashes, and one-process invariants on both hosts. Temporary credential helpers were removed. Evidence lives at `target/mdns-provider-transition-conflict-link/20260903T000933Z-837819`.

## 2026-09-02 21:10 EDT — session 8, PH-001 CachyOS workstation closure

commit: Koi `41ad76b`; installed koi-desktop package `koi-desktop-git 0.1.2.r43.gd2951f6-1` | run: `20260903T003500Z-cachyos-workstation` | peer: test-02/Bluefin for link and resume observations | verdict: pass
koi state now: exactly one enabled root `/usr/local/bin/koi --daemon`, PID `635293`, `NRestarts=0`, binary SHA-256 `686dd03bb1e03161a3e5eca98d7e44474e3bea3ae329d61c54f36cf30453cdc6`; standard `5641:5644`; `/etc/koi/config.toml` absent; Avahi Ready with one permanent desired/established publication. Exactly one package-owned `/usr/bin/koi-desktop --minimized`, PID `854105`, SHA-256 `cf6f256aef2254cc3ef8f19fcf8892c60d8b32463748ed6de6149f0fbac70f74`, and one SNI item remain in unlocked Plasma Wayland session `436`.
findings:

1. The intended operator and root both resolved the active service config path as `/etc/koi/config.toml`. UID `nobody` instead resolved its own `/tmp/.config/koi/config.toml`, reported every local capability offline, and received `Permission denied` opening the mode-0600, operator-owned `/run/koi.sock`; it learned no daemon fact.
2. Real ScreenSaver lock/unlock changed both the D-Bus state and logind `LockedHint` while retaining daemon PID `635293`, desktop PID `549703`, zero daemon restarts, one UI process, and the responsive loopback UI health endpoint.
3. A guarded NetworkManager down/up of the sole primary link removed its global address and default route. The daemon and desktop PIDs stayed fixed; a root transient timer restored connection UUID `d01645c0-bbf6-347f-875b-56c58184047a`; the address `192.168.1.109/24`, gateway, and metric 100 settled back exactly; Avahi returned Ready; and test-02 resolved the run-owned publication again. The timer self-collected and the registration was removed.
4. The kernel journal records a real suspend from `2026-09-02 20:38:47 EDT` until resume at `20:47:33 EDT`. After wake, the same daemon and desktop PIDs, zero restarts, SNI item, authenticated UI, LAN route, Avahi routes, and desired=established publication counts remained; test-02 resolved both the permanent MCP publication and the run-owned lease that crossed sleep. The lease and RTC alarm were removed. Because `systemctl suspend` returns before this NVIDIA/systemd stack enters sleep, acceptance is keyed to the journal's `Performing sleep operation` → `System returned from sleep operation` boundary and post-wake identity, not command return.
5. The Status UI enabled native XDG autostart and produced `/home/test/.config/autostart/Koi.desktop` with `Exec=/usr/bin/koi-desktop --minimized`. Restarting the real Plasma Login Manager replaced session `1` with fresh Wayland session `436`; Plasma PID `863→853936`; the old validation-launched workbench PID `549703` was gone; and autostart created exactly one hidden package-owned PID `854105` under `app-Koi@autostart.service`. AT-SPI initially exposed the application with no window, the real tray menu revealed it, and the UI reflected its enabled state. Disabling autostart through that UI removed the entry while leaving one live workbench.
6. Final restoration returned linger to `no`, autostart to absent, screen lock to false, provider services/sockets and global/link mDNS to active+enabled/`yes`, LAN state exactly, one permanent registration, and one Koi process. No recovery unit, RTC alarm, test registration, second daemon/core, or alternate data root remains. Five stale temporary credential/helper files used by prior fleet runs were permanently removed.

## 2026-09-03 00:28 EDT — session 9, pre-freeze shared-workbook acceptance

commit: checkout baseline `f80acb9`; Pond evidence/restoration correction in this commit; installed Koi remains the `41ad76b` artifact at `/usr/local/bin/koi`, SHA-256 `686dd03bb1e03161a3e5eca98d7e44474e3bea3ae329d61c54f36cf30453cdc6` | gates: script syntax/diff; surface, documentation-leak, publish-list, and lean-embedded checks; focused `koi-lab`/`koi-mdns` tests and strict clippy; full locked workspace/all-target/all-feature tests and strict clippy; physical provider and Pond workbooks — PASS
koi state now: exactly one enabled+active root `koi.service`, PID `869662`, `NRestarts=0`, standard `5641:5644`, operator HTTP only on `127.0.0.1:5641`, Pond desire false with 5644 closed, and Avahi Ready on all routes with one permanent desired/established publication. Exactly one package-owned `/usr/bin/koi-desktop --minimized`, PID `854105`, remains in the real Plasma/Wayland session.
peers/runs: provider run `20260903T041144Z-866860` PASS; corrected Pond run `20260903T041803Z-869519` PASS. Bluefin `test@192.168.1.95` remained the independent physical peer with one enabled+active `/usr/local/bin/koi --daemon`, PID `2332`, `NRestarts=0`, SHA-256 `d5faa755a15b27985992e296d9c8ffc0ddfaec1ab3668b19961cfe9dff7c047f`.
restoration: Avahi service/socket, systemd-resolved and both activation sockets are active+enabled; resolved global and `enp0s31f6` mDNS are `yes`; no run-owned drop-in, mask, registration, recovery unit, firewall rule, credential helper, or peer process remains. UFW is again active/default-deny and its exact `user.rules`/`user6.rules` SHA-256 values are `ee39d3ed4c36016803847fc47857dd160b2d0c1cc205db2407d42b665144afff` and `98df0b16012a952b013c98b74e46133367cad99bf2ff1e10bc97635d50d59e66`. The fixed UI files and persisted Pond intent match their captured hashes, and a bounded evidence scan found no DAT material.
findings:

1. The generalized provider workbook passed unchanged on systemd. Generations `11→15` selected `avahi → systemd-resolved+native → native → systemd-resolved+native → avahi`; every phase held `desired=3, established=3, pending=0, failed=0`. A real late resolve1 conflict produced degraded/recovering truth and returned to `3/3/0/0` without restarting either Koi. Bidirectional publication, address/TXT resolution, long-lived browse observation, withdrawal, PID/hash invariants, and exact provider restoration all passed.
2. The first Pond diagnostic pass (`20260903T041529Z-868951`) proved the product behavior but exposed an acceptance-harness defect: predicate output replaced the structured enabled/recovered evidence, PASS was emitted before the exit trap restored state, and cleanup errors were suppressed. That run is not acceptance evidence. The shared workbook now stores full status objects, restores through authenticated local control on success, failure, and signals, bounds and verifies convergence to the original desired/runtime state, proves a disabled listener is unreachable from the peer, and makes restoration failure turn the verdict red.
3. The corrected run used the sole installed service and Bluefin's Python client for both live phases. Each phase loaded the five UI assets and four allowlisted status routes and rejected four excluded/mutation routes; explicit stop and final restoration both closed peer access. The required serial service restart changed PID `869083→869662` without changing executable bytes, reconstructed the desired Pond listener, then returned to the original disabled desire before writing PASS.
4. The temporary peer-specific UFW admission was protected by a bounded fail-safe and removed after acceptance. Independent post-run inspection confirmed exact firewall/UI/intent hashes, one daemon and one workbench, loopback-only operator access, provider restoration, unchanged Bluefin service identity, and removal of both local and remote credential/recovery helpers. No production defect or advertised-surface change was found; the correction belongs solely to the shared physical-gate ownership boundary.

## 2026-09-03 09:04 EDT — PH-4 frozen CachyOS artifact, whole story, and mixed-supervisor provider lane

commit: exact frozen product source `5c89e9de11bf23ab81fd8b5b0778c58477359360`; release/installed `/usr/local/bin/koi` SHA-256 `f0e999b0077eb25935f1ad563aee33f2e659ffde915d3f3c55f5aa568691682b`, 54,184,944 bytes; the OpenRC-peer acceptance-harness correction is published with this entry | gates: clean detached-source formatting; script syntax; full locked workspace/all-target/all-feature tests; strict clippy; locked release build; public systemd install; installed Find → Name → Trust → Serve/Pond; CachyOS Avahi/resolve1/native ↔ Alpine/native provider transition — **PASS**

koi state now: exactly one enabled+active root `/usr/local/bin/koi --daemon`, PID `924696`, `NRestarts=0`, on the standard `5641:5644` run. `/etc/koi/config.toml` remains absent; operator HTTP is loopback-only; Pond desire is false and 5644 is closed. Avahi owns publish, explicit publish, browse, and resolve with one permanent desired/established publication. The existing member remains HEALTHY/Authenticated. Exactly one package-owned `/usr/bin/koi-desktop --minimized`, PID `854105`, SHA-256 `cf6f256aef2254cc3ef8f19fcf8892c60d8b32463748ed6de6149f0fbac70f74`, remains; `--poke` was acknowledged without duplication.

peers/runs: local journey `ph4-5c89-cachyos-story-01`; Pond `20260903T124749Z-924542` PASS through unchanged Bluefin `test@192.168.1.95`, final PID `80259`, SHA-256 `288362b8ae909d655f683f290f2b34bd5d8cb802a1184a0cebf27a1b3219e200`; provider run `20260903T125812Z-925941` PASS through unchanged Alpine `test@192.168.1.221`, OpenRC PID `24201`, SHA-256 `4db6a257b9303157bd8dff03887b478b2c8f5a20f777166d35a59f77436a95e9`.

restoration: DNS, health, local-access, Pond, member/certificate, UI, and data-root config bytes match the captured baseline. The run-owned proxy, explicit and derived checks, static DNS record, mDNS leases, fixture, and ports 18091/18441/5644 are absent. UFW is active with only its original rules; `/etc/ufw/user.rules` and `user6.rules` are again SHA-256 `ee39d3ed4c36016803847fc47857dd160b2d0c1cc205db2407d42b665144afff` and `98df0b16012a952b013c98b74e46133367cad99bf2ff1e10bc97635d50d59e66`. Avahi, resolved, and both resolved activation sockets are active+enabled, global/link mDNS are `yes`, and no runtime mask/drop-in, recovery unit, credential helper, second Koi, peer mutation, or run-owned publication remains. A bounded evidence scan found no DAT or credential material.

findings:

1. The release came from a clean detached worktree whose HEAD was exactly the final freeze. Formatting, the full locked native suite, strict all-feature clippy, and release build passed there. The public installer replaced the sole standing service serially, retained the effective standard run without manufacturing `/etc/koi/config.toml`, preserved member/operator state, and installed bytes identical to the built artifact.
2. Authenticated local control created a real static `.internal` record, explicit HTTP health check, and certificate-backed proxy over a deliberately bare `127.0.0.1:18091` backend. Both explicit and derived checks reached Up. Bluefin resolved DNS over UDP and TCP and fetched the workload through the real TLS proxy with hostname and member-CA verification. A run-owned explicit-address `_koi-ph4._tcp` service carried exact source/OS TXT and resolved from Bluefin before acknowledged withdrawal. Trust remained HEALTHY/Authenticated throughout.
3. A serial systemd restart changed only daemon PID `920050→923794`; DNS, both health projections, and the proxy reconstructed and passed the same physical traffic. Removing the proxy pruned its derived health projection within two seconds without another restart, and all run-owned state was then removed through public APIs. The product-managed TOML serializer normalized the temporary proxy section; the gate restored the captured comment-only data-root template byte-for-byte afterward.
4. The installed five-file UI bundle matched the sibling packaged workbench sources byte-for-byte. The restoration-gated Pond workbook exposed only its fixed read allowlist to Bluefin, denied excluded and mutation routes, closed on explicit stop, reconstructed after the required service restart (`923794→924696`), and returned to the original disabled desire before PASS. Narrow peer-only UFW admissions were protected by a timed recovery unit and then removed exactly.
5. Provider generations 1→5 selected `avahi → systemd-resolved+native → native → systemd-resolved+native → avahi` without restarting either host's Koi. Every settled phase held `desired=3, established=3, pending=0, failed=0`; a real late resolve1 conflict truthfully exposed `3/1/2/0` and recovered to `3/3/0/0`. Bidirectional ordinary/explicit publication, address/TXT resolution, one long-lived subscription, and withdrawal passed against Alpine's native provider.
6. That physical lane exposed one harness-only separation defect: peer attestation assumed systemd even though the subject side already modeled service-manager capability. The shared gate now independently detects systemd or OpenRC on the peer, binds systemd to `MainPID`, binds OpenRC to its active+enabled service and singleton process, records provider-service facts, and still requires a direct or peer-sudo `/proc/<pid>/exe` hash. No product, dependency, installer, package recipe, or shipped asset changed, so the frozen source remains valid.

## 2026-09-03 09:52 EDT — PH-4 exact-source replacement accepted

commit: exact frozen product source `e49bfe2b3e403fa87d4b8b237b49f3bb9e5cb5ef`; fleet synchronized through `31ea62ce957b339752d39e23a4507fcb15f20841`; release/installed `/usr/local/bin/koi` SHA-256 `f0e999b0077eb25935f1ad563aee33f2e659ffde915d3f3c55f5aa568691682b`, 54,184,944 bytes | gates: clean detached-source `cargo fmt --all -- --check`; integration-script syntax; `cargo test --locked --workspace --all-targets --all-features`; strict matching clippy; locked `koi-net` release build; public systemd install; installed-state, provider, trust, Pond, firewall, workbench, and log audit — **PASS**

koi state now: exactly one enabled+active root `/usr/local/bin/koi --daemon`, PID `962933`, `NRestarts=0`, invocation `08b74ea4e88f4601ad7b9e23060ef797`, on the preserved standard `5641:5644` plan. `/etc/koi/config.toml` remains absent; the operator API listens only on `127.0.0.1:5641`; Pond desire is false and 5644 is closed. mDNS generation 1 is Ready with publish, explicit publish, browse, and resolve all on Avahi; one permanent publication is desired and established with none pending or failed. Avahi, resolve1, and native all report Ready with their real capability flags. Trust diagnosis is HEALTHY/Authenticated as `test-01`, with an intact identity chain, no self-revocation, and a leaf healthy for five more days. Exactly one package-owned `/usr/bin/koi-desktop --minimized`, PID `854105`, SHA-256 `cf6f256aef2254cc3ef8f19fcf8892c60d8b32463748ed6de6149f0fbac70f74`, package `koi-desktop-git 0.1.2.r43.gd2951f6-1`, remains; its poke was acknowledged without duplication.

restoration: the public installer serially replaced PID `924696→962933`, retained `5641:5644`, and installed bytes identical to the clean release. Unit, local-access, member, data-root config, DNS, health, and Pond files are byte-exact at SHA-256 `c7657f5c5735a0b31c16e68f29df375b12e29d9713326542e5c4ae3104305b4c`, `338c07cfd7d645ef2cb24227d18318311841e81ecddba7afe53c509e490d9893`, `0a52d45009d7ee9ab9f5b9d0afc7cde1457ff04392d61787fbb42af19aeac570`, `46017703d090561b05d2b87a134a93d2a1cda1bbee74b6b6f42410d09b009d3a`, `498ca50ab1b22eb01c73569d8d8538c1f5e47f45d7a72bd72803a39a3206d8aa`, `7204c4461be5cc50762fbad7d897730a550c40657393c9f120b2a70fbd3e02da`, and `c5c8be6a208223669d383e621e8d3da1bace14f233898560747750f451291769`. Avahi service/socket, systemd-resolved service/monitor socket/varlink socket, and their enablement are restored active+enabled; global and primary-link mDNS are `yes`. UFW remains active/default-deny with only its original rules, and `user.rules`/`user6.rules` remain `ee39d3ed4c36016803847fc47857dd160b2d0c1cc205db2407d42b665144afff` and `98df0b16012a952b013c98b74e46133367cad99bf2ff1e10bc97635d50d59e66`. No installer transaction, recovery unit, provider drop-in, run-owned publication, second Koi, or new config remains.

findings:

1. The new release is byte-identical to the superseded Linux artifact because `e49bfe2` changes only the Windows DNS-SD adapter. Exact provenance was not assumed: a clean detached worktree at the full frozen SHA independently passed every native gate and produced the bytes installed through the public product path.
2. The existing Linux whole-story, desktop, lifecycle, Pond, and Avahi/resolve1/native physical workbooks were not repeated because their executable paths did not change. This host is now an unchanged exact-source Avahi peer for Windows-owned run `ph4-e49b-win-avahi-01` and read-only peer for Debian-owned run `ph4-e49b-debian-pond-01`.
3. The accepted invocation reached local-control, HTTP, cooperative DNS, and systemd readiness immediately. Its only warning is the expected truthful absence of Docker or Podman; no product error or regression was observed.

## 2026-09-03 11:36 EDT — PH-4 peer reconciliation and post-reboot audit

commit: exact frozen product source remains `e49bfe2b3e403fa87d4b8b237b49f3bb9e5cb5ef`; fleet synchronized through `1100e18abb5cd778ac4830344a3e56ebcf009668`; installed `/usr/local/bin/koi` remains SHA-256 `f0e999b0077eb25935f1ad563aee33f2e659ffde915d3f3c55f5aa568691682b` | gates: read-only reconciliation of Windows Avahi, Bluefin, Debian/Pond, and Alpine artifact evidence; physical post-reboot daemon, provider, trust, Pond, firewall, workbench, SNI, and log audit — **PASS**

koi state now: boot `2026-09-03 10:59 EDT` started exactly one enabled+active root `/usr/local/bin/koi --daemon`, PID `721`, invocation `74d03fc43b0a480da4e5859a68b685db`, `NRestarts=0`, with loopback-only operator HTTP on 5641 and 5644 closed. mDNS generation 1 is Ready with publish, explicit publish, browse, and resolve on Avahi; one permanent registration is desired/alive/established and none is pending or failed. Avahi, resolve1, and native all report their expected Ready capability facts. Trust is HEALTHY/Authenticated as `test-01`; DNS has zero static/certmesh entries, health and proxy have zero configured services/listeners, and Pond remains disabled. UFW is active+enabled and its rules remain byte-exact at SHA-256 `ee39d3ed4c36016803847fc47857dd160b2d0c1cc205db2407d42b665144afff` and `98df0b16012a952b013c98b74e46133367cad99bf2ff1e10bc97635d50d59e66`.

workbench: the active Plasma/Wayland session contains exactly one package-owned `/usr/bin/koi-desktop`, PID `1150`, installed SHA-256 `cf6f256aef2254cc3ef8f19fcf8892c60d8b32463748ed6de6149f0fbac70f74`, package `koi-desktop-git 0.1.2.r43.gd2951f6-1`, one registered SNI item, and a healthy UI endpoint. `--poke` was acknowledged without changing the process count. Koi's XDG autostart entry remains absent as intended; the process began with the restored Plasma session rather than the earlier `--minimized` autostart shape.

peers/runs: Windows `ph4-e49b-win-avahi-01` and Debian `ph4-e49b-debian-pond-01` both passed against the unchanged pre-reboot PID `962933` and exact hash above, then restored their run-owned traffic and this host's baseline. Bluefin independently published its exact-source systemd artifact. Alpine published signed/indexed `koi-1.0.0_rc2_git20260903-r3`/OpenRC artifacts from exact `e49bfe2`, installed executable SHA-256 `095371b9ed13cde254214155aa231db9333342c35255717654299fcd514e9941`, with its native/stopped-Avahi baseline exact. Those entries close every replacement and physical lane except Windows↔Alpine/native without a CachyOS mutation. The later ordinary reboot accounts for `962933→721` and independently proves the exact installed service returns healthy.

findings:

1. An initial process check falsely reported the desktop absent because it required the historical `--minimized` argument. The actual package-owned process was present as the sole `koi-desktop`; direct process, SNI, UI-health, package/hash, session-start timing, and singleton-poke checks all passed. No product correction is warranted from an over-specific observer.
2. The sole daemon warning remains the truthful absence of optional Docker/Podman. No Koi error, second process, run-owned registration, provider residue, open Pond listener, firewall drift, or peer credential remains.
3. PH-4 now waits only for Windows run `ph4-e49b-win-native-02` against unchanged exact-source Alpine; this host requires no further mutation before final reconciliation.

## 2026-09-03 12:03 EDT — PH-4 fleet reconciliation complete

commit: exact frozen source `e49bfe2b3e403fa87d4b8b237b49f3bb9e5cb5ef`; synchronized final Windows evidence `c7cbbd47576d8cc19563c1b5f55306ab69b752fe` | gates: all five exact-source artifact identities; installed whole-story and Pond entries; Windows↔Avahi, Windows↔native, and Avahi/resolve1↔native provider evidence; singleton/restoration/secret checks — **PASS, PH-4 GREEN**

koi state now: unchanged post-reboot CachyOS baseline: one enabled+active `/usr/local/bin/koi --daemon`, PID `721`, `NRestarts=0`, installed SHA-256 `f0e999b0077eb25935f1ad563aee33f2e659ffde915d3f3c55f5aa568691682b`; Ready generation 1 with all routes on Avahi and one permanent desired/established publication; HEALTHY/Authenticated identity; loopback operator HTTP; Pond disabled/closed; one package-owned workbench PID `1150` and one SNI item.

fleet evidence: Windows `9c4998461d3d0760a75c78dc2075d8d095666aa91626146af794f68d49e4b588`; CachyOS hash above; Bluefin `288362b8ae909d655f683f290f2b34bd5d8cb802a1184a0cebf27a1b3219e200`; Alpine `095371b9ed13cde254214155aa231db9333342c35255717654299fcd514e9941`; Debian `51682d682f155139f0e0d4c7f2bdce6b2e237f6bbf464ccdbfc7b30a7251cbbb`. Final run `ph4-e49b-win-native-02` held Windows PID `5312` and Alpine daemon/supervisor PIDs `2023`/`2022` through generations 1→5, bidirectional ordinary/explicit discovery, continuous resolution/removal, provider loss/return, and exact cleanup. The earlier exact-source Windows/Avahi and Debian/Pond runs plus the byte-identical Linux provider lane complete the physical matrix.

findings:

1. Every PH-4 exit condition is supported by installed physical evidence; no critical/high issue, source ambiguity, second Koi, credential leak, or unrestored system state remains. PH-4 is closed and must not be repeated on unchanged artifacts.
2. PH-5 is dispatched as installed-entry/diagnostic proof followed by one coordinated installed-service soak. The existing collector remains the sole evidence boundary, but its generic SSH-port probe and systemd-only observation are insufficient for fleet acceptance; the shared core and native systemd/OpenRC/SCM observers must pass short Koi-traffic canaries before the six-to-24-hour run starts.

# Surface Ledger

> **Rotation contract.** Before the lane leaves this repo or surface: tag; CI green;
> a tripwire exists for every surface the departing work was exercising; status
> endpoints tell the truth; this ledger is updated. Leave a guard at the door when
> you leave the room.

> **Current validation state (2026-09-03).** The workspace is `1.0.0-dev.0` while
> [ADR-043](adr/043-observable-domain-boundaries.md) is implemented and revalidated. Rows
> below retain exact historical test evidence, but no prior candidate freeze or soak result
> validates this development line. Fleet execution is paused by
> [Epic 002](../fleet/epics/002-observable-domain-boundaries.md).

This is the mechanical memory behind the maintainer's serial-lane model (Epic E02).
Each row records a pillar/plane-level **surface**, **who exercises it**, **when it was
last exercised**, and **what guard protects it**. When the focus lane rotates away,
unguarded surfaces rot silently — the ledger turns that from a discovered fiction into
a known risk.

The motivating incident lives in this repo: the TLS **proxy** worked, regressed silently
at the axum 0.8 upgrade while nothing was exercising it, and `status()` kept reporting
`running: true` for months. Its row below reads **guard = none** — the truth, until
proxy work (E08+) changes it.

**Columns.** `Exercised by` ∈ {a named in-repo test/sample suite, `zen-garden`, `koan`,
`private downstream solution`, `none`}. `Guard` ∈ {test/CI job name, `none`}.
`Last exercised` is a real date (`YYYY-MM-DD`) or `unknown since <date>` — never a
guessed "works".

**Honesty notes for this repo.**
- **Dates** are seeded from `git log -1 --format=%as <paths>`.
- **CI exists** (`.github/workflows/ci.yml`: 3-OS build + test + clippy + fmt + MSRV +
  audit). Guards naming a domain suite run there. A `surfaces` job in that workflow lints
  *this ledger* (it does not run the domain suites).
- The **HKDF domain-separation byte strings** in `crates/koi-crypto/src/unlock_slots.rs`
  are **frozen** (STACK-0001 K3) — never renamed.

| Surface | Exercised by | Last exercised | Guard | Notes |
|---|---|---|---|---|
| release identity and publication channels | centralized release-version + manifest/bootstrap contract tests; hosted Release dry run | 2026-07-20 | `scripts/release-version.mjs` + `release-version.test.mjs` + `release-manifest.test.mjs` + npm tests + `release.yml`/`publish.yml` channel gates | One SemVer evaluator derives tag identity, prerelease status, and npm dist-tag for preparation, tagging, manifests, and CI. A tag/workspace mismatch fails before publication. Stable releases alone advance GitHub/GHCR/npm defaults; prereleases are GitHub prereleases, exact-only GHCR tags, npm `next`, and exact-version crates.io publications. Local stable/RC/invalid/versioned-manifest/bootstrap contract suite passes 13/13, including static checks that every channel consumes the centralized decision; npm pack passes when given the staged manifest required by the workflow. Hosted dry run `29698199626` previously proved all six build/archive/checksum lanes without publication; the new RC policy still requires a hosted dry run after integration. |
| physical lab evidence + profile orchestration | all completed check-bearing `koi-lab` scenarios and `run-profile` policies | 2026-07-20 | `tools/koi-lab/src/evidence.rs` + `profile.rs` + `profile_journal.rs` tests + `scripts/lab/scheduled-profile.ps1` + physical smoke/certmesh/full/recovery profiles | One publisher derives canonical JSON, JUnit XML, and readable text from each report's single check list and refuses publication unless the report explicitly attests secret redaction. Smoke/certmesh/full/soak policies centrally list existing evaluators; each case owns a fresh deploy/scenario/exact-cleanup transaction and the aggregate compares stable pre/post service, artifact, and listener baselines. Smoke `v1-20260720T205716Z-41637b4e` passed 8/8; certmesh `v1-20260720T211240Z-df893184` passed 23/23; manual full `v1-20260720T211813Z-47d0f395` and scheduled full `v1-20260720T221929Z-238c05e1` each passed all 12 transactions and aggregate JUnit 38/38. Profiles append/sync child identities before mutation, heartbeat live ownership, and expose one stale `recover-profile` evaluator that reuses exact local/remote cleanup and the captured preflight baseline; the scheduler delegates to it before local builds. Controlled execution `v1-20260720T232552Z-d5dfe569` was killed after deployed child `v1-20260720T232601Z-11b15882` held matching locks and staged run directories on both Linux nodes; stale recovery removed the exact child and passed 3/3 including baseline restoration. Independent preflight left Brook inactive/not-found, preserved Granite's active/enabled PID 803, and found no live journal markers. This is complete full green 2/3; the soak gates stable `1.0.0`, not `rc.1`. |
| platform system service supervision | `koi-lab service-lifecycle` + CachyOS, Debian, Bluefin, Windows, and Alpine PH-001 installed lifecycle | 2026-09-03 | `tools/koi-lab/src/service_lifecycle.rs` + systemd/SCM/OpenRC transaction tests + fleet journals | The earlier Brook run proved transient `Type=notify`, restart, DAT rotation, and capability reconstruction. Debian 13 additionally proves the fixed-path public product path with one real `/usr/local/bin/koi`: installed upgrade, a deliberately broken daemon candidate rejected non-zero with exact binary/unit/operator restoration, durable migration of legacy port decisions, complete uninstall, and reinstall from the preserved installed path without ETXTBSY. The ordinary operator reached the root service through authenticated local control and `koi trust diagnose` matched the daemon's Authenticated posture. Bluefin 44 proves the immutable-workstation composition: build dependencies stayed in a toolbox; ownership-aware systemd upgrade retained its live no-config standard-port run, a distinct unhealthy candidate restored exact deployment state, and a genuinely fresh install shifted around a real non-Koi incumbent before restoring the intended `/usr/local/bin/koi` deployment. The native desktop RPM survived rpm-ostree install, upgrade, rollback, and reboot activation while GNOME autostart retained exactly one workbench and tray item. Windows 11 now proves the product-owned SCM path with a durable cross-substrate transaction: recovery begins before parsing a corrupted transaction-owned config, recreates an expected-but-missing service from a complete lossless descriptor, reapplies lifecycle policy, restores exact binary/config/operator-policy bytes and profile-scoped firewall semantics, and commits only after SCM PID/image plus health verification. Its ownership-aware acceptance additionally retained a no-config service on standard ports, rolled back a candidate stuck before control-handler registration through exact-state and same-handle image verification, exercised a fresh install around a real non-Koi listener on 5641, and restored the intended standard deployment with no manifest, backup, firewall, task, listener, or recovery-copy residue. CachyOS accepted ownership-aware in-place upgrade and exact failed-candidate rollback, then retained the same system daemon through primary-link churn, lock, and real suspend/resume while a fresh Plasma Login Manager session started exactly one minimized package-owned workbench. Alpine accepted ordinary indexed main/OpenRC upgrade while retaining its standing shifted ports, then rejected a zero-process candidate and restored exact accepted binary/config/policy/identity/init/logrotate bytes with no transaction debris. Its final static-PIE APK remained one healthy supervised OpenRC daemon. Launchd remains a separate gate. |
| bounded physical resilience soak | `koi-lab bounded-soak` | 2026-07-20 | `tools/koi-lab/src/soak.rs` + shared `derived.rs` verdicts | Short proof `v1-20260720T195910Z-a235c0c4` completed 3/3 unique container convergence/reversal iterations in 29,614 ms. Iteration 2 restarted Brook `66702→67236`, rotated the DAT, and reconstructed runtime/DNS/cross-host-mDNS/health/TLS-proxy/live traffic before exact reversal. Both iteration count and next-iteration admission time are bounded (up to 100,000 / 24 hours); exact cleanup and final preflight preserved Granite PID 803. The schedulable profile is proved; a 6–24 hour release soak is not yet claimed. |
| certmesh lifecycle + cold recovery (physical LAN) | `koi-lab` Linux forward/reverse lifecycle and recovery profiles + Windows-client tracked lifecycle profile | 2026-07-20 | `tools/koi-lab` + dedicated Brook/Granite/Windows release gate | Both Linux role directions prove pinned CSR-only join, local custody, chain/SAN integrity, key-rotating renewal, restart, revocation/RED behavior, encrypted v2 backup, wrong-pass immutability, exact run-owned data loss, host-bound restore, locked restart, passphrase replacement, and renewed mTLS continuity. Elevated Windows run `v1-20260720T043212Z-0b37baee` proves exact `LocalMachine\Root` SHA-256 install/remove, Schannel `curl.exe`, `Invoke-WebRequest`, wrong-host and fail-after rejection, byte-exact hosts restoration, complete store restoration, wrong-pin refusal before key creation, SID-based ACL custody, key-and-leaf rotation with key correspondence and CA-roster convergence, exact run-owned daemon PID `41980→19832` with identity/diagnosis/proxy continuity, revocation restart to PID 23936, RED/self-revoked diagnosis, and mutation-free renewal/rejoin refusal. Fresh issuance is backdated by the centralized 300-second skew tolerance; failed enrollment stages its key until a matching leaf is validated. Windows cold recovery remains unclaimed. Generic TLS revocation remains limited by the documented lack of CRL/OCSP. |
| physical capability breadth (Acts 0, 3, 4, 5, 6, 7, 8, 10, 11 + ACME) | `koi-lab capability-story` Linux forward/reverse profiles | 2026-07-20 | `tools/koi-lab/src/story.rs` + dedicated Brook/Granite release gate | Both role directions prove isolated mode-0600 breadcrumbs and Unix IPC; HTTP and real cross-host DNS; multicast resolve, heartbeat, and goodbye removal; real Docker inventory plus orchestrator-derived mDNS/DNS/health/live self-signed TLS proxy state; Koi daemon restart while the container remains running; full runtime/mDNS/DNS/health/live-proxy reconstruction under a new PID; exact reverse container teardown; health Up→Down→Up; protected UDP SSE; and the live aggregation surfaces. Act 7 additionally proves pinned certmesh enrollment, certmesh-issued/native-trusted proxy TLS, hostname rejection, key-and-certificate rotation, a changed physical leaf without daemon restart, exact native-root removal, and fail-after-removal. Its real `instant-acme` order publishes proof through Koi's authenticated TXT API, observes it from the other host with `dig`, exact-clears it, and validates the issued chain/SAN. Forward `v1-20260720T014815Z-96794673` (Brook PID `38290` → `38620`) and reverse `v1-20260720T015153Z-558233f4` (Granite PID `67926` → `68309`) passed with musl SHA-256 `928522b3c18fce60a28310e619fbb4ff715d8b0a9f03c059842eaef6629f8d07`. The binary is built and packaged locally into a run-tagged scratch image; no remote build occurs. Compensating cleanup verifies ownership before removing run state. Portable Tier-1 aggregation now additionally runs two isolated real daemons concurrently and proves each status/host HTTP surface, the shared dashboard ladder, public MCP card, DAT refusal, all 11 MCP tools, all four resources, and a live resource read. Physical Windows whole-story capability breadth remains unclaimed. |
| outbound webhook fan-out (ADR-028) | koi-compose webhook unit suite + real-TCP integration tests; embedded parity test; `koi-lab webhook-fanout` both Linux rotations | 2026-08-24 | `crates/koi-compose/tests/webhook_delivery.rs` + `webhook::tests` + `crates/koi-embedded/tests/webhook_embedded.rs` + `koi-crypto::hmac` RFC 4231 vector + `tools/koi-lab/src/webhook_fanout.rs` physical gate | Envelope shape, closed key set (redaction guard), bounded backoff, manifest validation, and HMAC signing are pinned locally; real-TCP tests assert exact headers, retry-after-failure carrying identical id/body, and disabled-sink isolation. The embedded instance proves programmatic-sink parity: a configured sink receives a signed `dns.updated` over real TCP and `/v1/status` reports it. Physical runs `v1-20260823T234022Z-ae31ed73` proved brook→granite (45 deliveries) and granite→brook (41), each HMAC-verified at receive by the peer's fixture, daemon healthy throughout, exact cleanup, baseline restored. `/v1/status` reports `webhooks {enabled,sinks}` on both daemon and embedded paths (ADR D1 closed 2026-08-24). |
| principal identity — client-role mesh membership (ADR-026) | koi-certmesh principal/profile/invite unit suites; Tier-2 two-daemon principal lifecycle over the real binary; `koi-lab mgmt-principal` both Linux rotations | 2026-08-24 | `crates/koi-certmesh/src/principal.rs` tests + `csr::client_profile_refuses_server_auth_and_key_encipherment` + `invite` role-binding tests + `enrollment::role_bound_invite_refuses_mismatched_join_and_burns_the_token` + `crates/koi/tests/two_daemon_certmesh.rs::client_principal_enrolls_reaches_mgmt_plane_and_revocation_closes_it` + `tools/koi-lab/src/mgmt_principal.rs` physical gate | Client-role leaves carry clientAuth-only EKU and digitalSignature-only KU (ServerAuth is structurally unreachable); role-bound invites refuse mismatched joins and burn on attempt; enrollment audits carry `role` + `via` provenance; management-plane authorization (`CertmeshCore::authorize_principal`) refuses unknown/expired/revoked CNs with named reasons audited as `mtls_unknown_cn` / `mtls_expired_cn` / `mtls_revoked_rejected`. The real-binary run proves local keygen + CSR custody, a client join whose response carries no private key, a healthy principal completing an MCP initialize over the mTLS management listener, and immediate 403-with-named-reason after revocation. Physical runs `v1-20260824T122847Z-5d021108` forward (brook CA / granite probe) and reverse swapped proved the whole story across the real LAN: controller-local keygen (rcgen), raw cross-host tokenless join, empty `service_key`, roster role `client`, a healthy principal's MCP initialize = 200 over mTLS dialed by SAN name (`--resolve`; generic OpenSSL curl enforces hostname verification — D14), revocation → 403 naming its reason, exact cleanup, baseline restored (Brook not-found, Granite active/enabled koi 0.7.0). Musl SHA-256 `233345caf7166f7304c9580cb42d8eeffe3328db4742fc4f697449e5a604183f`. Per-mutation actor attribution beyond the enrollment/authorization events is deferred with the V1-11 SDK work. |
| mdns | koi-mdns suite + live Linux adapter probes + one-installed-process provider transition + two-box LAN gates | 2026-09-03 | koi-mdns lib tests, strict clippy, Windows provider-crate cross-target check, ignored real Avahi/resolve1/native tests, `scripts/integration/mdns-provider-transition.sh` | ADR-039 gives exclusive ownership to `RegistrationRegistry` (intent), `DiscoveryHub` (browse demand/cache), `MdnsControlPlane` (routes/materializations), and stateful provider sessions (native resources/recovery/shutdown). Publication/withdrawal and browse close are acknowledged; route transitions disturb only changed routes and roll back when publication replay fails; native Koi remains the reserved lowest-priority complete provider. Physical run `20260902T004825Z-546815` used the sole installed Koi on test-01 and Bluefin; both stayed fixed while generations 1→5 selected `avahi → systemd-resolved+native → native → systemd-resolved+native → avahi`, with bidirectional publish/resolve/TXT/removal, explicit-address publication, subscription continuity, synchronized intent, and exact provider cleanup. Debian run `20260902T202909Z-22323` repeated all five phases on the one installed system service and test-01. It surfaced and closed resolve1's asynchronous acceptance seam: a registered Dnssd object is now observed through its bounded conflict-probe window, `Conflicted` becomes a typed failed publication with explicit cleanup/retry, and readiness is reported only after quiet settlement. The installed gate then passed every phase and restored Avahi/resolved activity, enablement, and per-link mDNS policy exactly. Windows run `20260902T223728Z-windows-provider` held one installed PID through Windows DNS-SD reads/native publication, verified Bonjour promotion/loss/return, Public/Private categories, primary-NIC disable/enable, lock, and real S3 resume against unchanged CachyOS/Avahi and Alpine/native peers. Generations 1→5 kept three desired/established publications with zero pending/failed; every phase proved bidirectional ordinary/explicit publication, address/TXT/interface resolution, and exact provider/link/profile/peer cleanup. CachyOS run `20260903T000933Z-837819` then held one installed PID on each host while a peer-emitted SRV conflict reached an already-established resolve1 lease: status became degraded with the provider recovering and counts `desired=3, established=1, pending=2`, then returned to ready `3/3/0` without a Koi restart; all provider and link state restored exactly. The same installed artifact retained its Avahi publication through sole-primary-link down/up and a kernel suspend/resume; test-02 resolved run-owned records after both recoveries, and each mutation restored one permanent desired/established publication with no daemon restart. Alpine OpenRC run `20260903T014605Z-1730` generalized the same installed-service gate by provider capability, held subject PID `2688` and Debian PID `24507` through `avahi → native → avahi`, then recovered real primary-interface down/up with bidirectional read/removal and exact provider/link cleanup. Frozen-source runs `ph4-e49b-win-avahi-01` and `ph4-e49b-win-native-02` finally held the exact installed Windows, CachyOS, and Alpine artifacts through five native/Bonjour generations each. Long-lived Windows subscriptions received real Avahi and native goodbyes, both peers independently observed Windows withdrawal, counts converged, and every service/provider/network/firewall/publication baseline restored exactly. |
| Pond read-only LAN adapter | koi-serve/compose/installer contract suites + installed-service physical browsers | 2026-09-03 | `pond` and DAT/OpenAPI/port-plan tests + `windows_firewall` adapter tests + `scripts/integration/pond-lan.sh` | ADR-042 keeps one process and two intentionally different routers: DAT-gated desire/publish on the configured operator adapter, and a fixed read-only allowlist on derived port `http+3`. Desired state persists and retries; status carries exact URL/firewall/reason; desktop and the authenticated installed CLI render that URL and expose explicit stop; Windows install owns the managed rule while Linux only observes. Physical runs `20260902T002655Z-542877` (CachyOS, two peers), `20260902T004557Z-9528` (Bluefin, two peers), `20260902T003409Z-14183` (Alpine), and `20260902T203312Z-24421` (Debian, test-01 peer) preserved one installed deployment, proved every public and excluded route, explicit stop, restart recovery, and restoration of prior desire; CachyOS additionally restored its byte-exact UFW baseline. Debian also proved honest Linux applicability: neither UFW nor firewalld was active, the remaining nftables topology contained Docker forwarding/NAT but no host-input filter, status reported `unknown` with that exact reason, and Koi made no firewall mutation. Windows workbooks `20260902T185542Z-firewall-truth` and `20260902T192334Z-active-store` prove startup diagnostics and Pond share one executable/profile-aware typed verdict over effective `ActiveStore` policy and its associated filters, while installer lifecycle remains local-`PersistentStore` scoped; both runs retained one installed process, made no acceptance-only policy change, and restored prior Pond desire. Alpine PH-3 run `20260903T015848Z-ph3` additionally sent 200 excluded requests across DNS mutation, certmesh log, Pond control, OpenAPI, and MCP from Debian; all returned 404 while allowed reads stayed 200, the LAN operator port refused 40 attempts, and Pond returned exactly to disabled. Alpine upgrade run `20260903T024856Z-16330` repeated the complete public/excluded/stop/restart/recovery gate from Debian against the dated installed musl artifact and restored disabled desire; the portable peer path now uses Python 3 and tolerates BusyBox `setsid`. Exact-source Debian run `ph4-e49b-debian-pond-01` repeated the full allowlist, excluded/mutation denial, explicit stop, systemd reconstruction, and peer-refusal checks from CachyOS, then restored its shifted service, disabled Pond desire, packages, helpers, and credentials exactly. Debian PH-5 entry validation then proved `koi pond status|start|stop` from a neutral ordinary-user shell: start returned the socket-backed `http://192.168.1.112:24444/`, CachyOS read its real `/healthz`, and stop restored the exact disabled-state bytes and closed listener. |
| dns | koi-dns suite + `koi-lab capability-story` Act 3 and ACME mini-act | 2026-09-03 | koi-dns/runtime-state tests + Brook/Granite physical gate + Debian link/hostile-input workbook | mDNS-to-DNS alias bridge; needs query caching. DNS now binds UDP+TCP before a start can report success; the shared lifecycle chokepoint leaves failed launches stopped/retryable and generation-guards completion so an old loop cannot stop its replacement. Forward `v1-20260720T190754Z-fdc848da` and reverse `v1-20260720T191154Z-adb441bc` physically prove record preservation across stop, unified `stopped` status, explicit HTTP failure under an owned UDP conflict, conflict removal, successful retry, and restored cross-host `dig`. Debian run `20260902T203225Z-link` removed the primary DHCP address/default route behind pre-armed local recovery: the installed daemon retained its PID/restart count and loopback/Docker DNS binds, then restored `192.168.1.112:53` after DHCP returned. Run `20260902T203531Z-hostile` accepted 500 malformed UDP DNS datagrams and 100 malformed TCP sessions from test-01 without a crash, restart, route/publication change, descriptor/thread growth, or fatal/resource log. The authenticated ephemeral-TXT API owns exact set/clear semantics so concurrent dns-01 proofs cannot clobber one another; public dashboard events omit proof values. A real cross-host ACME order proves `AcmeDnsBridge → DnsCore` publication, observation, validation, and cleanup. **Rate limiting is per source IP** with a whole-resolver backstop + bounded client map; configurable via `--dns-qps` / `KOI_DNS_QPS`. Alpine PH-3 run `20260903T015848Z-ph3` repeated 500 malformed UDP DNS datagrams and 100 length-mismatched TCP sessions twice from Debian; PID `2688`, generation 5, publications `1/1/0/0`, 14 threads, and 21 armed/20 restored descriptors stayed stable with no retry, fatal, OOM, or route/provider failure. |
| certmesh (CA / enroll / roster / renew) | koi-certmesh suite (258 tests) + koi-embedded whole_story (in-process) + koi-net two_daemon_certmesh (real-binary) + docker cross-host | 2026-06-24 | koi-certmesh + koi-embedded whole_story + koi-net two_daemon_certmesh + cross-host (ci.yml) | The claimed invention; revocation is roster-only (no CRL/OCSP). **ADR-017 fully implemented** (phases 1–4): CSR-only issuance + member-pull rotate-key renewal; signed monotonic trust bundle + sequenced single-writer roster + boundary revocation; pinned-fingerprint bootstrap (invite carries CA fp → preflight + install hard-fail) + `_certmesh._tcp` fp= advertise + anchor self-heal; machine-binding clone-refusal at boot + full failure audit + promote key/cert check + RFC 1123 validator + persisted TOTP rate limiter. One residual: live CA re-key transition (deferred). **ADR-018 Tier 1** (`crates/koi-embedded/tests/whole_story.rs`): two embedded daemons (CA + member) exchange the whole story over real HTTP + mTLS in one `cargo test` — join-over-HTTP, key-rotating mTLS renewal, **live boundary revocation (403 + self_revoked)**, F3 pin hard-fail, F7 lockout-survives-restart, F11 clone-refusal — gated per-OS via `cargo test --locked`. **ADR-018 Tier 2** (`crates/koi/tests/two_daemon_certmesh.rs`): two real `koi` binary daemons exchange create→invite→join→revoke over real cross-process HTTP through the DAT middleware (gated POST→401, `/join` exemption, member-csr/member-cert custody adapters) + the cross-process revocation boundary (revoked re-join→403). **ADR-018 Tier 3** (`scripts/cross-host-certmesh.sh` + `docker/cross-host/`, the `cross-host` ci.yml job): two containers (distinct hosts on a bridge) drive create→invite→join→revoke→revoked-rejoin over the real network via the `koi certmesh` CLI — which **surfaced + fixed a real CLI bug** (join misrouted key custody to the CA via a clap `--endpoint`/positional id collision). **ADR-018 Tier 4** (`scripts/cross-platform-certmesh.ps1`): a native Windows `koi.exe` member joins a Linux container CA **across platforms** — validated locally (a Windows+Docker host or self-hosted runner; GitHub-hosted runners can't pair OSes). All four ADR-018 tiers landed. **ADR-020 trust-plane guards:** the P7 deterministic LAN-trust simulator (`crates/koi-certmesh/tests/trust_sim.rs`, a sans-IO + `proptest` state machine that doubles as Koi's internal trust state-machine backbone) and the certless conformance vectors + validator (`docs/reference/vectors/trust-vectors.json` + `crates/koi-certmesh/src/conformance.rs`). **ADR-021 (0.6.0):** CA-side renewal extracted into the transport-agnostic `CertmeshCore::renew_member(authenticated_cn, csr_pem)` (the mTLS `/renew` handler is now a thin CN-proving wrapper) — authorization checked before the CSR is parsed, SAN-expansion rejected loudly; guarded by 6 direct unit tests (active / revoked / SAN-expansion-rejected / expired-still-renews / happy-path-issues+records+emits / ca-locked) + the two-box hardware gate (`scripts/integration/cross-host-test.sh`, 20/0 on 2026-06-24) |
| trust primitives wire contract (Envelope / Posture / Sealed / same-port dual-mode) | koi-certmesh suite (sign/verify + seal/open + conformance) + koi-embedded both-postures gate | 2026-06-24 | `docs/reference/trust-protocol.md` + conformance vectors/validator (`docs/reference/vectors/trust-vectors.json` + `crates/koi-certmesh/src/conformance.rs`) + `crates/koi-certmesh/tests/trust_sim.rs` (ci.yml) | The ADR-020 mode-transparent primitives as a **language-neutral wire contract** (Posture descriptor, Envelope carry-cert sign/verify, Sealed v0-passthrough, the same-port ClientHello-sniff dual-mode handshake, the diagnose report) — the STACK-0001 **D7 surface extension**. A non-Rust sibling implements identical primitives from the spec; the certless deterministic vectors keep the reference impl honest and let siblings self-test. **ADR-022 (0.7.0):** the request-binding identity door `Assurance::identity_for(env, expected)` (authorize on signer **and** payload match — closes the captured-envelope-replay footgun) and trusted-only signer attribution `Rejected { reason, signer_cn }` (Some for Expired/Revoked where the leaf chained, None otherwise) — both in trust-protocol.md §2; guarded by koi-common `identity_for` units + koi-certmesh `expired/unknown_signer` attribution units + the trust_sim oracle |
| certmesh mtls library API (koi_certmesh::mtls) | koi-certmesh suite (mtls handshake test) | 2026-06-15 | koi-certmesh tests (ci.yml) | build_server_config + serve(router) + extract_cn — a tokio-rustls listener that requires CA-signed client certs and injects the peer CN as Extension(ClientCn). Promoted out of the koi binary adapter so consumers reuse the trust wiring (stack canon: no duplication); the binary adapter now delegates here. Test: a no-cert client is rejected, a CA-signed client is accepted and its CN surfaced. Uses an explicit aws-lc-rs provider (no global install_default dependency) |
| crypto (envelope enc / TOTP / auth) | koi-crypto suite + lean tripwire | 2026-06-19 | koi-crypto tests + lean (ci.yml) | HKDF domain-separation byte strings are FROZEN (STACK-0001 K3). OS-keychain sealing (`keyring`) + QR rendering (`qr`) are opt-in default-on features (ADR-014); lean consumers shed the Secret Service/D-Bus + image-codec closures. ADR-017 P4 added `vault::machine_fingerprint()` (clone detection) and made `totp::RateLimiter` wall-clock + serde so the lockout persists across restart |
| truststore | os-truststore (os-tools repo) | 2026-06-20 | os-tools CI (external repo) | **Spun out of koi to the external `os-truststore` crate** (os-tools repo; ADR-019) — koi consumes it via a crates.io version dep (`os-truststore = "0.0.2"` in the workspace `Cargo.toml`). Implementation + tests (unit + cross-distro install round-trips on Debian/Fedora/Arch) live in os-tools; `koi-certmesh` + `koi trust` were rewired to the cert-as-identity API. koi's own CI no longer exercises trust-store install. |
| proxy (TLS passthrough) | koi-proxy data_plane_tests + `koi-lab capability-story` Act 7 | 2026-07-20 | koi-proxy tests (ci.yml) + Brook/Granite physical gate | **Rebuilt (P04) as a `tokio-rustls` TLS passthrough** (`copy_bidirectional`): real `ListenerStatus` over a watch channel and cert hot-reload through one runtime chokepoint. Focused tests guard round-trip, full-duplex, bind-conflict→Error, and cert-change-without-restart. Physical Act 7 now composes `.internal` DNS, certmesh issuance, native trust, wrong-host rejection, member renewal/key rotation, and proves the listener serves a different leaf while the daemon PID remains unchanged; exact root removal restores rejection. The original "panics / 0 tests / outside all contracts" status is **superseded** — see [the assessment realignment](assessment/realignment-2026-06-22.md) (D1 DONE). |
| udp (datagram bridging) | zen-garden | 2026-03-20 | koi-udp tests (ci.yml) | Exists for the zen-garden Garden-mesh substrate; token-topology fix pending |
| token / DAT auth | koi-net two_daemon_certmesh (daemon-side gate) + private downstream solution / in-flight | 2026-06-19 | koi-net two_daemon_certmesh (ci.yml) | DAT constant-time auth. The **daemon-side** `x-koi-token` middleware is now guarded by ADR-018 Tier 2 (`crates/koi/tests/two_daemon_certmesh.rs`): a gated POST without the token → 401 `unauthorized`; GET is exempt; `/v1/certmesh/join` is the one exempt mutation. The downstream token-**topology** work remains in-flight and not inspected (CHARTER rule 8) |
| serving layer (koi-serve: one HTTP router + serve()) | koi-serve units + koi-net/koi-embedded suites + two-box cross-host gate | 2026-09-02 | koi-serve tests (http [incl. `protected_read_*`]/mcp_http/prometheus_sd/dispatch/acme/local-control) + `scripts/integration/cross-host-test.sh` checks 13–18 (ci.yml + two-box) | The transports and posture-reactive trust plane live in the **koi-serve** crate; `koi_serve::serve(cores, ServeConfig, cancel, tasks)` is the one serving entry point shared by the daemon and Windows service. Its local transport authenticates the installed operator before returning endpoint, DAT, and optional resolved data root. Bluefin proved the real system service → Unix socket → desktop path while public `/v1/status` omitted that root. The main HTTP router remains secure-by-default on loopback and peer-aware when exposed. ADR-042 closes the Phone seam with one independently reconciled, read-only Pond router on the derived fourth port; the desktop publishes through authenticated operator control and renders only the exact socket-backed URL returned by Koi. Three physical-host runs proved the allowlist, tokenless mutation refusal, dynamic listener reconciliation, and serial restart recovery. |
| runtime / orchestrator (Docker/Podman) | koi-runtime suite + `koi-compose` provider lifecycle + `koi-lab capability-story` Docker lifecycle + `koi-lab runtime-reconnect` | 2026-07-20 | koi-runtime/koi-compose tests (ci.yml) + Brook/Granite physical gate | `RuntimeBackend` plus `RuntimeCore::start_with_backend` is the one provider entry: built-in and custom adapters participate in connect, initial lossless snapshot-to-stream observation, inventory reconciliation, event consumption, and owned teardown. Direct aggregate event injection is test-private. Orchestrator startup reconciles the current inventory through that start path before consuming live events. The Docker backend preserves inventory across event-stream loss, drives health false/true through the domain ingest chokepoint, retries with bounded backoff, pings and relists, emits deterministic material stopped/started/updated reconciliation facts, and resumes from an inclusive Docker event cursor to close the relist→stream race. Full normalized snapshots plus deterministic port/IP ordering suppress observation-time churn, duplicate engine facts, and inclusive replay for unchanged instances, so their derived services remain registered. The orchestrator consumes only normal lifecycle facts and has no parallel reconnect engine. Both physical role directions prove startup recovery across a Koi daemon restart (`v1-20260720T014815Z-96794673`, `v1-20260720T015153Z-558233f4`). Both also prove an isolated event-stream fault through Koi's mode-0600 run-owned Unix relay without restarting Docker (`v1-20260720T181705Z-f96627ae`, `v1-20260720T181902Z-165ff7be`): health falls and recovers, the last good inventory remains available, one unchanged container retains the same mDNS registration plus live DNS/health/proxy traffic, exactly one stopped/started/updated delta is reconciled, inclusive replay adds no duplicate, and exact owned cleanup succeeds. Docker/Podman remains an opt-in `docker` feature. |
| health | koi-health suite + installed Alpine/Debian PH-4 journeys | 2026-09-04 | koi-health/client/serve tests (ci.yml) + Alpine/Debian physical gates | Service and machine health. The durable transition log is now queried only through `HealthCore`/`HealthRuntime`; the real `/v1/health/log` adapter, client route test, strict required-field DTO, OpenAPI guard, and mode-aware CLI prevent presentation code from reading another owner's persistence. This development-line boundary has local test evidence; the earlier physical journeys have not yet been rerun against it. Proxy-derived HTTP checks normalize the supported bare `host:port` form into a valid URL and reconcile removed entries from capability state. Alpine run `20260903T043922Z-ph4-alpine` proved explicit and proxy-derived checks Up across OpenRC restart while Debian traversed its certmesh TLS listener; Debian then independently proved a bare-backend proxy serving verified TLS while its derived check stayed Up across systemd restart. Exact removal returned both ladders to zero services without another restart. |
| config + authenticated local control | koi-config/common/client/serve suites + installed Windows/Bluefin/CachyOS workbenches + Debian/Alpine system services | 2026-09-03 | `local_control` wire tests, koi-client/serve tests, strict clippy, Windows/Bluefin/CachyOS installed workbench and Debian/Alpine CLI physical gates | Owner-private breadcrumbs remain the fast path. Machine services capture one operator principal and hand endpoint/DAT plus the optional resolved data root over the authenticated Unix socket or Windows pipe. A separate authenticated `info` request reports the daemon's non-secret data root and launch-selected config path even when HTTP is disabled. Windows proved the installed SYSTEM service, mapped interactive operator, explicit protected pipe DACL, exact endpoint/DAT handoff, and workbench data-root rendering at the default planned port; broad grants remained absent. Its serial PH-1 lifecycle then put a real non-Koi listener on 5641 with no Koi service present, installed one fresh Koi on shifted `5651:5654`, persisted that run, removed it, removed the incumbent, and restored the intended no-config standard deployment. Bluefin rendered `/var/lib/koi` from the real system daemon while public status and Pond omitted it. Debian proved ordinary-operator and root `config path/show` resolve the installed `/etc/koi/config.toml`, `show` is byte-identical, explicit `--config` wins, an unrelated UID receives no daemon fact, and all results survive a real service restart. CachyOS repeated the machine-service boundary in a lived-in Plasma session: operator/root resolved `/etc/koi/config.toml`, while UID `nobody` received offline state and `EACCES` from the operator-owned mode-0600 socket before learning any daemon fact. Legacy daemons fall back without guessing. Alpine physically repeated the machine-service boundary on its installed OpenRC artifact: `/run/koi.sock` was `test:root` mode 0600, the intended operator received the exact shifted endpoint and a DAT, and UID `nobody` received `EACCES` plus only its offline `/nonexistent` config path. |
| dashboard + mDNS browser | koi-dashboard tests | 2026-06-24 | koi-dashboard tests (ci.yml) | XSS closed structurally (DOM render + http/https launch allowlist) + CSP header; lazy meta-browse; lives in koi-dashboard crate, not the koi-common kernel (P06). Well-known mDNS service-type annotations (`well_known::annotate` → friendly browser labels for HomeKit/Matter/ESPHome/Google Cast/etc., raw type as tooltip; `well_known` unit tests + the TypeSummary snapshot carry them, 0.6.0) |
| CLI command manifest (clap + help metadata) | koi help conformance suite | 2026-06-22 | `crates/koi/src/help/mod.rs` tests: `meta_covers_every_clap_leaf` (name↔clap-leaf bijection), `every_command_name_routes_through_clap` (each leaf `--help` routes through `Cli::try_parse_from`), `every_example_parses` (every curated example round-trips through clap), `example_corpus_is_populated` (non-vacuous) (ci.yml) | The hand-written `surface.rs` manifest with its seven drift bugs is **removed** (D9 superseded — see [the assessment realignment](assessment/realignment-2026-06-22.md)): clap (`crate::cli`) is the single source of truth and `help::meta` only adds presentation/semantic metadata keyed by the clap moniker. Drift (a phantom command, a renamed flag, a stale example) is now a **test failure** by construction, not a silent user-visible bug |
| embedded / client API | koi-embedded + koi-client | 2026-05-10 | koi build (ci.yml); examples untested | Non-compiling README examples; remote mode degrades silently |
| mcp (stdio + in-process Streamable HTTP for AI agents) | koi-mcp suite + koi http-session test | 2026-06-15 | koi-mcp + koi tests (ci.yml) | stdio `koi mcp serve` plus an in-process Streamable HTTP transport at /v1/mcp (token-authed on every method incl. the SSE GET; not in /openapi.json; --no-mcp-http gate; reported as the mcp_http status field). 11 tools + 4 resources (read = snapshot on both transports; resources/updated deltas on the in-process path). Tests: stdio session (tools + resources/list); an in-process HTTP session; and a real rmcp MCP client over a bound TCP listener through the production auth middleware (token required -> 11 tools + resources read; tokenless -> rejected); plus auth-carve-out and change-routing units. Tool results run against a mock source (live-core results are covered by the per-domain suites) |
| mcp discovery (_mcp._tcp announce + in-zone TXT + server-card) | koi http tests | 2026-06-15 | koi tests (ci.yml) | Exactly one _mcp._tcp mDNS record per host (never per service) + an in-zone _mcp.host.zone DNS TXT, both gated on the transport and withdrawn by the mDNS goodbye on shutdown; a public unauthenticated GET /.well-known/mcp/server-card.json discovery doc. Tests: server-card builder + the unauthenticated-GET guard. The single-record count is structural (a live count assertion belongs to the koi-mdns boundary harness) |
| acme (RFC 8555 facade over the CA) | koi-certmesh ACME suite + `koi-lab capability-story` ACME mini-act | 2026-07-20 | koi-certmesh + koi tests (ci.yml) + Brook/Granite physical gate | dns-01 only, EC/ES256 only, in-zone-only issuance. JWS verify is assembled from p256. The focused suite includes `instant-acme` conformance. The physical mini-act now closes the former mock seam: a real controller-side client orders through the deployed ACME TLS server, publishes proof through authenticated Koi DNS, a second host observes it with `dig`, the server validates through `AcmeDnsBridge → DnsCore`, exact cleanup preserves concurrent values, and the resulting chain/SAN verifies locally. EAB closed-mode rejects unbound new accounts, but the HMAC verification path remains unexercised. The listener uses an explicit aws-lc-rs provider, guarded by `build_tls_config_resolves_provider`. |

---

*Seeded by Epic E02 (2026-06-13). Every lane that touches a surface above updates its
row — `Last exercised` to today, `Guard` to the tripwire it left — before it leaves.*

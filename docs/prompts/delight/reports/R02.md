# R02 report — critical documentation truth

Status: **in progress; claim published before shared edits**

## Task and selected slice

Correct the high-consequence certificate recovery, name/SAN, route authorization,
revocation, runtime-certificate, ACME, and bootstrap-install advice identified by
R02. Pin the claims to current implementation and executable tests without changing
security policy to preserve stale prose.

## Starting revisions and dependency evidence

- Koi `dev`: `5557d5c6f046e1d05714321630f755d6630f76dc`.
- koi-desktop `main`: `2503ab2d3a417061d372092d8d515261778ebd3e`;
  clean and read-only for this task.
- R01 is `accepted/ready` at `5557d5c`; CONTRACT v1 records the exact ownership,
  route, authority, recovery, and client-trust distinctions consumed here.
- Bluefin's inherited OD-3 reservation is closed and exactly restored at `c613264`.
  No installed product or native state is reserved by this source-only task.

## Plan / exact owner and changed files

The claim owns these paths for this bounded iteration:

- `docs/prompts/delight/reports/R02.md` and
  `docs/prompts/delight/LEDGER.md` — claim, evidence, and final status;
- `docs/prompts/delight/CONTRACT.md` — required final R02 finding dispositions and
  handoff text, but **released from this claim after the post-push recheck** because
  Alpine's earlier R28 claim at `9291a28` owns this shared path. R02 will reacquire
  only its rows after R28 publishes or releases the file;
- `docs/reference/http-route-auth-matrix.md` — new authoritative route/auth table;
- `crates/koi-serve/src/http.rs` — tests that exercise the production DAT middleware
  against every matrix category and assert mounted-path coverage;
- `docs/guides/certmesh-ha-recovery.md`, `docs/tutorials/trusted-https.md`,
  `docs/guides/certmesh.md`, `docs/guides/acme.md`, `SECURITY.md`, and
  `docs/reference/security-model.md` — recovery, validity, SAN, client trust,
  ACME, authorization, and revocation truth;
- `README.md` and `docs/guides/runtime.md` — request-only container certificate
  metadata and planned secure-service boundary;
- `install.sh`, `install.ps1`, and `scripts/bootstrap-installers.test.mjs` — valid,
  artifact-qualified download-only next actions and stable/prerelease fixtures.

The dated assessment remains an immutable observation. Its D01/D02/D03/D06/D09/D10
rows will be reproduced or retired with current file/line and test evidence here and
in CONTRACT rather than rewritten as though its original snapshot never happened.

## Exploration / existing pattern

- `CertPolicy` in `crates/koi-certmesh/src/roster.rs` owns the 7/3/1 default;
  persisted rosters carry their actual policy. Existing validation/default tests are
  the policy oracle; no duplicate constant or type is needed.
- `IssuanceNames` in `crates/koi-certmesh/src/issuance_names.rs` owns hostname plus
  configured-zone FQDN SANs and already tests normalization/extras.
- `dat_auth_middleware` and its in-module request tests in
  `crates/koi-serve/src/http.rs` are the closest route/auth pattern. The new matrix
  check will send requests through that production middleware rather than compare
  prose strings alone.
- `scripts/release-manifest.test.mjs` supplies the temporary-fixture pattern for the
  bootstrap test. Both currently published `v0.9.0` and `v1.0.0-rc.2` sources parse
  explicit `--standalone`.

No new shared/domain/client types, protocol formats, or constants are planned. There
will be no `mdns-sd` import, cross-domain dependency, persistence access, or new
production Rust path. Installer copy stays co-located in the two bootstrap scripts.

## Verification plan

- focused `koi-certmesh` policy and issuance-name tests;
- focused `koi-serve` production-middleware route/auth matrix tests;
- installer stable/prerelease fixture test, `scripts/release-version.test.mjs`, and
  `scripts/release-manifest.test.mjs`;
- `sh -n install.sh`, ShellCheck, and PowerShell parse/execution checking through an
  available native-compatible tool or an explicitly recorded unavailable prerequisite;
- Markdown local links/source paths, `scripts/lint-surfaces.sh`,
  `scripts/check-doc-leaks.sh`, and `git diff --check`;
- CHARTER Rust gates if implementation extends beyond tests.

## Current status and next concrete action

The claim was published at `f08b4fc`. The required post-push recheck found Alpine's
earlier R28 claim at `9291a28`; its claimed `CONTRACT.md` write overlaps this plan.
R02 therefore releases that shared path, proceeds only on the remaining non-overlapping
files, and will fetch/reacquire its finding rows after R28 finishes. R02 remains
`in_progress/pending` until every required source-layer case passes; client/native
claims not exercised here will remain explicit.

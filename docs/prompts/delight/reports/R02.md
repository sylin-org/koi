# R02 report — critical documentation truth

Status: **accepted / ready; required CONTRACT dispositions published and released to Debian**

## Task and selected slice

Correct the certificate recovery, name/SAN, route authorization, revocation,
runtime-certificate, ACME, and bootstrap-install advice identified by R02. Pin the
high-consequence claims to current implementation and executable tests without
weakening policy to preserve stale prose.

## Starting revisions and dependency evidence

- Koi `dev`: `5557d5c6f046e1d05714321630f755d6630f76dc` at selection;
  the R02 claim was published as `f08b4fc`, then its overlapping CONTRACT claim was
  released at `f773936` after the mandatory post-push recheck.
- koi-desktop `main`: `2503ab2d3a417061d372092d8d515261778ebd3e`;
  clean and read-only for this task.
- R01 is `accepted/ready`. Its CONTRACT v1 ownership, route, authority, recovery,
  and client-trust distinctions are the starting contract.
- Bluefin's inherited OD-3 reservation was already closed and exactly restored at
  `c613264`. This source-only task did not change the installed service, firewall,
  provider, Pond state, or machine configuration.

## Plan / exact owner and changed files

R02 owns:

- this report and `docs/prompts/delight/LEDGER.md`;
- `docs/reference/http-route-auth-matrix.md` and the matching
  `crates/koi-serve/src/http.rs` middleware test;
- `docs/guides/certmesh-ha-recovery.md`, `docs/tutorials/trusted-https.md`,
  `docs/guides/certmesh.md`, `docs/guides/acme.md`, `SECURITY.md`, and
  `docs/reference/security-model.md`;
- `README.md` and `docs/guides/runtime.md`;
- `install.sh`, `install.ps1`, and `scripts/bootstrap-installers.test.mjs`.

`docs/prompts/delight/CONTRACT.md` was initially unavailable under R28's earlier
shared-file reservation. The coordinator released that reservation at `8f83e72` and
assigned Bluefin exactly the D01/D02/D03/D06/D09/D10 finding rows. This completion
edits only those rows and releases the shared file to Debian for R03.

## Before -> after

- D01 reproduced. The recovery guide's fixed 90/30/14 promise became guidance based
  on the persisted `policy` and earliest actual `cert_expires`. It states that new
  meshes default to 7/3/1 and that grace authorizes renewal but never extends TLS
  validity.
- D02 reproduced. Current issuance uses hostname plus configured-zone FQDN (default
  `.internal`), while v0.9.0 used `.local`. The tutorial now tells users to inspect
  `cert_sans`, use the configured resolver, and separately verify each chosen client
  trust source. Service aliases still need their own authorized SAN.
- D03 reproduced. SECURITY named full status as the public preflight and generalized
  DAT across enrollment. The new authority matrix records main HTTP, mTLS principal,
  ACME, and Pond listeners; full remote status requires DAT, while bootstrap/trust
  material and invite/TOTP enrollment retain narrow exceptions.
- D06 reproduced. Both installers printed a daemon-dependent command as “no daemon.”
  They now say the binary was downloaded without starting a service and print the
  installed full-path `--standalone mdns discover` command; service installation is
  a separate elevated action.
- D09 reproduced. README no longer says a container label certifies a workload, and
  the runtime guide states that `koi.certmesh` is parsed request metadata only.
- D10 reproduced. Certmesh, recovery, ACME, SECURITY, and security-model prose now
  distinguish signed-bundle enforcement by Koi-aware verification from ordinary TLS
  clients, which have neither Koi metadata consumption nor CRL/OCSP and can accept a
  leaf until its real `NotAfter`.

The dated assessment remains immutable evidence; it was not rewritten.

## Positive and negative acceptance results

- New-policy defaults, backward deserialization, stored-policy round trips, policy
  validation, configured-zone normalization, authorized extras, and self SANs pass.
- The production DAT middleware test passes public bootstrap/trust bundle, local full
  status, remote full-status rejection/acceptance, tokenless enrollment dispatch,
  mutation rejection/acceptance, all-method MCP protection, and Pond operator control.
- The real two-daemon suite passes public bootstrap, authenticated local management,
  negative unauthenticated mutation, invite-authorized tokenless join, remote mTLS
  principal MCP, and revocation closure.
- POSIX stable `v0.9.0` and prerelease `v1.0.0-rc.2` fixtures each install into a path
  containing a space and execute the exact printed full-path standalone action with
  no daemon. Release-version and release-manifest positive/negative fixtures pass.
- `install.ps1` parses in PowerShell LTS, and the printed call-operator/full-path
  standalone form executes against an isolated command fixture. No Windows native
  service or trust claim is made.
- README/runtime no longer imply workload-certificate injection. No second-client
  browser trust claim is accepted here; R22/client-verification owns that later native
  proof.

## Commands and exit/result evidence

- `cargo test -p koi-serve documented_route_auth_matrix_matches_production_middleware --lib`
  — 1 passed.
- `cargo test -p koi-certmesh --lib issuance_names::tests` — 3 passed.
- `cargo test -p koi-certmesh --lib roster::tests` — 14 passed.
- `KOI_NO_CREDENTIAL_STORE=1 cargo test -p koi-net --test two_daemon_certmesh --locked`
  — 4 passed.
- `toolbox run -c koi-dev node --test scripts/bootstrap-installers.test.mjs scripts/release-version.test.mjs scripts/release-manifest.test.mjs`
  — 11 passed.
- `sh -n install.sh`; ShellCheck stable container; PowerShell LTS parser and
  representative printed-action execution — passed.
- `KOI_NO_CREDENTIAL_STORE=1 cargo check --workspace --all-targets --locked` — passed.
- `KOI_NO_CREDENTIAL_STORE=1 cargo test --workspace --locked` — passed (platform-only
  ignored tests retained as reported by the harness).
- `KOI_NO_CREDENTIAL_STORE=1 cargo clippy --workspace --all-targets --locked -- -D warnings`
  and `cargo fmt --all --check` — passed.
- `scripts/lint-surfaces.sh docs/SURFACES.md`, `scripts/check-doc-leaks.sh`, and
  `git diff --check` — passed.
- Final CONTRACT completion on synchronized baseline `aa229ea`: local Markdown link
  targets resolved from the Bluefin development toolbox; the surface, leak, diff,
  and immutable-assessment guards passed. No production source changed, so the
  coordinator-directed prose tail retained the already-green implementation gates.

## Native artifact/peer/restoration evidence

No product artifact was installed and no physical peer or OS trust-store result is
claimed. The previously installed Bluefin service remained outside this source-only
work. The PowerShell check is portable source validation, not Windows physical proof.

## Docs / ADR / contract changes

The new route/auth reference is the single cross-listener matrix and SECURITY plus
the certmesh/tutorial guides link to it. No ADR, policy, protocol, shared type, or
production domain logic changed. CONTRACT now records the verified R02 dispositions
for D01/D02/D03/D06/D09/D10 and preserves the later owners for work beyond R02.

## Current status, dependency readiness and remaining cases

**Accepted / ready.** All four R02 acceptance cases, the applicable positive and
negative source/native-portable checks, and all six required CONTRACT dispositions
are complete on published implementation source `299ae89`. Windows native and named
second-client browser trust remain later R22/R29 evidence rather than R02 acceptance
cases, and this report does not claim them.

## Bluefin feedback to the coordinator on R28

Bluefin reviewed R28's published CI/validation boundary through exact source
`1f65a0f4bebd7b485914660c70f11f7123b4aa66`. It is compatible with R02 and includes
`scripts/bootstrap-installers.test.mjs` in the required client/release/frontend
contract job; that hosted job completed successfully. At `2026-09-05T04:59:30Z`,
Ubuntu, macOS, Clippy, MSRV, formatting, audit, surfaces, architecture, lean,
Windows GNU cross-check, and the contract job were green. The native Windows
workspace test was still running and the dependent cross-host job had not started.

R02 has no correction or objection for R28's source contract. The coordinator later
separated R28 acceptance from shared-file ownership at `8f83e72`; Bluefin consumed
that bounded handoff for only its six finding rows. R28's replacement hosted result
remains its owner's independent acceptance work and does not block R02.

## Next concrete action

Publish this acceptance record, fetch `origin/dev`, and verify the handoff remains
current. R02 then has no remaining work. The CONTRACT/LEDGER write reservation is
explicitly released to Debian for R03's bounded observation/D04 completion; do not
wait for or repeat R28's independent hosted reconciliation.

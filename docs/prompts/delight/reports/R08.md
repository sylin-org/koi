# R08 — Devices and comparison

## Final reconciliation — implemented / linux-ready, 2026-09-07 UTC

[Product CI34087132808](https://github.com/sylin-org/koi/actions/runs/34087132808)
passed **all14 jobs** on e3b11572d440f6dbda39333faea4e9479559cbf9, including
Ubuntu/macOS/Windows suites and cross-host CertMesh. Desktop062b0b5 pins that source.
The installed native evidence and exact artifact identities below remain unchanged;
this reconciliation requires no rebuild or reinstall. Final exact-pinned desktop
all-target strict Clippy also passes.

R08 is implemented and linux-ready. All required source, controlled comparison,
Linux native and hosted checks for this slice passed. Full acceptance remains open
for the explicitly listed Windows WebView2 cases below; the work order reserves the
physical two-peer demonstration for R29. R09 may consume this Linux-ready prerequisite
and must inherit those Windows cases. No R09 work starts in this invocation.

Next concrete action: execute R09 Settings/About and surface consolidation using
this device/comparison contract; reconcile the recorded Windows and physical peer
journeys in R29 before final acceptance. Browser access remains parked.

## Task and selected slice

Make Devices explain where catalog services run, and replace the manual Diff
inventory with “Compare what devices can see”. Browser access remains parked.

## Starting revisions and dependency evidence

Koi c76c3b4; desktop be834e8 (Koi pins 34e0d8c). R07 is accepted with all14
product CI jobs passing in34077834757 and installed native evidence in its report.
Only the local continuation.md was dirty; it is excluded from publication.

## Plan / exact owner and changed files

- koi-common/src/service.rs and koi-compose/src/catalog.rs: additive explicit local
  device identity, without parsing opaque IDs or guessing hostname correlation.
- koi-ui/src/devices.rs (new pure projection), screens/devices.rs, lib.rs, home.rs:
  device identity/counts, links to existing service details, peer selection and
  explicit comparison states. assets/refresh.js owns only transport/DOM mechanics.
- koi-client/src/comparison.rs (new HTTP reader): bounded read of the existing
  /v1/mdns/snapshot route, no local token on peer requests, no redirects.
- desktop src/comparison.rs (new native adapter) and src/ui.rs: one bounded in-memory
  comparison run, explicit action, concurrent local/peer reads, retained result.
  ui/index.html and ui/app.js move legacy Diff into the shared Devices panel.
- Controlled tests cover equal/different results, missing evidence, incompatible
  query coverage, timeout, refused authorization, and stable device/service links.

Scope is each observer’s active mDNS .local query coverage. Physical interface and
same-network equivalence are not exposed by this API and will not be invented.
Unknown/unavailable coverage is incomplete; equal query coverage permits a bounded
snapshot comparison, never a claim that the whole network or reachability agrees.

## Current status and next concrete action

Implementation in progress. Run affected behavioral checks, publish a coherent
milestone and run relevant CI. Exercise the installed native device/compare journey
under a fresh own-host rollback guard. Physical two-peer demonstration stays R29;
Windows native verification remains explicitly unverified.

## Source verification milestone

Implemented the planned catalog/device projection, native comparison action and
bounded HTTP reader. Removed legacy Diff, its manual node UI and unrestricted
`daemon_get` command. The existing discovery API and auth policy are unchanged.
The renderer API disables comparison actions when a native adapter is absent.

Passed local behavioral checks: 50 koi-client tests;80 koi-compose unit plus3
composition integration tests;25 shared UI integration tests;5 affected common
schema cases. Additional focused observer/empty-catalog assertion passes. Desktop
passed31 Rust tests and46 Node cases using local Koi dependency patches for source
verification. Affected all-target strict Clippy passes in both repositories.
Formatting, diff, surface and documentation leak checks pass.

An isolated Chromium fixture exercises the actual refresh/submit handler: POST
contains only the selected peer ID, renders comparing and finished responses, and
preserves focus in Devices when Home contains the same service. Existing draft,
caret, disclosure, stale-link and narrow-panel mechanics still pass. This is
component evidence, not installed native or physical peer acceptance.

Native packaging/installation and complete product CI remain pending. Desktop
publication will replace local dependency patches with exact published Git pins.

## Installed focus correction

The first normal installation used daemon d9fe736 and desktop cd8564b. Device
identity and the no-peer state rendered correctly, but real keyboard inspection
showed that refresh could move focus off a device disclosure. Device summaries
now have stable focus IDs; the actual DOM regression check preserves them across
refresh. This computer is sorted first and the one-service count is grammatical.
Only the affected renderer checks were repeated. Native desktop verification will
repeat with the corrected package; daemon catalog behavior is unchanged by this fix.
The earlier product CI34086438041 is superseded by the correction's final CI.

## Installed native evidence — 2026-09-07 UTC

Final workbench: desktop062b0b5580a37b65c4f468549ed59525a369f39f, exact UI/client
pin e3b11572d440f6dbda39333faea4e9479559cbf9, package
`0.1.3.r78.g062b0b5-1`. Package SHA-256
`4b2b120f7fa3756520c26956890aa50ae9d26843b9d50321e2e0dbb26176d078`;
installed desktop SHA-256
`9614c70a840b9a320d584f2de2a51f0ee8c78189bb5268499766019b553f40e3`, PID119042.
Its exact pinned Rust suite passes31 cases and all-target strict Clippy passes.
The Windows GNU all-target check passed before the pure UI correction.
The corrected renderer's six device cases, strict Clippy and isolated Chromium
transport/focus regression checks pass.

Daemon remains source d9fe7365c24030c978bd9964817df436adb7a697, PID111468,
SHA-256 `696e5498c5f2deed2c160a8e2077e195cfc4c116b587d6fa3c19fd76743b9fc6`.
The correction only changes shared presentation, so its retry replaces the desktop
package without another daemon restart. Composition/client/common source is identical
between those Koi revisions. Both were built through clean source/normal release
paths; package archive ownership/modes and installed binary equality were checked.
The package recipe's source-path warning remains; no package test failure occurred.
The already-passed exact Rust suite supplied the package check instead of repeating it.

Fresh root-private `/var/tmp/koi-r08.0ozTjP2K` retained the previous daemon/package,
policy and identity hashes. Checksum-verified rollback helper and45-minute timer
preceded the first mutation. The correction rechecked that still-active guard,
current artifact/PIDs and all captured hashes under its root lock, preserving the
first R08 package before replacement. Guard syntax passed; full rollback was not
exercised. No certificate, identity, firewall, resolver or unit change occurred.

Observed in the actual installed GTK/WebKit workbench:

- This device is first and expands by native keyboard. Its disclosure retains both
  focus and expanded state through multiple automatic refreshes; viewed captures
  verify the normal1100×748 window (client1100×720).
- Choosing its hosted MCP from Devices navigates to the existing service identity
  `svc_1504a81eaa4cf04c45bfb481e572d865` and the corresponding API endpoint in Home.
  The local catalog explicitly identifies `dev_4eb6ba00d4743fbafd48511eefd89f4a`;
  an authenticated local read verified that grouping and the readable mDNS snapshot.
- Advanced tools → Status → Compare what devices can see opens the shared panel.
  There is no legacy Diff tab. With no eligible current peer, it says Not configured
  and asks to connect a Koi device; it never reports agreement. That screen was viewed.
- Two same-named device/service destinations and equal/different/refused/timeout/
  incompatible-scope comparisons have controlled Rust/HTTP/DOM evidence only.
  No peer was mutated or remotely probed; physical two-peer proof remains R29.
  This is not new Windows or320px native proof.

The sole root service remains healthy/enabled on loopback5641;5644/5645/18707 are
closed. After the targeted native checks, the deployment was accepted under the root
lock. The timer stopped (zero matching timers), root executable helper/run marker
and volatile privilege wrapper were removed, and backup executables are0600.
The workbench is left on Devices. Native acceptance is complete for this slice;
final full product CI34087132808 remains pending at this checkpoint.

## Remaining platform acceptance

R08 will be **implemented / linux-ready**, not fully accepted, once final CI passes.
The new Windows WebView2 journey has build/API compatibility evidence but no
physical Windows run. R29 must exercise Devices expansion/focus through refresh,
service selection to the correct endpoint, diagnostics return to
`http://koi-ui.localhost/#comparison`, and the bounded native comparison POST with
its loading/error/result states. These Windows cases must be inherited by R09 and
reconciled before final acceptance. R08 explicitly reserves the real two-peer
comparison demonstration for R29; it is not claimed by controlled fixtures.

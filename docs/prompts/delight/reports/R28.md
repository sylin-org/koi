# R28 report — CI and release contracts

Task and selected slice: **R28 — make routine CI validate the actual development
and candidate tree**.

## Starting revisions and dependency evidence

- Koi `dev`: `5557d5c6f046e1d05714321630f755d6630f76dc`.
- R01 is `accepted/ready`; its published report authorizes Alpine to claim R28.
- The worktree was clean and matched `origin/dev` at claim time.
- Current CI runs on `main` only. The manual release build validates artifacts but
  inherits `contents: write`, emits no joined candidate result, and ordinary CI does
  not run the language-client or release-contract suites. `publish.yml` is also
  directly dispatchable, so validation and irreversible publication are not fully
  separated.

## Plan / exact owner and changed files

The existing `release.yml` manual build-only path is the closest repository pattern:
it already builds the six supported targets and exercises manifest/npm consumers
without entering tag-gated publication jobs. R28 will strengthen that path instead
of creating a parallel artifact pipeline.

- `.github/workflows/ci.yml` — run exact-tree checks for `dev` and PRs; retain the
  existing Rust, formatting, MSRV, architecture/lean and supply-chain gates while
  making client, launcher, manifest, version and current dashboard coverage visible.
- `.github/workflows/release.yml` — make validation read-only by default, run the
  shared contract checks, retain write permission only on tag-only publishing jobs,
  preserve the six-target build, and emit a candidate validation summary artifact.
- `.github/workflows/publish.yml` — remove independent manual publication and make
  the reusable publishing workflow read-only except for its explicit registry secret.
- `scripts/candidate-validation.mjs` and
  `scripts/candidate-validation.test.mjs` — own schema-1 candidate evidence assembly,
  exact source/manifest identity, required hosted-result enforcement, pending native
  evidence, and positive/negative fixtures.
- `crates/koi-client/src/lib.rs` — make the ephemeral raw-HTTP fixture consume each
  declared request body before responding when hosted macOS exposes the race; product
  client behavior remains unchanged and the duplicated error fixture is removed.
- `crates/koi-embedded/src/handle.rs` — apply the same body-complete rule to the
  embedded facade's daemon fixtures and consolidate the three duplicated JSON stubs.
- `crates/koi-serve/src/local_ipc/unix.rs` — replace a hosted-Linux test assumption
  that a just-unlinked socket inode cannot be reused with direct live-listener and
  owned-path identity checks; production generation fencing remains unchanged.
- `crates/koi-compose/src/cores.rs` — give the Windows shutdown-cancellation fixture
  a short injected transaction deadline and a separate observer margin; production
  shutdown timing and ownership remain unchanged.
- `crates/koi-serve/src/pond.rs` — leave the two-second stalled-client drain contract
  unchanged while giving its test observer a separate eight-second margin.
- `crates/koi-serve/src/pond_ui.rs` — reopen an already-visible immutable generation
  with a write-capable handle before re-flushing it, as required by Windows, while
  preserving the idempotent persistence and durability contract.
- `crates/koi/tests/named_pipe_ipc.rs` — isolate the Windows pipe integration daemon
  to the mDNS+IPC capabilities under test, remove unrelated fixed/listener ports,
  allow a bounded thirty-second startup, and surface early child stderr.
- `scripts/release-version.test.mjs` — protect the validation/publication boundary.
- `CONTRIBUTING.md` and
  `docs/adr/025-release-channels-and-bootstrap-contract.md` — document exact local
  contributor commands, candidate invocation/result format and non-publishing scope.
- `docs/prompts/delight/CONTRACT.md`, `docs/prompts/delight/LEDGER.md`, and this report
  — record D05 disposition, implementation evidence and remaining hosted/native work.

No product domain model or Rust type changes are planned. The required constant/type
searches found existing domain-owned models; the requested `koi-mdns/src/protocol/`
directory has moved to the single `crates/koi-mdns/src/protocol.rs` module and neither
location is an owner for CI evidence.

## Before -> after

Before: `dev` can be untested by hosted CI, manual candidate validation has broad
default permission and no durable joined verdict, and required user-facing contracts
are split across optional commands.

After: pushes and PRs identify their exact checked SHA; ordinary CI runs
the actual contract surfaces; a manual candidate build emits one deterministic JSON
summary joining its source, artifact manifest, hosted run and explicitly pending
native evidence; any missing/skipped/failed required hosted job rejects the hosted
verdict; only a tag-push event can reach explicitly permissioned publishing jobs.

## Positive and negative acceptance results

The implementation spans `fce2d58` through `a3b2700`, including concurrent R02/R03
changes already on `dev`; replacement exact-SHA hosted validation is pending. The
schema tests passed
with exact source/manifest/run binding and pending native evidence. Missing, skipped
and failed `channels` results each produced a failed infrastructure verdict; a
six-target manifest with one target removed failed, while the corrected manifest
passed. Wrong versions, changed manifest bytes, missing npm carriers, client HTTP
errors and bootstrap token-boundary violations are covered by the existing negative
fixtures that now run in both ordinary and candidate CI.

The first [hosted run](https://github.com/sylin-org/koi/actions/runs/33935920134)
exposed two macOS client fixture races after the required platform-independent jobs
passed; `2e8db7b` made those fixtures drain declared request bodies. The next
[hosted run](https://github.com/sylin-org/koi/actions/runs/33936529934) proved all 43
client tests green on macOS, then exposed the same defect in an embedded-facade stub;
`ec041e5` consolidates and corrects those fixtures without skipping either test.
The third [hosted run](https://github.com/sylin-org/koi/actions/runs/33937417963)
proved those macOS paths green, then caught an unrelated Linux filesystem assumption:
ext4 may immediately reuse an unlinked Unix-socket inode. `ac72d54` now checks the
recovered listener's actual ownership and connectivity instead of requiring unequal
generation numbers; the exact-identity cleanup behavior is unchanged.
The fourth [hosted run](https://github.com/sylin-org/koi/actions/runs/33938430611)
proved macOS and Ubuntu green, then caught a Windows timing race where a two-second
shutdown transaction and its observer shared the same deadline. `6b94658` uses a
250-millisecond injected transaction deadline beneath the unchanged two-second
observer, preserving cancellation-ownership coverage without a boundary race.
The fifth [hosted run](https://github.com/sylin-org/koi/actions/runs/33939647138)
proved that correction plus macOS/Ubuntu green, then reached a newly added Windows
named-pipe integration test that no prior successful `main` run had exercised. Its
opaque five-second daemon wait failed. `7e5ed96` removes unrelated HTTP, certmesh,
DNS and port acquisition from that mDNS+IPC fixture and reports any early child exit
with stderr rather than hiding the cause.
The sixth [hosted run](https://github.com/sylin-org/koi/actions/runs/33941122124)
then proved the isolated daemon remained live for ten seconds, confirming that the
harness expired before the control plane's own documented 20-second initial
reconciliation ceiling. `27972c9` uses a 30-second harness bound.
The seventh [hosted run](https://github.com/sylin-org/koi/actions/runs/33942132091)
proved the pipe integration passed and then exposed four Windows access-denied
failures in idempotent `koi-serve` persistence paths, while a stalled-client observer
also hit its outer deadline. `7dda53d` temporarily serialized the complete Windows
suite as a diagnostic and separated the test observer's eight-second ceiling from
the unchanged two-second product drain. The eighth
[hosted run](https://github.com/sylin-org/koi/actions/runs/33943493070) proved the
observer correction passed but the same four access-denied failures remained under
serialization, ruling out runner-wide fixture contention. The exact cause was an
existing generation opened read-only immediately before `sync_all`; Windows requires
a write-capable handle for that flush. Peer-published `7344db3` corrects the product
persistence path, and `a3b2700` restores parallel Windows test execution after the
diagnostic run.

The release workflow's top-level token is read-only. `release`, `docker`,
`publish-crates`, and `publish-npm` require both a `push` event and a tag ref before
their individually scoped write or registry paths can run. `publish.yml` is reusable
only and has no `workflow_dispatch`, so selecting a tag in a manual validation run
cannot publish. No source-only result is reported as native acceptance.

## Commands and exit/result evidence

- `node --test packages/ts/test/client.test.js packages/npm/test/launcher.test.js
  scripts/bootstrap-installers.test.mjs scripts/release-version.test.mjs
  scripts/release-manifest.test.mjs scripts/candidate-validation.test.mjs`: exit 0;
  30 passed, 0 failed/skipped.
- `PYTHONPATH=packages/python/src python3 -m unittest discover -s
  packages/python/tests -v`: exit 0; 7 passed and the existing optional
  `cryptography` interoperability case skipped because that package is absent on
  this host. The Rust CA/TypeScript CSR cross-language test passed in the full suite.
- `actionlint` 1.7.12 and `npx --yes yaml-lint` over `ci.yml`, `qa.yml`,
  `release.yml`, and `publish.yml`: exit 0.
- `cargo check --workspace --all-targets --locked`: exit 0.
- `cargo test --workspace --locked`: exit 0; all enabled workspace and doctests
  passed. Existing explicitly environment-dependent Avahi/resolved/native mDNS,
  Docker and doc-generation cases remained ignored; no skip was added by R28.
- `cargo clippy --workspace --all-targets --locked -- -D warnings` and
  `cargo fmt --all --check`: exit 0.
- Focused `cargo test --locked -p koi-common --test architecture` (15 passed) and
  `cargo test --locked -p koi-dashboard` (38 passed): exit 0.
- Both affected raw-HTTP cases in `koi-client` passed five repeated runs and its full
  43-test library suite passed. Both affected `koi-embedded` POST cases passed five
  repeated runs, its full 112-test library suite passed, and strict all-target Clippy
  passed for each affected crate.
- The corrected Unix-socket recovery test passed 20 repeated runs; the full 152-test
  `koi-serve` library suite and its strict all-target Clippy check passed.
- The corrected shutdown-cancellation test passed 20 repeated runs; the full 66-test
  `koi-compose` library suite and its strict all-target Clippy check passed.
- The Windows named-pipe test target passed GNU cross-compilation and strict Clippy
  after installing the local MinGW compiler; execution remains hosted-Windows proof.
- The widened stalled-client observer passed five repeated runs; actionlint, the
  release workflow contract guard, formatting and diff checks passed.
- Each of the four affected idempotent-generation retry tests passed five repeated
  runs after the write-capable re-flush correction; the full 152-test `koi-serve`
  library suite and strict all-target Clippy passed on Linux, and the Windows GNU
  target passed strict all-target Clippy. Actionlint also passed after parallel
  Windows execution was restored.
- `scripts/check-lean-embedded.sh`, `scripts/check-publish-list.sh`,
  `scripts/lint-surfaces.sh docs/SURFACES.md`, `scripts/check-doc-leaks.sh`, and
  `git diff --check`: exit 0.
- `cargo-audit` is unavailable on this Alpine host; the unchanged required hosted
  audit job remains in CI. No local audit result is claimed.

## Native artifact/peer/restoration evidence (if required)

No Koi service, fleet state or native candidate was mutated, and no native candidate
run was performed. R29 owns fresh exact-artifact native links and the final cross-check.
The generated schema contains pending entries for Windows, CachyOS, Bluefin, Alpine
and Debian and qualifies both macOS targets as build-only/unverified until such
evidence exists.

## Docs / ADR / contract changes

ADR-025 remains the release-channel owner and now specifies the exact schema-1 report,
required jobs, evidence binding, preview qualification and validation/publication
boundary. `CONTRIBUTING.md` carries the commands CI actually runs. CONTRACT.md records
D05 as infrastructure-bound by R28 and acceptance-owned by R29; release identity and
artifact topology are unchanged.

## Current status, dependency readiness and remaining Windows/external cases

**Implemented / corrected Windows persistence fix pending hosted validation.** The
claim is published at `9291a28`; the implementation and evidence report are published
through exact source `1f65a0f4bebd7b485914660c70f11f7123b4aa66`. The prior
[exact-SHA CI run](https://github.com/sylin-org/koi/actions/runs/33943493070) supplied
the diagnostic evidence for that correction. At handoff, replacement
[exact-SHA CI run 33945321135](https://github.com/sylin-org/koi/actions/runs/33945321135)
has ten completed successful jobs: architecture, MSRV, strict Clippy, Windows GNU
cross-check, macOS workspace tests, client/release/frontend contracts, formatting,
surfaces, lean-embedded, and audit. Ubuntu and Windows workspace tests remain in
progress; the required aggregate job has not yet run. This partial result is not
claimed as acceptance. Windows/macOS builds in candidate validation are build
evidence only; Windows physical and all final native journey evidence remain R29
obligations, and macOS remains explicitly unverified.

## Next concrete action

CachyOS should inspect exact-SHA CI run 33945321135 through completion and promote
R28 only when every required job, including cross-host, succeeds. A failure requires
correction and replacement evidence. Do not dispatch the `Release` workflow for R28.

## Coordinator handoff — 2026-09-05

At the owner's instruction to proceed, CachyOS separates shared-file ownership from
task acceptance. All R28 source changes are published; the remaining hosted job does
not write CONTRACT.md. The blanket CONTRACT/LEDGER reservation is released now to the
bounded order published in LEDGER.md: Bluefin's six R02 finding rows, then Debian's
R03 observation/D04 rows. This supersedes this report's earlier instruction to retain
the reservation until CI finishes. R28 remains implemented/pending, and every
required acceptance check is retained.

The inspected run at source `1f65a0f4bebd7b485914660c70f11f7123b4aa66` completed with
all twelve preceding jobs green, including Windows, macOS and Ubuntu workspace tests,
but cross-host failed. Its `certmesh create` returned `machine ID unavailable` because
the Alpine fixture provides neither `/etc/machine-id` nor the dbus fallback while
explicitly selecting the machine-bound vault with `KOI_NO_CREDENTIAL_STORE=1`.

### CachyOS bounded correction claim

Starting source: `6b35f0c3cfdc863505affa51d8217f479b3061ba` plus this coordinator
handoff. CachyOS owns only `docker/cross-host/docker-compose.yml`, new fixture files
`docker/cross-host/node-a.machine-id` and `node-b.machine-id`, and
`scripts/cross-host-certmesh.sh`, plus this report, its own journal and the R28
ledger status. These paths do not overlap R02/R03's contract edits. Alpine retains
the parent R28 source assignment and has delegated final reconciliation here.

Exploration: the driver owns the isolated container lifecycle; Compose owns its two
host fixtures; the Dockerfile deliberately omits a keychain; `koi-crypto::vault`
already reads the standard machine-ID paths and rejects unavailable identity; the
CI workflow invokes this driver. Existing constant/type searches across binary,
common, mDNS protocol, client and domain crates require no new Rust type or constant.
The protocol module is `protocol.rs`, replacing the old directory in the skill.

Plan: follow the existing distinct hostname/IP fixture pattern with two distinct
synthetic 32-hex machine IDs mounted read-only at `/etc/machine-id`. Add an in-container
preflight that requires both IDs to be valid and different before CA creation. Keep
the production vault fail-closed. No domain dependencies, shared types, mDNS imports,
protocol schemas or production storage paths change. The fixture IDs belong beside
their Compose consumers and are non-secret test data.

Verification: shell syntax, fixture shape/distinctness and read-only Compose mounts,
documentation guards, then the actual cross-host exchange in hosted CI on the new
published SHA. This CachyOS host has no Docker/Podman executable; local static checks
cannot claim the container result. R28 remains pending until replacement complete
hosted evidence passes. Any further shared-file need requires a new bounded claim;
this correction does not reclaim CONTRACT.md.

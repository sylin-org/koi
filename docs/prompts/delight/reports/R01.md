# R01 report — product contract and campaign handover

Status: **implemented/readiness pending; contract written, inherited OD-3 run active**

## Starting revisions and dependency evidence

- Koi `dev`: `e2c4a8266ff07ffd85846b672e1be84b5fbc1e42`.
- koi-desktop `main`: `4c05ed22dfdc2c11fd220eb3a65a64abad05e299`.
- R01 has no source predecessor. Acceptance remains gated on the actual Epic 002
  OD-3 collector verdicts and Bluefin restoration, not its scheduled end time.
- At 2026-09-04 22:36 UTC, Bluefin's exact frozen service/Pond peer was healthy;
  Alpine's real OpenRC collector was running after its scheduled PID transition;
  all four installed peers were visible through Koi mDNS. No Epic 003 product
  deployment was started.

## Plan and exact owners

This is the published claim required before shared contract edits. The selected
write paths are:

- `docs/adr/044-service-experience-ownership.md` — durable architectural decision;
- `docs/prompts/delight/CONTRACT.md` — exact type/module/route and migration contract;
- `docs/prompts/delight/reports/R01.md` — evidence and handoff;
- `docs/prompts/delight/LEDGER.md` and `fleet/epics/003-delight-realignment.md` —
  status only when the contract and inherited handover are actually satisfied.

No production or koi-desktop source is in this claim. R01 will map existing domain
facades/status feeds and establish later target paths without implementing them.
It will keep domain mutation in the owning facade, cross-domain projection and
coordination in `koi-compose`, HTTP/IPC transport in `koi-serve`, and user language
in the shared presentation boundary. It will choose one owner each for service
catalog truth, preferences, share intent/receipts, diagnosis, and secure setup;
define stable identity/correlation, errors, revisions, retention, authorization,
rollback, and migration; and include the required serialized examples.

## Verification plan

- Compare ADR-044 against ADR-040, ADR-042, ADR-043, and STACK-0001.
- Resolve every named current and proposed path with `rg`/`test`, labeling proposed
  paths rather than pretending they already exist.
- Run Markdown local-link/source-reference checks, `git diff --check`,
  `scripts/lint-surfaces.sh`, and `scripts/check-doc-leaks.sh` where applicable.
- Reconcile Windows, Alpine, Debian, and Bluefin canonical OD-3 evidence after the
  active collectors finish. R01 remains `in_progress/pending` until that succeeds.

## Current next action

ADR-044 and CONTRACT.md now define the exact ownership, identity/correlation,
authorization, retention, migration, sharing, diagnosis, and secure-service paths.
No product source, installed artifact, service, provider, firewall, trust store, or
peer surface changed.

Documentation verification completed while the inherited run remained active:

- all existing paths named by the contract were resolved with `rg`/`test`; every
  future path is labeled proposed and assigned to one R task;
- the contract was reviewed against ADR-040, ADR-042, ADR-043, STACK-0001, current
  `KoiStatus`, orchestration, persistence, HTTP/auth, mDNS, DNS, Health, Proxy,
  CertMesh, Runtime, client, and desktop watched-state sources;
- the old June charter conflict is resolved explicitly: task-oriented commands may
  change, durable user state must migrate, frozen protocol identity remains stable,
  and the active fleet dispatch needs no repeated routine plan approval;
- serialized examples cover duplicate advertisements, uncertain identity, an absent
  favorite, publication pending peer proof, forbidden remote mutation, process
  restart, and unknown schema handling.

At `2026-09-04T18:44:28-04:00`, the coordinator's read-only host audit found the
frozen `/usr/local/bin/koi` still byte-exact at SHA-256
`b2079cd3bb2e35a46f0344b8181b92e302b33681c12104124dadf4026d54cdc3`, with exactly
one active+enabled systemd daemon PID `1249009`, `NRestarts=0`, and the installed
workbench/SNI present. Loopback `/healthz` returned `OK`; aggregate revision `24733`
reported every required domain healthy, with only the known optional runtime and
disabled Pond unhealthy. This is a stable coordinator observation, not a substitute
for the three remote collector verdicts.

Commands/results: `git diff --check` passed; every local Markdown link in the three
R01 documents resolved; `scripts/lint-surfaces.sh` passed 30 rows; and
`scripts/check-doc-leaks.sh` found no downstream-consumer names. Koi source-only
documentation required no product build or install. The sibling desktop checkout was
clean at the recorded `4c05ed22` revision.

R01 cannot yet be accepted. After the three canonical six-hour collector reports and
Bluefin's exact Pond/firewall restoration land, CachyOS must reconcile their source,
artifact, PID/restart, traffic, resource, and cleanup evidence; close or precisely
redispatch Epic 002; then update the activation record, both epic statuses, this
report, and LEDGER.md. Only that publication may set `Campaign: active`.

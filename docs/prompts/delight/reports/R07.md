# R07 — Home launchpad

## Current bounded iteration / exploration and exact claim

R05 and R06 are accepted; R06 reconciliation 06eb206 records original gates and
the exact d948015 Windows/Alpine correction passes. Starting Koi 06eb206 and desktop
d432b9e are clean. CachyOS alone owns implementation. No peer or installed mutation.

**Task:** Begin R07 with pure shared Rust search/selection/action projection and
safe native external opening. This is one coherent foundation iteration, not a
claim that the entire interactive Home/reconnect journey is complete.

**Files read:**

- `crates/koi-common/src/service.rs`: schema-1 catalog, IDs, favorites, observations,
  endpoint/browser evidence and available actions already exist.
- `crates/koi-compose/src/catalog.rs`: domain projection alone declares current
  conditions and Open; UI must only narrow, never invent those actions.
- `crates/koi-ui/src/{lib.rs,screens/home.rs,components/service_row.rs}`: existing
  pure rendering and family rows; no client, domain or credential ownership.
- `crates/koi-ui/tests/render.rs`: escaped hostile data and declared-condition tests.
- Desktop `src/main.rs`: existing `open_url` uses Windows `cmd /C start`; an HTTP
  prefix check is not a shell argument boundary. Existing Linux/macOS use direct
  single URL arguments. This first slice replaces the shell path before new links.

Also read R07, charter, accepted R06 contract/architecture and UX direction. Exact
constant/type searches covered binary, common, client, mDNS `protocol.rs`, DNS,
Health, Proxy and Certmesh. No new shared wire/domain type is needed. Existing
workspace `url = "2"` supplies parsing; existing windows-sys 0.61.2 supplies native
ShellExecuteW. No independent browser catalog model or port-based URL inference.

**Creating new / exact write paths:**

| Code | Path | Reason |
|---|---|---|
| HomeQuery/HomeProjection, bounded attention, validated BrowserDestination | `crates/koi-ui/src/home.rs` | Pure adapter-independent UI projection; borrows authoritative catalog |
| Search, selection, absence and hostile URL tests | `crates/koi-ui/tests/home.rs` | Meaningful shared behavior tests |
| Module/dependency/docs wiring | `crates/koi-ui/{src/lib.rs,Cargo.toml,README.md}`, root `Cargo.lock` | Reuse existing URL parser, no new runtime framework |
| Validated native opener and launch seam tests | Desktop `src/external.rs` | Thin native boundary, not UI/domain state |
| Command/dependency/docs wiring | Desktop `src/main.rs`, `Cargo.toml`, `Cargo.lock`, `README.md` | Replace existing opener and pin exact shared/client revision together |
| Product archive pin | Desktop `packaging/alpine/APKBUILD` | Preserve reproducible build of published source |

Documentation: this report, `docs/prompts/delight/{LEDGER.md,CONTRACT.md}`,
`docs/SURFACES.md`, owned `fleet/cachyos-linux/journal.md`. All claims precede
production edits; peer namespaces stay untouched.

**Reusing / pattern:** CatalogSnapshot/Service/Endpoint and stable ServiceId,
existing pure koi-ui components and URL parser, existing platform launch mechanisms.
Search matches all query words across name/alias, exact associated device names/
addresses, endpoint host and category. Ordering uses stable IDs; selection is by ID,
not current row position; absent favorites remain catalog-owned. Attention is
bounded, with a total count instead of an unbounded feed.

**Risks / guards:** Only explicit Open + browser-usable HTTP(S) evidence can yield
a destination. API-only/absent/stale/ambiguous services, wildcard hosts, credentials,
control characters, backslashes, host/path confusion and unsafe schemes fail closed.
URL construction must keep authority separate from path/query. A validated URL is
not proof of reachability or client TLS trust. Windows uses ShellExecuteW with a
URL as lpFile and no command parameters, never cmd/start. Tests use a launch seam,
not the real user browser. No mdns-sd import, domain duplication, upward dependency,
new protocol schema or centralized constants. The charter supersedes the old
explore plan-approval pause under the user's proceed instruction.

## Verification plan (not results)

Focused pure behavior tests, existing renderer/architecture tests, full workspace
locked check/test/strict Clippy/format and relevant lean/publish/surface/doc guards.
Desktop locked check/test/strict Clippy/format/Node tests; Windows-target compilation
where available, otherwise explicitly pending. Exact product/shared/recipe pins
published together. No new native acceptance or peer request is claimed by this
foundation iteration. Later live Home wiring must receive its own native requests.

## Remaining R07 work

Wire search/favorite controls and details to shared rendering on native and
authenticated browser adapters; preserve query/selection across automatic updates
and reconnect, with honest freshness and distinct empty/no-match/unavailable states.
Use the validated destination/native opener for real inspectable links. Exercise
keyboard/pointer/live Notes/search/open/details at wide and 320px layouts, persist
stopped favorites, and prove automatic interrupted-feed recovery. Update Home usage.

Status: **in progress**, not implemented/accepted; R08/R10 remain dependency-gated.

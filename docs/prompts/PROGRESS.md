# Koi Prompt Stash — Progress Ledger

Tracks execution of the work orders in this directory (`P01`–`P13`). Agents:
update your row when you **start** (status `in-progress`) and when you **finish**
(`done`, `blocked`, or `obsolete`, with a one- or two-line note). Link commits by
short SHA. Uncommitted working-tree work is noted as `(wt)`.

See [README.md](README.md) for the prompt catalog and sequencing, and
[CHARTER.md](CHARTER.md) for the session protocol (research → plan → implement →
verify). Every prompt must leave a plan file at `plans/<id>-plan.md`.

| ID | Status | Date | Agent/model | Commits | Notes |
|---|---|---|---|---|---|
| P01 | done | 2026-06-11 | prior session + opus-4-8 | 221d077, 83867e4, (wt) | Docs deep-sweep: security-model.md linked from 6 docs, ADR hygiene (011→Accepted, 012 Block-4 hashes, 001–010 retro headers, dup §7.7), envelope-encryption/system/architecture/MSRV/profile tables matched to code, MSRV drift-guard test (cli.rs:1465). Gap-patch (wt): 2 tokenless CONTAINERS.md `announce` examples now carry `x-koi-token`. |
| P02 | done | 2026-06-11 | prior session + opus-4-8 | a0e6a99, 53ff67c, cf5376d, 221d077, (wt) | Version reset to 0.3.0 (=0.3.0 internal pins), version.json deleted, build.ps1 stops mutating; qa.yml `.ps1`-only; release.yml tag-only; publish.yml repaired (pipefail, 15-crate ordered list, crates.io verify) + `workflow_dispatch`-only; CI gains MSRV/cargo-audit/Dependabot; CHANGELOG seeded; README install → GitHub Releases. Gap-patch (wt): release.yml no longer reads deleted version.json (CHANGELOG extraction); build.ps1 docstring corrected. |
| P03 | done | 2026-06-13 | opus-4-8 | (wt) | `--http-bind` (loopback/bridge/`<ip>`/0.0.0.0 + `KOI_HTTP_BIND`) with startup warning UX, `koi status` `Bind:`/`http_bind`, and breadcrumb surfacing; `koi token show\|write` (tty-guarded show, 0600 write) reading the breadcrumb; Windows firewall opens the HTTP port only when exposed; both main.rs and the Windows service path wired. Tests: 7 bind/breadcrumb unit + 4 DAT-auth router (GET exempt, POST 401/200). Docs: CONTAINERS.md (banner→exposure + token/compose-secrets recipe), README, security-model.md, http-api.md, cli.md, agentic refs. Verify green: check/clippy -D/157 tests/fmt. **Live-validated** on Debian 13 with a real `docker0` bridge (cross-built musl binary): 23/23 checks incl. bridge resolution → 172.17.0.1, `koi status`/breadcrumb surfacing, token tty-refusal + 0600, and the headline **container→daemon over 172.17.0.1** (GET unauth ok, tokened POST 201, tokenless POST 401). Plan at [plans/P03-plan.md](plans/P03-plan.md). Prior broken attempt reverted (divergence log). |
| P04 | done | 2026-06-13 | opus-4-8 | 7bb0d24 | TLS-terminating passthrough replaces the broken HTTP forwarder. Fixes all 3 defects in claim 1 (axum-0.8 route panic, notify-thread cert-watch panic, no-WebSocket) structurally: `copy_bidirectional` passthrough, `ResolvesServerCert` hot-reload via notify→tokio bridge, real `watch`-channel listener state (`running`/`error`+detail), self-signed fallback. Breaking `ProxyStatus` shape propagated to dashboard/CLI/OpenAPI; `/v1/proxy/*` paths + facade unchanged. 5 in-crate integration tests (no Docker). Verify green: check/clippy -D/fmt; koi-proxy 17, workspace + certmesh 264 (single-threaded). **Caveat:** acceptance criterion 7 (smaller crate / <954 src excl tests) **not met** — prod grew ~871→~1117 because correct cert-resolution+hot-reload+self-signed+state cost more than the broken forwarder; kept charter-non-negotiable DX (zero-config self-signed) + correctness over the size target and surfaced it (see divergence). Plan at [plans/P04-plan.md](plans/P04-plan.md). |
| P05 | done | 2026-06-13 | opus-4-8 | 6281b0f→7a9570e (squashed to dev) | mDNS browse multiplexing rebuilt: one real browse per type in `MdnsDaemon`, reference-counted `broadcast` fan-out to N `BrowseSubscription`s (replaces `BrowseHandle`). Concurrent subscriptions are independent; `resolve()` is now a cache-checked temporary subscription (never kills others); Removed events parsed once at the boundary. `browse.rs` deleted → mdns_sd boundary restored and guarded by a grep unit test. Consumers migrated (http SSE, dispatch, commands/mdns+certmesh, main failover, integrations.rs, koi-embedded lib/handle/adapter/example); `active`-HashSet workaround + `extract_*` helpers removed in both bridges. Verify green: check --all-targets, clippy --locked -D, fmt; koi-mdns 98 (+3 ignored real-mDNS e2e, validated live), workspace + certmesh 264 (single-threaded). Plan at [plans/P05-plan.md](plans/P05-plan.md). |
| P06 | done | 2026-06-13 | opus-4-8 | 599504b | New `koi-dashboard` crate absorbs the dashboard + mDNS browser (HTML, SSE, browse cache, the single event forwarder + browse adapter, lazy meta-browse); koi-common restored to a **types-only kernel** — 5 presentation deps removed (`tokio`, `tokio-stream`, `tokio-util`, `async-stream`, `hostname`); `axum`/`chrono` kept (legit kernel uses). **XSS (claim 9) closed structurally**: mdns-browser.html rebuilt with DOM construction (createElement+textContent+dataset, zero innerHTML of dynamic values) + `safeLaunchUrl()` http/https allowlist (drops `javascript:`/`data:`); dashboard.html `esc()` hardened (quote-safe) + activity-log ported to DOM construction; CSP header on both pages. Meta-browse now **lazy** (starts on first browser request, idles after 300s; `koi status` shows `Browse: active\|idle`); default daemon startup performs no LAN-wide browsing. Both consumers (binary + koi-embedded) wired to the new crate; `adapters/mdns_browser.rs` + `mdns_browse_adapter.rs` + embedded inline forwarder deleted; all 3 daemon start sites updated (main.rs, platform/windows.rs, embedded). Tests: koi-dashboard 6 unit (forward map fns + lazy controller) + 5 integration (XSS asset guards written **failing-first**, hostile-snapshot inertness, CSP, dashboard DOM guard). Security-reviewer pass: headline XSS **fully closed**, no CRITICAL/HIGH (its MEDIUM dashboard-log + CSP recs applied). Verify green: check/clippy(-D)/fmt; workspace tests + certmesh 264 (single-threaded); leakage gate — `cargo tree -p koi-dns` shows async-stream/hostname **absent**. Plan at [plans/P06-plan.md](plans/P06-plan.md). |
| P07 | pending | | | | One orchestrator — single composition layer (prereq P03–P06 ideally). |
| P08 | pending | | | | Certmesh diet — 18.6k → ~9k LOC. |
| P09 | pending | | | | CLI surface unification — fold command-surface. |
| P10 | pending | | | | Domain template extraction (prereq P07). |
| P11 | pending | | | | koi-mcp server (prereq P03). |
| P12 | pending | | | | ACME facade (prereq P08). |
| P13 | pending | | | | Ecosystem doors — Prometheus SD, Traefik labels, DNS recipes, `koi trust` (prereq P03). |

## Divergence log

When a pre-flight check fails or repo reality contradicts a prompt, record it
here: date, prompt ID, what was found, what was done instead.

| Date | ID | Finding | Action |
|---|---|---|---|
| 2026-06-13 | P03 | Prior-session attempt left 4 uncommitted files that did not compile: `Command::Token` enum variant added with no dispatch arm (E0004); `init_logging` else-branch lost its `Ok(vec![stderr_guard])` return (E0308) — an unrelated regression; two `platform/windows.rs` call sites not updated for the new `startup_diagnostics`/`http::start` signatures (E0061×2); plus a clippy-failing unused-import warning. Token command, Windows firewall rule, tests, and all docs were absent (~40% done). | Reverted the 4 files via `git restore` (full diff captured in session history); workspace compiles again. Rebuilding P03 cleanly from a written plan. |
| 2026-06-13 | P01/P02 | Validation found two committed prompts substantively done but with minor gaps: P01 left 2 tokenless `curl -s -X POST` examples in CONTAINERS.md (escaped the acceptance grep, which only matched `curl -X POST`); P02's release.yml still `jq`-read the deleted `version.json`, which under `shell: bash` (`-eo pipefail`) would hard-fail every release; build.ps1 docstring described removed timestamp machinery. | Patched all three in the working tree (uncommitted): added `x-koi-token` headers, switched release description to a CHANGELOG.md awk extraction, rewrote the build.ps1 docstring. |
| 2026-06-13 | P04 | Prompt's "Keep: `allow_remote` (loopback vs 0.0.0.0 listen — preserve its security semantics)" describes a **listen-side** semantic that contradicts the code: `allow_remote`/CLI `--backend-remote` actually gates whether the **backend** may be non-loopback (`safety.rs`), and the listener always binds `0.0.0.0`. | Preserved the existing **backend-loopback-gate** semantic and the `0.0.0.0` listen (a TLS reverse proxy is meant to be LAN-reachable; TLS is its security boundary — distinct from P03's loopback-default control-plane HTTP). Honors "preserve its security semantics" + charter's no-silent-behavior-change rule. |
| 2026-06-13 | P04 | certmesh writes member certs to `certs/<hostname>/` (`koi-certmesh/certfiles.rs`) but the proxy read `certs/<entry.name>/` (`listener.rs`), so certmesh certs were never found unless entry name == hostname; and `load_tls_config` had **no self-signed fallback** despite `proxy.md` claiming one (so a proxy on a fresh machine failed to start at all). | Rebuild resolves certs in order: `certs/<entry.name>/` → `certs/<hostname>/` → generated self-signed (rcgen). Makes the common single-host certmesh case work and gives true zero-config TLS. Full certmesh↔proxy cert-naming reconciliation deferred to a certmesh prompt. |
| 2026-06-13 | P04 | **Acceptance criterion 7 (crate smaller / well under 954 src lines excl tests) not met:** production src grew ~871 → ~1117. The forwarding path collapsed (`copy_bidirectional` replaced the 85-line `forwarder.rs`; reqwest/hyper/http-body-util/futures-util deps dropped), but the rebuild added what the broken original lacked — working cert resolution, self-signed fallback, hot-reload via `ResolvesServerCert`, real listener-state plumbing (~34 broken cert lines → ~470 working listener+tls lines). | Kept correctness (criteria 2/3) + the charter's non-negotiable DX (#2 zero-config first success ⇒ self-signed fallback) over the size target. Removing self-signed changes user-facing behaviour, regresses DX, and still would not reach <871 — so per the charter it was **surfaced**, not silently applied. **Resolution (2026-06-13): user accepted the honest size as the correct behaviour** — keep DX + correctness, no further trimming or feature removal. |
| 2026-06-13 | P04 | Pre-existing `clippy::too_many_arguments` on `crates/koi/src/adapters/http.rs::start` (8 args, P03 code, untouched by P04) failed `cargo clippy -- -D warnings` under the machine's clippy **0.1.95** (repo MSRV is 1.92); P03 was verified under an older clippy. | Added a one-line behaviour-neutral `#[allow(clippy::too_many_arguments)]` on the wiring entrypoint to keep the shared hard gate green. No other pre-existing lints surfaced behind it. |
| 2026-06-13 | P05 | Prompt cites **mdns-sd 0.17** (line/symbol refs); the workspace is on **0.20.0** since v0.3.0. | Re-verified the single-querier defect against the 0.20 source (`exec_command_browse` overwrite at `service_daemon.rs:3540`; `exec_command_stop_browse` + `cache.remove_service_type` at 3717/3735; `ServiceRemoved(ty_domain, fullname)`, `ServiceFound(ty, fullname)`). Bug premise holds identically; the "do not upgrade mdns-sd" caveat was already satisfied. |
| 2026-06-13 | P05 | The adversarial design pass found the prompt's implied "release lock → `await browse` → re-lock with `refcount==1` rollback" sequence has a **double-browse race** (two concurrent vacant inserts each call the real `browse` → the 2nd overwrites the 1st querier = the very bug) plus an ABA-racy rollback. | Made `subscribe_type` **synchronous**: under a short `std::sync::Mutex` it `entry().or_insert_with(..)` and `tokio::spawn`s the pump; the pump performs `daemon.browse().await` as its first action. Exactly one pump (one real browse) per type can exist, and the std-mutex is never held across an await — so the guard's sync `Drop` (refcount--/abort/stop_browse) cannot deadlock. |
| 2026-06-13 | P05 | The prompt's "`resolve()` … gains: a concurrent subscriber's browse warms its cache" does not hold for a late broadcast subscriber: mdns-sd replays its cache only to the *first* listener at browse-start, so in daemon mode (permanent `MdnsBridge` per-type browses keep types Live) `resolve()` would attach with no replay and spuriously time out for an already-resolved service. | Added a per-type **records cache** in the hub (pump updates it on Resolved/Removed); `resolve()` checks it for an immediate hit before waiting. Realizes the intended "warm cache" as a hub-level cache and fixes the latent resolve regression. Mandatory, not a nicety. |
| 2026-06-13 | P05 | koi-mdns had **zero real-network tests** and `MdnsCore`/`MdnsDaemon` were never constructed in tests; a literal "register + two subscribe" acceptance test needs real multicast, which is not guaranteed in CI and would risk flaky reds on the green release pipeline. | Acceptance tests are **deterministic** and proven at the mdns-sd boundary via `#[cfg(test)]` browse/stop-browse counters: `n_subscribers_collapse_to_one_real_browse` (3 subs → exactly 1 real browse), `last_drop_issues_exactly_one_stop_browse` (non-last drop → 0 stops, last drop → exactly 1), `distinct_types_start_distinct_browses`, `resolve_reuses_live_browse_without_starting_another`. Fan-out/resolve-isolation also covered via a `#[cfg(test)] inject` seam. Three thorough `#[ignore]` real-multicast e2e tests (`real_two_subscribers_both_resolve_same_service`, `real_dropping_one_subscriber_keeps_the_browse_alive` — the exact regression, `real_resolve_does_not_terminate_concurrent_subscriber`) are kept ignored (CI multicast not guaranteed) but **all validated live this session**. (Initial suite was the inject seam only + one shallow `is_some` e2e; hardened after review for being partly tautological.) |
| 2026-06-13 | P05 | `--all-targets` clippy surfaces two **pre-existing** lints in test code untouched by P05: `collapsible_match` in `koi-runtime/tests/docker_integration.rs` and `items_after_test_module` at `koi/src/main.rs:1608` (P03 `http_bind_tests`). | Left as-is. The CI gate and CHARTER verify command are `cargo clippy --locked -- -D warnings` (no `--all-targets`, lib+bins only) — both lints are outside that gate, in `tests/`/test-module code, and unrelated to mDNS; fixing them would expand the diff into other crates/P03 code against "only intended changes". |
| 2026-06-13 | P04 | At branch creation `dev` carried 4 pre-existing **uncommitted** files unrelated to P04 (`.agentic/CONTEXT.md`, `.github/workflows/ci.yml`, untracked `docs/SURFACES.md`, `scripts/lint-surfaces.sh`). | They were cleaned from the working tree externally mid-session; the final P04 tree contains only proxy-rebuild changes. The `.agentic/CONTEXT.md` edit in this branch is P04's own (koi-proxy dependency line), not the earlier surfaces work. |
| 2026-06-13 | P06 | Prompt's "north star" gives `koi-dashboard → koi-common (types only), axum, tokio`. But the diverged duplicates to unify — the event forwarder (`match koi_mdns::MdnsEvent`/`koi_runtime::RuntimeEvent`/…) and the browse adapter (wraps `MdnsCore`) — reference **concrete domain event types**. A koi-common-only crate cannot host them, so acceptance #4 ("one event forwarder; embedded duplicates deleted") would be unachievable. | `koi-dashboard` depends on the 6 **event-bearing** domain crates (mdns, certmesh, dns, health, proxy, runtime). It is a **composition/presentation** crate (peer to the binary's adapters), not a domain — nothing depends on it but the two top-level consumers, so the kernel + every domain closure stay clean. Kernel restoration (acceptance #1) is fully met. |
| 2026-06-13 | P06 | Prompt frames the kernel removals as "axum/async-stream/chrono/hostname". Precise grep shows `axum` is used by koi-common's own `error.rs`/`http.rs` (wire error responses) and `chrono` by `integration.rs` (`MemberSummary.cert_expires: DateTime<Utc>`) — **not** presentation-only. | Removed only the genuinely presentation-only deps: `tokio`, `tokio-stream`, `tokio-util`, `async-stream`, `hostname` (5). Kept `axum`/`chrono`/`utoipa` (legit kernel uses). The literal `cargo tree -p koi-dns \| grep axum\|chrono` still matches — koi-dns depends on `axum` **directly** (its own routes) and chrono comes via axum — that is **not** leakage through koi-common. Proven: `cargo tree -p koi-dns -i async-stream`/`-i hostname` → "did not match any packages" (absent entirely). |
| 2026-06-13 | P06 | Acceptance #4 says the forwarder covers "all domains incl. runtime/**udp**". UDP exposes **no** lifecycle-event broadcast — only per-binding datagram streams (`UdpRuntime::subscribe(id)`). | The unified forwarder superset = mdns + health + dns + certmesh + proxy + **runtime** (runtime is the real daemon/embedded divergence). There is no udp arm because there is no udp event channel to forward — faithful to the code, not an omission. |
| 2026-06-13 | P06 | The two **snapshot builders** (binary `build_snapshot_value` with capability cards + detail panels; embedded `build_embedded_snapshot` with cards only) are duplicated **and diverged**. | Left per-consumer: the prompt names only the *forwarder* + *browse adapter* for dedup and says "keep the SnapshotFn injection — it's good". Unifying would change embedded dashboard behaviour (add detail panels) — out of P06 scope. The injection seam is preserved; each consumer injects its own snapshot. |
| 2026-06-13 | P06 | The verified XSS is on `mdns-browser.html` (attacker data → double-quoted attributes). `dashboard.html` routes attacker data (mDNS names) only into **text context** via `esc()` and interpolates only JS-literals into attributes — not independently exploitable. | Full structural DOM-construction rewrite applied to `mdns-browser.html` (the verified-XSS page). `dashboard.html` got the cheaper defense-in-depth: quote-safe `esc()` + its one LAN-attacker path (the activity log) ported to DOM construction + a CSP header. Both recommended by the security-reviewer pass (no CRITICAL/HIGH found). |
| 2026-06-13 | P06 | `cargo clippy --workspace --all-targets -- -D warnings` fails on a **pre-existing** `collapsible_match` lint in `koi-runtime/tests/docker_integration.rs` (untouched by P06; already noted in P05's divergence log). | Left as-is. The CHARTER/CI gate is `cargo clippy -- -D warnings` (lib+bins, no `--all-targets`) — green. koi-dashboard is clean under `--all-targets`. Fixing koi-runtime test code is out of scope ("only intended changes"). |

## Process notes

- **Plan files:** none of P01–P03's first passes left the CHARTER-mandated
  `plans/<id>-plan.md`. Enforced from P03 onward.
- **Binary crate package name** is `koi-net` (not `koi`); `cargo` invocations that
  target the binary use `-p koi-net`.
- **Verify** with `cargo check --workspace`, `cargo test`,
  `cargo clippy -- -D warnings`, `cargo fmt --check` before marking a row `done`.

## Releases

- **v0.3.0** — shipped 2026-06-13 (tag `v0.3.0` → release.yml run #33, Success): GitHub
  Release with 6 platform binaries + all 15 crates published to crates.io. Bundles
  P01–P04 + the dependency/security modernization (hickory 0.26, 6 RUSTSEC TLS CVE
  patches, clean `cargo audit`, netdev removal); green CI across Linux/Windows/macOS.
